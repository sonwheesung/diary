# 구독 시스템

> 정책(가격·상품 구성·원가)은 [`../CLAUDE.md`](../CLAUDE.md) §7.2.
> 광고 정책은 §7. 백업은 [`BACKUP_SYSTEM.md`](./BACKUP_SYSTEM.md).

---

## 1. 상품은 **하나**다

```
조각 Pro   월 ₩3,900 / 연 ₩29,000 / 7일 무료 체험
  ├─ 광고 제거      ✅ 구현
  ├─ 백업/복원      ✅ 구현 (텍스트 1차)
  └─ AI 요약 리포트  ❌
```

**엔타이틀먼트 키는 `pro` 하나다.** `remove_ads`로 쪼개지 않는다 — 제일 싼 상품이 제일 비싼
상품을 잡아먹는다(§7.2).

### 업그레이드·다운그레이드가 **없다**

월↔연 전환도 **하지 않는다**(2026-08-11 사용자 결정). 그래서 구현이 크게 단순해진다:

| 안 하는 것 | 왜 없어도 되나 |
|---|---|
| `purchasePackage(pkg, upgradeInfo, productChangeInfo)`의 2·3번째 인자 | 갈아탈 상품이 없다. **`purchasePackage(pkg)` 한 인자만 쓴다** |
| proration 모드 선택 | 전환이 없으니 비례 정산도 없다 |
| "현재 플랜" 표시·비교 UI | 살 수 있는 것이 하나뿐이다 |

⚠ 월/연은 **같은 엔타이틀먼트의 두 가격**일 뿐이다. 둘 다 `pro`를 준다.
사용자가 월에서 연으로 옮기고 싶으면 **Play 구독 관리에서 직접** 한다 — 앱은 그 링크만 준다.

---

## 2. 로그인이 **먼저**다

**로그인하지 않으면 결제 화면을 열지 않는다.**

```
로그인 → Purchases.logIn(subject_id) → 결제 화면 → purchasePackage
```

### ⚠ 이 순서를 어기면 되돌릴 수 없다

`Purchases.logIn()`을 부르기 전에 결제하면 RevenueCat이 **익명 appUserID**를 만들고,
그 구독은 **subject와 영영 매칭되지 않는다**(`CLAUDE.md` §7.2 함정 #1 —
"웹훅 이력에 `anonymous-app-user-id`"). 돈은 나갔는데 `pro`가 안 붙는다.

그래서 게이트를 **두 겹**으로 둔다:
1. 로그인하지 않았으면 구독 화면 자체를 열지 않는다(로그인 화면으로 보낸다)
2. `purchasePackage` 직전에 **RC의 현재 appUserID가 익명이 아닌지** 다시 확인한다

### 계정이 바뀌면 `restorePurchases()`

⚠ **탈퇴 후 재가입하면 `subject_id`가 바뀐다**(`softDeleteSubject()`가 `provider_id`를
가명화한다 — `CLAUDE.md` §7.2). 그러면 RC의 새 appUserID에는 아무것도 없어서
**돈은 나가는데 `pro`가 아니다.**

→ 로그인 후 서버가 `pro=false`인데 스토어에 구독이 있을 수 있으면 `restorePurchases()`를 부른다.
  서버 쪽 `TRANSFER` 웹훅 처리는 common_server Phase 9에 이미 있다.

### 1 구독 = 동시에 **1 계정**

RC의 이동은 *공유*가 아니라 *이동*이다. 같은 기기에서 다른 구글 계정으로 로그인하면
**구독이 그쪽으로 옮겨가고 원래 계정은 잃는다.** 조각은 혼자 쓰는 일기 앱이라 그대로 간다 —
다만 **구독 화면에 이 사실을 적는다.** 가족·기기 공유를 기대하고 결제한 사람에게 조용히
뺏기는 것이 가장 나쁘다.

---

## 3. SDK 사용 (실측 확인, 2026-08-11)

`react-native-purchases@10.7.0`. 타입 원문(`dist/purchases.d.ts`)에서 확인한 시그니처:

```ts
static configure(configuration: PurchasesConfiguration): void
static logIn(appUserID: string): Promise<LogInResult>
static logOut(): Promise<CustomerInfo>
static getOfferings(): Promise<PurchasesOfferings>
static purchasePackage(aPackage, upgradeInfo?, productChangeInfo?, googleIsPersonalizedPrice?): Promise<MakePurchaseResult>
static restorePurchases(): Promise<CustomerInfo>
static getCustomerInfo(): Promise<CustomerInfo>
```

공식 예제(MagicWeather)가 쓰는 패턴:

```ts
const offerings = await Purchases.getOfferings();
if (offerings.current !== null && offerings.current.availablePackages.length !== 0) { … }

const { customerInfo } = await Purchases.purchasePackage(pkg);
if (typeof customerInfo.entitlements.active[ENTITLEMENT_ID] !== 'undefined') { … }

// 취소는 오류가 아니다
if (e.code === Purchases.PURCHASES_ERROR_CODE.PURCHASE_CANCELLED_ERROR) { … }
```

⚠ **네이티브 모듈이라 Expo Go에서 못 돈다.** Expo Go에서는 "Preview API Mode"로 JS 목이
대신 응답하므로 **실제 결제는 dev build에서만** 확인된다. 조각은 이미 dev build다(§7).

---

## 4. 엔타이틀먼트의 진실은 **우리 서버**다

RC가 주는 `customerInfo`를 **최종 판정에 쓰지 않는다.**

```
결제 → RC → common_server 웹훅 → entitlements 테이블 → /api/v1/entitlements → 앱 캐시
                                                              ↑ 여기가 진실
```

- 앱은 결제 직후 `customerInfo`로 **화면만 즉시 갱신**하고(사용자를 기다리게 하지 않는다),
  곧바로 `useEntitlementStore.refresh()`로 서버 값을 받아 덮는다.
- 웹훅이 늦으면 잠깐 어긋날 수 있다 — 그래서 결제 직후 **몇 초 간격으로 재조회**한다.

⚠ RC의 판정만 믿으면 서버가 모르는 `pro`가 생기고, 백업 서버는 그걸 인정하지 않는다
(백업 쓰기는 `/entitlements`를 introspect한다). 둘이 갈리면 "구독했는데 백업이 안 된다"가 된다.

---

## 5. ⚠ 전자상거래법 — 착수 전에 매듭 (2026-08-11 원문 확인)

**한국에서는 Google이 아니라 우리가 판매자(merchant of record)다.** Google의 MOR 국가
목록에 한국이 없고, DDA §3.4·§11.3이 *"You are the merchant of record … as the principal
to the transaction"*이라 못박는다. 그래서 아래가 전부 우리 책임이다.

| | 상태 |
|---|---|
| ✅ **§13⑥ 무료체험 → 유료 전환 별도 동의**(2024-02-13 신설) | ~~미구현~~ → **구현 완료**(2026-08-17 실측 정정). `app/subscribe.tsx`의 전용 동의 화면 — 전환 일시·전후 가격·결제방법·해지 4항목 + 체크박스. 계산은 `features/subscription/trial.ts`(순수 계층), 문구는 15개 언어. 아래 §5.1 참조 |
| 🔴 **§17⑥ 단서 — 청약철회 제한** | 디지털콘텐츠는 *"표시 **와 함께** 시험 사용 상품 제공"* 을 **둘 다** 해야 한다. 안 하면 §17②5에 해당해도 **청약철회가 그대로 인정**된다. ✅ **7일 무료 체험이 시행령 §21조의2 2호를 충족한다** — 체험은 마케팅이 아니라 **법적 자산**이다 |
| ✅ **통신판매업 신고 — 완료** | **2026-07-14 신고.** 공정위 공개 조회 실측(2026-08-14): `제2026-울산중구-0170호` · 신고기관 울산광역시 중구 · 사업자등록번호 749-25-02260 · 운영상태 `통신판매업신고`. **출시 블로커에서 빠진다** |
| ✅ **§13① 표시 6종 — 이미 충족** | **Play 스토어 등록정보가 그 표시·광고다**(아래 §5.1). ⚠ ~~앱·웹에 따로 넣을 필요가 없다~~ → **§13②까지 확대 적용한 것이 틀렸다**(2026-08-17 정정). ①은 청약을 받으려는 **광고**에, ②는 계약 **거래조건**에 붙는 다른 요구다 — ②1호의 상호·대표자·주소·전화번호는 **이용약관 제2조**가 담는다 |
| ✅ **§13② 거래조건 고지 + 계약내용 서면** | **이용약관으로 충족**(아래 §5.2). 각 호 ↔ 조 매핑표가 거기 있다 |
| ✅ **§13③ 미성년자 고지** | 구독 동의 화면에 상시 표시(아래 §5.2). 나이를 모르므로 모두에게 보여주는 것이 유일한 이행 방법이다 |
| 🟡 VAT 10% · 환불 1차 책임 | 둘 다 우리가 진다(국외 개발자만 Google이 대납) |
| 🟡 **탈퇴해도 구글 구독은 계속 청구** | 탈퇴 다이얼로그에 경고 + Play 구독 관리 링크. 15개 언어 |

~~⚠ **가격은 Play에 상품을 등록하기 전까지만 자유롭다.** 등록 직전에 한 번 더 확정한다.~~
→ **확정 완료(2026-08-11, 사용자 재확인)**: 월 **₩3,900** / 연 **₩29,000** / 무료 체험 **7일**.
  이 값으로 Play에 등록한다. 이후 인상은 기존 구독자 동의가 필요하므로 **되돌리기 어려운 지점**이다.

⚠ 체험 7일은 마케팅이 아니라 **법적 자산**이다 — 전자상거래법 §17⑥ 단서의 *"시험 사용 상품 제공"*
  요건을 충족해 청약철회 제한의 근거가 된다(§5). **없애면 그 근거를 잃는다.**

### ✅ 5.1 사업자 정보 표시 — **Play 스토어로 충족된다** (2026-08-14 원문 확인)

~~앱 안에 사업자 정보 화면을 만들어 §10① 6종을 표시해야 한다~~ → **과잉이었다. 필요 없다.**

**§10의 수범자를 잘못 봤다.** 조문은 *"사이버몰의 운영자"* 에게 의무를 지우는데, 앱 판매 구조에서
사이버몰 운영자는 **Google Play**이고 개발자는 거기 **입점한 통신판매업자**다.
§20(통신판매중개)이 중개자에게 입점 판매자의 신원정보를 소비자에게 제공하라고 요구하고,
Play의 **"개발자 연락처"** 표시가 정확히 그 이행이다 — 법이 설계한 대로 작동한다.

| 조항 | 수범자 | 조각 |
|---|---|---|
| §10 사이버몰 표시 | **Google** | 개발자 의무가 아니다 |
| §13① 표시·광고 | 통신판매업자 | **스토어 등록정보가 그 표시·광고** → 충족 |
| §12 신고 | 통신판매업자 | ✅ 2026-07-14 완료 |

⚠ **발란 사건(공정위 2025-1-24 약식2025-018)을 오독했다.** 주문 원문은
*"자신의 **웹** 사이버몰 초기화면에 …전화번호·전자우편주소 및 호스팅서비스 제공자의 상호를
표시하여야 한다"* 이다 — 발란은 **자기 웹몰을 직접 운영**한다. 앱 스토어 입점 개발자와 구조가 다르다.
⚠ 같은 인용에서 *"과태료 300만원"* 이라고 적었던 것은 **WebFetch 요약을 원문처럼 옮긴 것**이라
  **철회한다.** 주문에서 확인된 것은 시정명령이고 금액은 미검증이다.

⏭ **미해결**: "앱 자체가 별개의 사이버몰인가"에 대한 유권해석은 못 찾았다. 앱 안에 구독 화면이
있으니 가상의 영업장으로 보는 해석도 가능하다. **이론적 여지이고 실무는 위 결론 쪽이다.**

#### 🟡 남는 것은 법이 아니라 노출 — 개인 휴대폰

Play가 표시하는 번호가 **대표자 개인 휴대폰**(`010-9926-0925`)이고, 한국 사용자에게
스토어 페이지 하단에 노출된다. **세 프로젝트(조각·배구명가·my_word)가 같은 번호를 쓴다.**

**지금은 바꾸지 않는다**(2026-08-14 결정):
- 법적 리스크가 없다(위 표) · 조각은 미출시라 급하지 않다
- 050으로 바꾸면 **정부24 변경신고**(§12② 신고사항 변경)까지 따라온다 — 전화번호는 §12①1호 신고사항이다
- 이미 출시된 두 앱의 과거 노출은 지금 바꿔도 회수되지 않는다
- ⏭ 정리할 때 **세 프로젝트 + 정부24를 한 번에** 바꾸는 것이 공수가 적다

### §13⑥ 전환 동의 — 어떻게 받는가 (2026-08-11 결정)

조문이 요구하는 것은 **고지가 아니라 동의**다. 화면 아래 작은 글씨로 적어두는 것으로는
성립하지 않는다. 그래서 **체험이 붙은 상품을 누르면 전용 확인 단계**를 세운다.

| 조문이 요구하는 것 | 우리가 보여주는 것 |
|---|---|
| 전환 **일시** | *"2026년 8월 18일부터"* — 오늘 + 체험 기간(상품의 `introPrice`에서 읽는다) |
| 변동 **전/후 가격** | *"지금 ₩0 → 그 뒤 ₩3,900/월"* — 둘을 **나란히** 적는다 |
| **결제방법** | *"구글 플레이 결제수단으로 자동 결제"* |
| 해지 **조건·방법·효과** | *"종료 24시간 전까지 Play 구독 관리에서 해지. 해지해도 체험 기간 끝까지 쓸 수 있어요"* |

지킬 것:

- **명시적 동작이어야 한다.** 체크박스를 켜야 결제 버튼이 살아난다. **뒤로가기는 동의가 아니다**
  (자동 소멸 배너·기본 체크 금지 — PD&C와 같은 원칙).
- **체험이 없는 상품에는 띄우지 않는다.** §13⑥은 무료→유료 전환 조항이다. 없는 절차를
  만들면 구매 마찰만 는다. `introPrice`가 `null`이면 곧장 결제로 간다.
- **날짜는 문장 틀을 통째로 번역한다**(§9.1). 조각을 갈아끼우면 언어마다 순서가 다르다.
- ⚠ **전환 시각의 진실은 Play다.** 우리는 스토어가 알려준 체험 길이로 계산해 보여줄 뿐이고,
  실제 청구는 Play가 한다. 그래서 문구를 *"체험은 7일이에요"* 로 쓰고 분 단위를 약속하지 않는다.

⚠ 이 동의는 **§17⑥ 청약철회 제한의 짝**이기도 하다. 체험 제공(시행령 §21조의2 2호)과
  표시를 **둘 다** 해야 제한이 선다 — 체험을 없애면 청약철회 제한 근거를 잃는다.

### 이용약관 — 왜 쓰는가, 무엇을 담는가 (2026-08-17)

**한때 "이용약관은 법정 의무가 아니다"로 정리했다가 조문을 읽고 뒤집었다.**

처리방침은 개인정보보호법 §30이 *"수립하여 공개하여야 한다"* 고 직접 명한 법정 의무지만,
약관은 *"약관을 만들라"* 는 조문이 없다. 거기까지는 맞다. 그런데 **전자상거래법 §13②**가
계약 체결 **전** 고지 + 계약 후 **서면 교부**를 명하면서 각 호에 이런 것들을 열거한다:

| 호 | 요구 | 약관의 어디 |
|---|---|---|
| 1 | 상호·대표자 성명·주소·전화번호 | 제2조 |
| 2·2의2 | 재화등의 명칭·종류·내용 | 제5조 |
| 3 | 가격과 지급방법·지급시기 | 제7조 |
| 4 | 공급방법 및 공급시기 | 제9조 |
| **5** | **청약철회등의 기한·행사방법·효과** | 제12조 |
| **6** | **환불 및 환불 지연 배상금의 조건·절차** | 제13조 |
| 7 | 전송·설치에 필요한 기술적 사항 | 제10조 |
| 8 | 소비자피해보상·불만·분쟁 처리 | 제21조 |
| **9** | **거래에 관한 약관과 그 확인 방법** | 제4조 |
| 11 | 추가 부담 비용·판매조건 제한(시행령 §20) | 제7조·제8조 |

**5·6·8·9호는 약관 말고 담을 그릇이 없다.** 특히 9호는 약관의 존재를 전제한다.
→ 그래서 근거는 *"약관이 의무라서"* 가 아니라 **"§13②를 충족하는 그릇이라서"** 다.

⚠ 이 정정이 필요했던 이유: §13①(표시·광고)이 스토어 등록정보로 충족된다는 결론(§5.1)을
**§13② 전체로 확대 적용**하고 있었다. 두 항은 다른 것을 요구한다 —
①은 청약을 받으려는 **광고**에, ②는 계약 **거래조건**에 붙는다.

#### 법이 파놓은 함정 3개

| 조문 | 흔한 실수 | 약관이 쓴 것 |
|---|---|---|
| **§35** 소비자에게 불리한 계약의 금지 | *"회사는 일체의 책임을 지지 않습니다"* | 제20조 첫 줄을 **"이 약관의 어떠한 조항도 법령이 정한 회사의 책임을 배제하거나 제한하지 않습니다"** 로 시작한다. 넓은 면책은 그 조항만 무효가 되는 게 아니라 **불리한 약관을 쓴 사업자**가 된다 |
| **§36** 전속관할 | 본점 소재지(울산) 관할 | 제22조는 **이용자 주소지 전속관할**. 법이 정한 대로만 적는다 |
| **§17⑥** 청약철회 제한의 조건 | 제한을 **표시만** 하고 끝 | 디지털콘텐츠는 *"표시 **와 함께** 시험 사용 상품 제공"* 을 **둘 다** 해야 한다. 제12조가 7일 체험과 짝으로 적혀 있다 |

🔴 **§17⑥ + 시행령 §20조의2가 7일 체험을 구조적 제약으로 만든다.**
체험은 ① 청약철회 제한의 법적 근거이고 ② §13⑥ 전환 동의를 **전환 30일 이내**에 받게 해준다.
없애면 ①을 잃고, 30일보다 늘리면 ②가 깨진다(구매 시점 동의가 창 밖으로 나간다).
**7일은 마케팅 선택이 아니다.**

#### 담는 곳

```
정본        features/legal/legal-text.ts  TERMS (22조 · 지문 898aa8d7)
앱 화면     app/terms.tsx                 설정 → 이용약관 (처리방침 위)
                                          구독 동의 화면에도 링크 — §13②9호의 "확인 방법"
게시        docs/terms.html               https://sonwheesung.github.io/diary/terms.html
                                          ⚠ 약관 제4조가 이 주소를 본문에 적었다 — 파일명을 바꾸면
                                             약관이 자기 자신을 잘못 가리킨다
번역        14개 언어                      처리방침보다 **급하다** — "내 돈을 어떻게 돌려받나"라서
                                          못 읽으면 곧바로 분쟁이 된다
```

⚠ **미출시라 최초 약관이다.** 공개 사용자가 0이므로 §13의 "불리한 변경 30일 전 고지"가
보호할 대상이 없다 — 구독·백업·AI가 전부 첫 출시에 들어가므로 변경이 아니라 최초다.
그래서 `pending`이 없다(CLAUDE.md §12의 2026-08-13 결정과 같은 논리).

#### ⚠ 번역본의 통신판매업 신고번호는 **검색되지 않는 형태**다

정본은 `제2026-울산중구-0170호`인데, `check:legal`이 번역본의 한글 잔존을 막으므로
번역본은 로마자(`2026-Ulsan Jung-gu-0170`)나 한자(`第2026-蔚山中区-0170号`)로 옮겼다.
**공정위 공개 조회에서 검색되는 것은 한글 원문뿐이다.**

그대로 두는 이유: ① 정본(한국어)이 정확한 문자열을 갖고 있고 모든 번역본이 *"한국어본이
우선한다"* 를 띄운다 ② Play 스토어 판매자정보가 한글 원문을 노출한다 ③ §13①3호가 요구하는
것은 *"신고를 확인할 수 있는 사항"* 이고 신고기관 이름이 함께 적혀 있다.

⏭ 더 낫게 하려면 **식별자만 한글 예외**로 두면 된다(`check:legal`에 `OPERATOR.mailOrderNumber`
문자열 허용). 그러면 15개 언어 전부가 검색 가능한 원문을 갖는다 — 번역 14개를 한 번씩
고쳐야 해서 미뤘을 뿐, 방향은 이쪽이 맞다.

⚠ CJK 3개는 대표자를 **`ソン・フィソン（Son Hwi-seong）`** 처럼 자국 표기와 로마자를 함께
적었다. 지시는 로마자였지만 그 지시의 *이유* 가 "같은 파일의 `PRIVACY_XX`와 맞춘다"였고
CJK 처리방침은 이미 가타카나·한자를 쓰고 있었다 — **이유를 따른 것이 맞다.**

### §13③ 미성년자 고지 — 결제 화면에서 모두에게 (2026-08-17)

> **제13조③** 통신판매업자는 **미성년자와** 계약을 체결할 때에는 법정대리인이 동의하지
> 아니하면 미성년자 본인 또는 법정대리인이 그 계약을 **취소할 수 있다는 내용을
> 미성년자에게 고지하여야 한다.**

우리는 이용자의 나이를 모른다 — 연령 게이트도 생년월일 수집도 없고, 일기 앱에 그걸 만드는 것은
기둥 1·3에 정면으로 어긋난다. **그래서 §13⑥ 동의 화면에서 모두에게 보여주는 것**이 이 조를
이행하는 유일하게 성립하는 방법이다. 성인에게 한 줄 더 보이는 비용이, 미성년자에게 고지하지
않는 위반보다 싸다.

⚠ **동의 체크박스에 묶지 않는다.** §13③은 **고지**이지 동의가 아니고, 묶으면 §13⑥ 동의의
대상이 흐려진다("무엇에 동의한 것인가"가 두 개가 된다). 문구는 `subscribe.consentMinor`.

### 팔지 않는 것을 혜택으로 적지 않는다 (2026-08-11)

AI 요약 리포트는 ~~아직 없다~~ → **있다(2026-08-19 표시 해제).** 구독 화면 혜택 목록에
없는 것을 두면 없는 것을 파는 것이고 Play 정책으로도 위험해서, 한동안 `benefitAi`를
**"곧 제공"으로 명시**하고 목록 아래로 내려뒀다.

**뗀 조건이 충족됐다.** 표시를 떼는 조건은 *"앱 화면이 생기는 것"* 이 아니라
**생성이 실제로 성공하는 것**이었고, 그 기준으로 두 번 미뤘다:

| 시점 | 상태 | 판단 |
|---|---|---|
| 2026-08-12 | 리포트 탭·상세는 붙었으나 `POST /api/v1/ai/report`가 **없음** | 유지 |
| 2026-08-14 | 앱에서 [만들기] 성공 — 그러나 **localhost 기준**이었다 | 유지 |
| 2026-08-18 | 라우트를 **처음으로 실제 배포**(404 → 401). `server/shared/` 생성 복사 | — |
| **2026-08-19** | 배포본에 앱이 닿고 전 구간이 도는 것을 확인 | **해제** |

→ 15개 언어의 `subscribe.benefitAi`에서 "(준비 중)" 상당 문구를 제거했다.

⚠ **Play 콘솔 구독 상품 설명은 아직 2개짜리다**(§6.2 — 광고 제거 · 백업/복원).
   AI를 넣으려면 콘솔에서 따로 고쳐야 한다. 앱 화면과 스토어 설명은 **다른 곳**이다.

⚠ 이것이 §28-8② 3호(**AI 사업자 연락처**) 블로커를 없애지는 않는다. 그 게이트는
   `npm run check:ai`가 따로 들고 있고, 혜택 표기와 무관하게 출시를 막는다.

---

## 6. 착수 순서

```
1. 앱 코드          SDK 연결 · logIn 게이트 · 구독 화면 · 복원        ✅
2. AAB 업로드       빌링 권한이 든 빌드를 내부 테스트에              ✅ v7(2026-08-12)
3. Play 콘솔        구독 상품 등록(월·연) · 체험                     ✅ 아래 §6.2
4. RevenueCat       상품 import · 엔타이틀먼트 `pro` attach · 오퍼링  ❌ 남음
5. 법무             §13⑥ 동의 플로우 · §17⑥ 표시 · 통신판매업 신고
6. 실결제 확인      첫 결제에서 `pro`가 실려 오는지 **눈으로**
```

### 6.1 AAB 업로드는 `eas submit`으로 한다 (2026-08-12 결정)

브라우저 드래그가 아니라 **EAS 서버가 Play API로 직접 민다.** 파일이 로컬에 없어도 되고
기계가 바뀌어도 상관없다 — 실제로 회사/집 크롬이 갈리는 상황에서 이 경로만 살아남았다.

```
eas submit --platform android --profile internal --id <build-id>
```

⚠ **서비스 계정에 출시 권한이 없으면 실패한다.** 그런데 그 키는 **RevenueCat도 갖고 있다**
(배구명가에서 재사용한 `revenuecat-play@optimal-shard-426006-n3`). 상시로 켜두면
RC 침해 시 테스트 트랙 게시까지 열린다. → **켜고 → 올리고 → 끈다.**

| | |
|---|---|
| 켤 것 | 사용자 및 권한 → 계정 권한 → 출시 → **`앱을 테스트 트랙으로 출시`** |
| 켜지 않을 것 | `프로덕션으로 출시` · `테스트 트랙 관리 및 테스터 목록 수정` |
| 끝나면 | **즉시 해제하고 새로고침해서 실제로 꺼졌는지 확인한다** |

🔴 **해제가 조용히 실패한 적이 있다.** 저장 확인 다이얼로그까지 눌렀는데 다시 열어보니
켜져 있었다. 화면 전환만 보고 "껐다"고 판단하지 않는다 — **리로드 후 눈으로 확인**한다.

⚠ 키 파일은 저장소 **밖**(`C:\project\secrets\play-service-account.json`)에 두고
`eas.json`은 경로만 갖는다. 저장소에 비밀이 들어가지 않는다.

### 🔴 6.1.1 Play 콘솔 계정이 바뀌었다 (2026-08-14 사용자 통보)

**`bjpio113@gmail.com` → `sonwheesung925@gmail.com`.** 이전 계정은 설정을 잘못해 새로 팠고,
기존 앱은 **계정 이전(transfer)** 으로 옮기는 중이다.

| 항목 | 상태 (2026-08-14) |
|---|---|
| 계정 변경 | ✅ `sonwheesung925@gmail.com` |
| 개발자 인증 | ✅ 완료 |
| 결제 수단 등록 | 🟡 **확인 진행 중** — 은행 입금 확인 방식 |
| 앱 이전 | 🟡 **진행 중** (최종 승인 대기) |
| 앞으로의 업로드 | **전부 `sonwheesung925@gmail.com`** |

⚠ **이전 승인 전에도 테스트는 해야 한다.** 그래서 새 계정에 **조각 stg 앱을 따로 만들어**
내부 테스트를 돌린다(§6.1.2). 이전을 기다리며 멈추지 않는다.

🔴 **이전이 무엇을 옮기고 무엇을 안 옮기는지 확인 전까지 단정하지 않는다.** 최소한 아래는
이전 후 **눈으로 다시 확인**해야 한다 — 하나라도 끊기면 *결제는 되는데 권한이 안 붙는다*(§7.2):

| 다시 볼 것 | 왜 |
|---|---|
| `jogak_pro` 상품·기본 요금제·체험 | 상품 ID는 **재사용 불가**라 사라지면 되돌릴 수 없다(§6.2) |
| **Play 서비스 계정 키** | RevenueCat과 `eas submit`이 **같은 키를 쓴다**(§6.1). 계정이 바뀌면 Google Cloud 프로젝트 연결부터 다시 본다 |
| RevenueCat ↔ Play 연동 | 서비스 계정이 갈리면 웹훅은 오는데 검증이 깨진다 |
| **앱 서명 키 / SHA-1** | 구글 로그인 OAuth 클라이언트가 이 지문에 묶여 있다(`SUPPORT_SYSTEM.md`) |
| AdMob ↔ 스토어 연결 | 지금도 "검토 필요"라 광고가 0이다(CLAUDE.md §7) |
| 데이터 보안 선언 | `User IDs` 미선언 등 미결이 그대로 따라오는지 |

⏭ 위 목록은 **미확인 가설이다.** 이전이 승인되면 항목마다 콘솔에서 실제로 확인하고
이 표를 결과로 갈아끼운다(DOC_DISCIPLINE §6 — 기억으로 단정 금지).

### 🔴 6.1.2 stg와 운영을 **다른 앱 2개**로 나눈다 (2026-08-14 사용자 결정)

```
조각 stg    내부 테스트 전용. 새 계정에 지금 만든다
조각 (운영)  이전 완료 후
```

이렇게 하면 이전 승인을 기다리지 않고 테스트가 굴러가고, 운영 트랙에 테스트 빌드가
섞이지 않는다. 대신 **앱이 둘이면 앱에 박히는 식별자가 전부 두 벌이 된다:**

| 두 벌이 되는 것 | 지금 값 | 비고 |
|---|---|---|
| `applicationId` | `com.son0925.jogak` | stg는 별도 패키지명이 필요하다 — **Play는 패키지명으로 앱을 가른다** |
| **AdMob 앱 ID · 광고 단위** | `ca-app-pub-2731473780180274~6800808068` | `app.json`에 박혀 네이티브 매니페스트로 간다. 바꾸면 `prebuild` + 재빌드(CLAUDE.md §7) |
| **구글 OAuth 클라이언트 · SHA-1** | Android 클라이언트 1개 | 패키지명+지문 쌍이라 stg용을 **따로 발급**해야 로그인이 된다 |
| RevenueCat 앱 | **2개** | ~~stg를 같은 프로젝트에 둘지 나눌지 결정 필요~~ → **같은 프로젝트에 앱을 추가**(2026-08-17). RC가 한 프로젝트에 Play 앱 여럿을 허용한다(실측). 엔타이틀먼트 `pro`·오퍼링을 **한 벌만** 관리하게 되고, 그래야 *"stg에서 검증했으니 운영도 된다"* 가 성립한다 — 나누면 가장 자주 틀리는 **attach 누락**(§7.2 함정 #2)이 stg에서 안 잡힌다 |
| 조각 서버 URL | `EXPO_PUBLIC_BACKUP_SERVER_URL` | 이미 env라 제일 쉽다 |
| common_server `app` 코드 | `jogak` | stg를 따로 등록할지 확인 필요 |

#### 확정 (2026-08-14) — 그리고 콘솔 실측

`조각 stg` 앱을 만들었다. **앱 ID `4974338730427662829`.**

| | 값 | 근거 |
|---|---|---|
| 패키지명 | **`com.son0925.jogak.stg`** | 생성 시 "사용 가능" 확인 후 확정. **재사용 불가라 되돌릴 수 없다** |
| 기본 언어 | **한국어 – ko-KR** | stg는 내부 전용이라 스토어 등록정보가 공개되지 않는다 — 콘솔에서 읽는 사람이 한국어 사용자다. ⚠ **생성 후 변경 가능 여부는 공식 문서에 없다**(확인함) |
| 앱/게임 · 유료/무료 | 앱 · **무료** | 게시 후 무료→유료 불가 |
| AdMob 앱 ID | **`ca-app-pub-3940256099942544~3347511713`** | Google 공식 **테스트** 앱 ID(문서 확인). 실제 ID를 stg에 박으면 AdMob의 스토어 연결이 꼬이고 무효 트래픽 위험이 붙는다 |
| 테스터 | `sonwheesung925@gmail.com` · `bjpio113@gmail.com` | 내부 테스트는 최대 100명 |

🔴 **새 계정은 `개인 계정`이다** (개발자 ID `6329667596149711059` · `Vivace Games Studio`).
옛 계정(`6530743120826503035` · 조직)과 다르다. 콘솔 화면의 실제 문구:

> **12명 이상의 테스터를 대상으로 14일 이상 비공개 테스트 실행**(프로덕션 액세스 신청 조건)

~~20인 요건은 조직 계정이라 해당 없다~~ → **되살아났고, 숫자도 20이 아니라 12다.**
내부 테스트는 무관하지만 **프로덕션 일정에 직접 들어간다.**
⏭ 홈에 *"조직 인증을 위한 문서 업로드"* 다이얼로그가 떠 있다 — 조직 전환이 끝나면 이 요건이 사라진다.

🔴 **Android 개발자 인증 — 등록된 패키지 이름이 0개다.**
> "2026년 9월부터 일부 지역에서 인증된 Android 기기에 앱을 설치하려면 인증된 개발자가
> 모든 Android 앱을 등록해야 합니다"

**다음 달이다.** `com.son0925.jogak`과 `com.son0925.jogak.stg` 둘 다 대상으로 보인다.
⏭ 대상 지역과 stg(내부 테스트 전용) 포함 여부는 확인하지 않았다.

#### 빌드 배선 — `app.config.js`가 가른다

`app.json`은 정적이라 패키지명을 하나만 가진다. **`app.config.js`가 `APP_VARIANT=stg`일 때만**
패키지명·앱 이름·AdMob 앱 ID를 갈아끼운다. 기본값(변수 없음)은 **운영 그대로**라
`npx expo run:android` 로컬 개발은 영향받지 않는다.

```
eas build --profile stg          → com.son0925.jogak.stg · 조각 stg · 테스트 AdMob
eas build --profile internal     → com.son0925.jogak     (운영 앱의 내부 테스트)
eas build --profile production   → com.son0925.jogak     + EXPO_PUBLIC_ADS_REAL=1
```

⚠ **광고 단위는 원래부터 안전했다.** `ads.ts`가 `EXPO_PUBLIC_ADS_REAL=1`일 때만 실제 단위를
쓰고 기본값이 테스트다. 하드코딩돼 있던 것은 `app.json`의 **앱 ID 하나**뿐이었다.

⏭ **stg에서 안 되는 것**(고칠 때까지):
- ~~**구글 로그인**~~ → ✅ **해소(2026-08-19)**. 사연은 §6.1.5 — 등록은 돼 있었는데 **지문이 하나 모자랐다.**
  `webClientId`는 그대로 쓴다 — 그건 패키지에 안 묶인다.
- **결제** — RevenueCat 앱은 Play 패키지에 매핑된다. stg용 RC 앱을 나눌지 미정.

### 6.1.5 🔴 stg 구글 로그인 `DEVELOPER_ERROR`(10) — **Play 앱 서명 키는 하나가 아니다** (2026-08-19)

versionCode 4를 내부 테스트로 받아 깔았더니 로그인이 **코드 10**으로 죽었다. 이 절은
*"등록했는데도 안 되는"* 경우를 위해 남긴다 — 우리가 정확히 그 경우였다.

#### 무엇이 틀렸나

`jogak-stg-android-play` 클라이언트는 **이미 있었고 패키지명도 맞았다.** 등록된 SHA-1은
Play Console `앱 서명 › 기존 키`에서 복사한 값이었다. 그런데 **기기가 실제로 쓰는 지문이 달랐다.**

| 지문 | 정체 | 당시 등록 |
|---|---|---|
| `D6:D9:72:C2:…:41:4D:C6` | **기기에 배포된 APK의 실제 서명** | 🔴 없음 |
| `BE:D3:98:65:…:F9:93:49` | Play Console `기존 키` | ✅ (그런데 안 쓰임) |
| `DB:1D:8C:B8:…:A2:D4:1E` | Play Console `양자 내성 암호화 키`(베타) | 없음 |
| `3A:47:42:B7:…:47:CA:F2` | 업로드 키 | 없음 |

이 앱에는 `이전 앱 서명 키`(2026-08-14 08:43부터)가 있고 그 뒤 키가 바뀌었는데,
**Play가 배포하는 빌드는 아직 옛 키로 서명하고 있었다.** Play Console은 이전 키의 지문을
**화면에 인라인으로 보여주지 않는다**(인증서 다운로드만 준다) — 그래서 콘솔만 보고는
어긋난 것을 알 수 없다.

→ `D6:D9…`와 업로드 키 `3A:47…`로 Android 클라이언트를 **두 개 더** 만들어 해결했다.
   기존 `BE:D3…`는 **지우지 않는다** — Play가 새 키로 전환하는 날 그게 필요해진다.

#### 🔴 진단은 콘솔이 아니라 **기기에서** 한다

`패키지명 + SHA-1`이 맞는지는 콘솔끼리 대조해서는 확정할 수 없다. 기기에 깔린 것을 직접 잰다:

```bash
adb shell pm path com.son0925.jogak.stg          # 스플릿이면 Play 배포본이다
adb pull <base.apk 경로> ./stg-device.apk
"$ANDROID_HOME/build-tools/36.0.0/apksigner.bat" verify --print-certs -v ./stg-device.apk
adb shell pm list packages -i com.son0925.jogak.stg   # installer=com.android.vending 확인
```

⚠ **`keytool -printcert -jarfile`을 쓰지 마라.** v1(JAR) 서명만 읽어서 요즘 APK에는
`Not a signed jar file`이 뜬다 — 이걸 "파일이 깨졌나"로 오해하기 쉽다. **AAB에는 통하고
APK에는 안 통한다**(우리 AAB는 v1이 있어서 됐다). APK는 `apksigner`다.

⚠ **케이블이 안 되면 무선 디버깅이 빠르다.** `adb mdns services`가 기기를 찾아주므로
포트를 손으로 옮겨 적을 필요가 없다 — 필요한 것은 폰이 보여주는 **6자리 코드**뿐이다.

```bash
adb mdns services                       # _adb-tls-pairing._tcp 는 페어링 팝업이 열려 있을 때만 뜬다
adb pair <ip>:<pairing-port> <code>
adb connect <ip>:<connect-port>
```

#### 이 일이 알려준 것

- **"클라이언트가 없다"와 "지문이 틀렸다"는 증상이 같다**(둘 다 코드 10). 그래서
  `features/support/auth-gate.ts`의 의심 순서 *"① SHA-1 ② 패키지명 ③ webClientId"* 는
  맞지만, ①을 **콘솔에서 확인하면 안 된다**. 실측이 아니면 같은 함정에 다시 빠진다.
- **Play 앱 서명 키는 시간에 따라 여러 개다** — 이전 키 · 현재 키 · 양자 내성 키.
  API 제공자에 등록할 때는 **관련된 것을 전부** 넣는다. 지우는 것이 위험하지 넣는 것은 안 위험하다.
- 재빌드는 **필요 없었다.** Android 클라이언트는 번들에 안 들어간다
  (`google-services.json`을 쓰지 않으므로 앱이 들고 다니는 것은 `webClientId` 하나뿐이다).
  이 사실을 먼저 확인한 덕에 20분짜리 빌드를 헛돌리지 않았다.

### 6.1.6 🔴 결제 성공 화면이 거짓말을 했다 — **RC 성공 ≠ 서버 권한** (2026-08-19)

라이선스 테스트로 첫 테스트 결제를 돌리다 잡았다. 앱이

> **구독이 시작됐어요** — 광고가 사라지고 백업을 쓰실 수 있어요.

를 띄웠는데, 설정은 `조각 Pro · 이용 안 함`이었고 **배너 광고도 그대로**였다.

#### 원인 — "낙관적으로 켠다"고 적어놓고 서버를 물었다

```ts
// 고치기 전 (features/subscription/api/purchases.ts)
if (!hasPro(customerInfo)) return { kind: 'no-entitlement' };
/* 결제 직후 사용자를 기다리게 하지 않으려고 낙관적으로 켠다. */
void useEntitlementStore.getState().refresh();   // ← 낙관이 아니다. 서버를 읽는다
return { kind: 'ok' };
```

`refresh()`는 common_server에 묻고, **서버는 RC 웹훅이 도착해야** `pro`를 안다.
결제 직후에는 거의 항상 아직 모르고, 그러면 `refresh()`가 `pro: false`를 쓰고
캐시(`pro_until`)까지 지운다 — 성공 문구를 띄운 그 화면이 스스로를 뒤집는다.

⚠ **주석이 코드와 달랐다.** 주석은 낙관을 말하는데 코드는 비관을 실행했다.
   샌드박스라 드러난 것이 아니다 — **운영에서도 웹훅 지연만큼 같은 창이 열린다.**
   그리고 그 창은 사용자가 "돈 냈는데 안 됐다"고 판단하는 자리, 즉 환불·문의로 직행하는 자리다.

#### 고친 방법 — 낙관 구간 + 백오프 확정

| | |
|---|---|
| `grantPending()` | 결제·복원 직후 `pro`를 **로컬로** 켜고 3분짜리 창을 연다 |
| `refresh()` 가드 | 창이 열려 있는 동안 서버의 *"아직 없음"* 으로 **되돌리지 않는다**(캐시도 안 건드린다) |
| `confirmWithServer()` | 1·2·4·8·15·30·60·60초 백오프로 다시 묻고, 서버가 확정하면 멈춘다 |

지킨 원칙:

- 🔴 **낙관은 메모리에만 둔다.** `pro_until` 캐시에 쓰면 서버가 끝내 확정하지 않아도
  RC 만료일(한 달)까지 무료 이용이 되어 §5의 fail-closed가 뒤집힌다.
  앱을 다시 켜면 낙관은 사라진다 — **안전한 쪽으로 잃는다.**
- 🔴 **창은 3분으로 닫힌다.** 웹훅이 끝내 안 오면(SANDBOX 결제가 정확히 그렇다)
  화면이 정직하게 `이용 안 함`으로 돌아가야 한다. 무한 낙관은 거짓말의 다른 형태다.
- **복원(`restore`)에도 같이 걸었다.** 서버는 `TRANSFER` 웹훅을 기다리므로 같은 경주를 한다.

⏭ 남은 한계: 3분 안에 웹훅이 안 오고 앱을 끄면 사용자는 다음 실행에서 `이용 안 함`을 본다.
   그때 답은 **`구매 내역 복원`** 이고 그 버튼은 이미 구독 화면에 있다.

### 6.1.7 🔴 구독 전수조사 — **웹훅은 약속이 아니다** (2026-08-19)

§6.1.6을 고친 직후 실기기에서 *"구독하면 이용할 수 있어요"* 가 또 나왔다. 그 한 번을
계기로 구독 경로를 처음부터 끝까지 훑었고, **내가 만든 버그보다 그 아래 구조가 더 문제**였다.

#### 무엇이 문제였나

| | 문제 | 어디 |
|---|---|---|
| **A1** | 🔴 서버가 RC에 **되물을 수 없다** — 웹훅 push가 유일한 입력 | common_server |
| **A2** | 🔴 앱이 **포그라운드 복귀 때 갱신하지 않는다** | `app/_layout.tsx` |
| **B1** | 🔴 낙관 구간이 **서버 게이트 기능까지 열었다** | §6.1.6에서 내가 만듦 |

#### A1 — 웹훅의 실제 성질 (2026-08-19 공식 문서 확인)

문서가 *"엔타이틀먼트의 진실은 우리 서버"*(§4)라고만 적어놓고 **그 진실이 언제 도착하는지**를
안 적었다. 실제 수치는 이렇다:

| | |
|---|---|
| 정상 도착 | **5~60초** |
| **취소(`CANCELLATION`)** | **약 2시간** |
| 실패 시 재시도 | 5·10·20·40·80분 → **5회 후 포기** |
| 실측 사례 | 첫 이벤트 **6시간+** 지연 |

→ **웹훅이 유실되면 그 사용자는 영구히 `pro`가 아니다.** 복구 경로는 사용자가 스스로
`구매 내역 복원`을 누르는 것뿐인데, 돈 낸 사람이 그 버튼이 자기 문제의 답인 줄 알 리 없다.

RevenueCat 공식 권고는 **push를 믿지 말고 pull하라**이다:

> we recommend calling the `GET /subscribers` REST API endpoint after receiving any webhook

common_server에는 그 호출 경로가 없다(`/api/webhooks/revenuecat/[app]` 수신 + `/api/v1/entitlements`
조회뿐). ⚠ **여기가 진짜 수정 자리이고, common_server는 조각에서 수정 금지다.**

✅ 다만 **웹훅 처리 자체는 견고하다** — `purchase_events_rc_event_uniq`(rcEventId UNIQUE)가
중복 배달을 `deduped`로 처리하고, 갱신 계열은 `max()`라 도착 순서를 안 탄다.
공식 권고 중 멱등·순서는 이미 지켜져 있다. **없는 것은 pull 하나다.**

#### 🔴 왜 못 찾았나 — 탐지 실패가 아니라 **경로 미주행**이다

가장 중요한 답부터: **오늘이 결제 성공 경로를 처음 밟은 날이다.** 못 찾은 게 아니라 안 밟았다.

| # | 원인 | 교훈 |
|---|---|---|
| 1 | **로컬·에뮬레이터에는 웹훅이 없다.** 개발 내내 엔타이틀먼트는 늘 "없음"이었다 | 성공 경로를 안 밟으면 *"성공 직후의 창"* 이 존재하는 줄도 모른다 |
| 2 | 🔴 **테스트가 순수 계층에만 있다.** `check:subscription` 14개는 전부 체험 기간 계산·유예 산술이고, **스토어 상태 전이 검사는 0개**다 | `refresh()`가 결제 직후 `false`를 쓰는 것을 아무도 안 봤다 |
| 3 | **주석이 코드와 반대였다.** *"낙관적으로 켠다"* 아래에서 서버를 조회하고 있었다 | 검증이 없으면 **주석이 사실을 이긴다.** 읽는 사람은 주석을 믿는다 |
| 4 | **문서가 진실의 *위치*만 정하고 *지연*을 안 적었다** | "어디가 진실인가"만큼 **"언제 도착하는가"** 가 설계 입력이다 |
| 5 | 8/14 *"앱에서 [만들기] 확인"* 은 **에뮬레이터 + localhost**였고 결제를 안 밟았다 | 초록 체크가 **무엇을 안 밟았는지**는 말해주지 않는다 |

⚠ 2번이 재발 방지의 핵심이다. 결제는 실기기·실스토어가 필요해 자동화가 어렵지만,
**스토어 상태 전이는 순수 함수에 가깝다** — 거기까지는 Node에서 검사할 수 있었다.

#### 무엇을 고쳤나 (앱에서 할 수 있는 것)

| | |
|---|---|
| **A2** | 포그라운드 복귀 시 `refresh()`. 웹훅이 늦게 와도 앱 재시작 없이 반영된다 |
| **B1** | 서버 게이트 기능은 낙관 구간에 **"확인 중"** 으로 말한다 — *"구독하면 이용할 수 있어요"* 는 방금 결제한 사람에게 하는 거짓말이다 |
| **A1 완화** | 서버가 *"없다"* 고 할 때만 **RC `CustomerInfo`에 되묻는다.** RC가 *"있다"* 면 로컬 혜택(광고 제거)은 연다 |

🔴 **A1 완화는 §4의 불변식을 수정한다.** 정확히 쓰면:

> **서버가 진실이다. 단 서버가 "없다"고 답할 때만 RC에 되묻고, RC가 "있다"면
> 로컬로 끝나는 혜택은 연다. 서버가 게이트를 쥔 기능(AI 생성·백업 업로드)은
> 그래도 열리지 않으며 "확인 중"으로 말한다.**

이것이 fail-closed를 깨지 않는 이유: RC `CustomerInfo`는 **스토어의 기록**이지 우리 추측이 아니다.
안 낸 사람이 열리는 경로가 생기지 않는다. 반대로 **낸 사람에게 광고가 나오는 것**은 실재하는 해악이다.

⚠ 그래도 **A1은 남는다.** 이 완화는 광고만 고치고 AI·백업은 못 연다 —
서버가 모르면 서버가 막는다. 웹훅 유실은 여전히 **유료 사용자 이탈**로 직결된다.

#### ✅ A1 해소 (2026-08-19, common_server `04dd12a`)

`GET /subscribers` pull이 두 자리에 붙었다 — 웹훅 수신 후 1회, 그리고
`/api/v1/entitlements`가 **부정 답을 주기 직전** 1회. 가드 31 → 47개.

| 결정 | 내용 |
|---|---|
| 쿨다운 | 🔴 **Postgres 조건부 UPDATE**(`subjects.rc_pulled_at`). 한 문장이 쿨다운과 동시성 락을 동시에 준다 |
| 주기 | 일반 **6시간** · `?fresh=1` **60초** |
| 상태 전이 | `nextState()`를 웹훅과 **공유**. 입력 파싱만 `decideEvent`/`decideSnapshot`으로 가른다 |
| 순서 역전 | `eventAt`을 **RC 요청 발사 시각**으로 찍어 기존 `lastEventAt` 가드가 되감기를 버린다 |

🔴 **내가 틀린 전제를 줬고 그쪽이 잡았다.** 나는 *"Upstash·KV가 이미 있으니 쿨다운을 거기 두라"* 고 썼는데,
`process.env.*` **이름이 코드에 있는 것**을 **설정돼 있는 것**으로 읽은 것이었다.
실제로는 `common_server/CLAUDE.md:36`이 *"Upstash·Sentry는 미설정 = 의도적 no-op"* 이고
`ratelimit.ts`가 **fail-open**이다 — 거기 쿨다운을 얹었으면 **미설정 = 쿨다운 없음**이 되어
조각이 포그라운드 복귀할 때마다 RC를 때렸을 것이다. 막으려던 것을 정확히 만들 뻔했다.
→ §6.1.7 "왜 못 찾았나" #3과 **같은 종류**다. 이름·주석은 사실이 아니다.

⚠ 내가 보탠 것: **pull 실패 시 쿨다운 롤백**(스탬프는 claim 때 찍히므로 답을 못 받고도 6시간 잠긴다)과
**"실패"의 좁은 정의**(200 *"구독 없음"* 과 404는 **정상 답**이다 — 실패로 세면 미구독자 전원이 2분마다 RC를 때린다).

#### 조각 쪽 배선

| | |
|---|---|
| `lib/common-server/index.ts` | **재복사**(원본 2026-08-19). `registerDevice`(2026-08-14)도 함께 따라왔다 — 안 쓰지만 사본은 통째로 맞춘다 |
| `refresh({ fresh })` | 스토어가 흘려보낸다 |
| `confirmWithServer()` | **결제·복원에서만** `fresh: true`. 8회를 붙여도 60초 쿨다운이라 실제 RC 호출은 2~3회 |

🔴 **포그라운드 복귀·앱 시작에는 붙이지 않는다.** 붙이면 6시간 쿨다운의 존재 이유가 사라진다.
그쪽은 `fresh` 없이도 서버가 알아서 6시간 주기로 pull한다.

⏭ 남은 실측: **RC secret key를 주입한 첫 pull.** 그쪽도 키가 없어 실제 RC 호출은 못 해봤고,
응답 필드명(`grace_period_expires_date` 등)은 **문서 기준**이다. 콘솔 웹훅 이력에 `PULL` 행이
뜨는지가 확인점이다.

⏭ 그리고 pull이 붙으면 조각의 백오프(1·2·4·8·15·30·60·60초)가 과해진다 — 첫 호출에서 답이 나온다.
   실측 후 줄인다.

### 6.1.8 🔴 **Play는 결제를 90초짜리로 발급하고 17분 뒤에 확정한다** (2026-08-19 실결제 실측)

첫 실결제(₩3,900 상당 `US$2.81`)에서 잡은 것이고, **오늘 가장 중요한 실측**이다.

```
14:06:21  결제
14:07:00  INITIAL_PURCHASE  →  만료 14:07:51   ← Play가 준 **90초짜리** 기간
14:07:51  만료 (진짜로)
14:24:19  RENEWAL           →  만료 2026-09-19 ✅  결제 확정
```

즉 결제 직후 **17분 동안 Play·RevenueCat·우리 서버가 전부 "만료"라고 답한다.**
Play가 첫 결제를 확정하기 전에 임시 기간을 발급하기 때문이고, **버그가 아니다.**

#### ⚠ 내가 이것을 서버 버그로 오진했다

`expiresAt`이 결제 50초 뒤로 찍힌 것을 보고 *"pull이 만료 시각을 잘못 읽어 덮었다"* 고
common_server에 버그 리포트를 넘겼다. 근거는 그럴듯했다 — pull이 `overwrite` 모드이고,
그쪽이 *"RC 응답 필드명은 문서 기준"* 이라고 스스로 위험을 밝혀뒀기 때문이다.

**틀렸다.** `purchase_events.raw`의 웹훅 원문이 **처음부터 90초를 실어왔고**, pull은 같은 값을
다시 썼을 뿐이다. 필드 매핑(`entitlements.pro.expires_date`)도 정확했다.

⚠ 그리고 **최소 주기 가드를 넣자는 내 제안이 거절된 것이 옳았다** —
   *"월간인데 만료가 며칠 안 되면 버린다"* 는 규칙은 이 90초를 버렸을 것이고,
   `PRODUCT_CHANGE`·조기 만료에서는 **더 긴 옛 값을 붙들어 과다 지급**이 된다.
   `overwrite` 모드가 존재하는 이유가 정확히 그것이다.

🔴 **교훈**: 증상이 이상하면 **원문을 봐라.** 나는 파생된 상태(`entitlements` 행)만 보고
   구조를 고치려 했다. 원문(`purchase_events.raw`)이 있었으면 5분에 갈렸다.

#### 진짜 문제는 그 17분이었다

만료 이후 `RENEWAL` 도착까지 **결제자가 미구독자**다. 그리고 읽기 경로의 pull은
결제 직후 찍힌 **6시간 쿨다운**에 막혀 있었다 — `RENEWAL`이 유실됐다면 6시간 잠긴다.

→ common_server가 쿨다운을 **상태에 따라** 갈랐다(`32f85c0`):

| 상태 | 쿨다운 |
|---|---|
| **갱신 예정인데 만료됨**(이벤트를 놓쳤을 수 있다) | **10분** |
| 해지하고 만료됨(답이 바뀔 일 없음) | 6시간 |
| 한 번도 구독 안 함 | 6시간 |

#### 조각 쪽 대응 — **창을 둘로 나눈다**

| | 창 | 왜 |
|---|---|---|
| 권한(`pro=true`) | **3분** | 근거 없이 오래 열면 fail-closed가 무너진다 |
| 안내("처리 중") | **30분** | 17분을 덮어야 한다 |

🔴 **하나로 합칠 수 없다.** 합치면 둘 중 하나가 반드시 틀린다 —
   권한을 17분 열면 근거 없는 부여이고, 안내를 3분에 닫으면
   *"구독하면 이용할 수 있어요"* 를 **방금 돈 낸 사람에게** 보여준다.
   `check:subscription`에 그 둘을 못박는 검사 3개를 넣었다(31개).

그리고 `confirmWithServer()` 백오프를 **3분 → 약 25분**으로 늘렸다.
뒤쪽 간격은 2~5분으로 벌렸다 — 서버가 10분 쿨다운으로 재확인하므로 촘촘히 때릴 이유가 없다.

⏭ 남은 한계: 문구가 *"잠시 뒤 다시 시도해 주세요"* 인데 실제로는 최대 20분일 수 있다.
   15개 언어를 다시 쓸 만큼의 문제인지 판단이 필요하다.

### 6.1.3 🔴 Play 서비스 계정 — **앱 권한과 계정 권한은 다른 것을 연다** (2026-08-17)

RevenueCat이 결제를 검증하려면 Play Developer API를 부를 권한이 필요하고, 그 권한은
**서비스 계정**(`revenuecat-play@optimal-shard-426006-n3.iam.gserviceaccount.com`)이 갖는다.
키 파일은 `C:\project\secrets\play-service-account.json` — 저장소 **밖**이고 `eas submit`이 같은 키를 쓴다.

#### 층위가 둘이고, RevenueCat의 검사 3줄이 그 층위를 나눠서 요구한다

| RevenueCat 검사 | 부르는 API | 필요한 권한 |
|---|---|---|
| Can read the in-app product catalog | `applications/{pkg}/subscriptions` | **앱 권한** — 앱 정보 보기(읽기 전용) |
| Can read the subscription catalog and base plans | 〃 | **앱 권한** |
| **Can validate subscription purchases** | `purchases/subscriptionsv2/tokens/…` | 🔴 **계정 권한** — 재무 데이터 + 주문 및 구독 관리 |

**앱 권한만 주면 카탈로그 2줄만 초록이 되고 구매 검증은 빨강으로 남는다.** 실제로 그렇게 겪었다.
RevenueCat 팁이 부르는 이름(*"View financial data, orders, and cancellation survey response"*)이
정확히 **계정 권한** 탭의 라벨이고, 앱 권한 탭의 비슷한 이름(`재무 데이터 보기`)과 **다른 것**이다.

⚠ 계정 권한은 **앱을 고를 수 없다.** 그 개발자 계정의 모든 앱(현재·미래)에 걸린다.

#### 계정이 둘이면 권한도 두 벌이다

```
revenuecat-play@…  (로봇 1개)
  ├─ Vivace Games (bjpio113 · 6530743120826503035)
  │    앱 권한 0개 · 계정 권한 4개          ← 조각 운영·배구명가·my_word·책담이 이걸로 돈다
  └─ Vivace Games Studio (sonwheesung925 · 6329667596149711059)
       앱 권한 조각 stg 4개 · 계정 권한 2개  ← 2026-08-17 추가
```

두 벌은 **서로 독립**이다. 한쪽을 고쳐도 다른 쪽은 안 변한다(전후 실측으로 확인).

🔴 **커지는 것은 계정이 아니라 키 하나의 도달 범위다.** 이 키가 새면 이제 두 계정의 매출·주문
조회와 **환불·구독 취소**가 가능하다. 키를 나누는 대안을 검토했으나 **채택하지 않았다** —
두 키가 같은 폴더·같은 노트북에 놓이므로 실질 위험이 안 줄고, `eas.json` submit 프로필과
RC 업로드가 두 벌이 되는 부채만 영구히 남는다. 이 저장소가 반복해서 데는 지점이 정확히
*"같은 것을 두 벌로 관리하기"* 다.

⏭ **앱 이전이 끝나면 한 번 정리한다.** 조각 운영이 새 계정으로 넘어가면 옛 계정에서 RevenueCat이
실제로 무엇을 쓰는지 다시 보고, 불필요한 권한을 빼서 반경을 줄인다.

#### 최종 상태 (2026-08-17 실측)

```
조각 운영    com.son0925.jogak         카탈로그 ✅ 200   구매 검증 ✅ 400
조각 stg     com.son0925.jogak.stg     카탈로그 ✅ 200   구매 검증 ✅ 400
배구명가      com.son0925.volleyball    카탈로그 ✅ 204   구매 검증 ✅ 400
My Word     com.myword.front          카탈로그 ✅ 204   구매 검증 ✅ 400
책담         com.chaekdam.app          카탈로그 ✅ 204   구매 검증 ✅ 400
LinkMemo    com.vivacegames.linkmemo  카탈로그 ❌ 403   구매 검증 ❌ 401   ← 권한 안 줌(의도)
```

RevenueCat 두 앱 모두 **`Valid credentials`**.

⚠ **카탈로그는 즉시, 구매 검증은 늦게 반영됐다.** 계정 권한을 저장한 뒤에도 한동안 401이
남아서 "저장이 안 됐나"를 의심했는데 콘솔에는 멀쩡히 저장돼 있었다 — **전파 지연**이었다.
저장 상태(콘솔)와 반영 상태(API)를 따로 확인해야 이 구분이 선다.

#### 진단은 대시보드가 아니라 **구글에게 직접 묻는다** — `npm run check:play-access`

RevenueCat은 *"Credentials need attention"* 만 말하고 이유를 안 알려준다. 키 문제인지·API
미활성인지·앱 인가인지 구분이 안 돼서 2026-08-17에 콘솔을 한참 뒤졌다. 스크립트를 만들어
같은 토큰으로 여러 패키지를 부르니 **한 번에 갈렸다**:

```
조각 운영      com.son0925.jogak         카탈로그 ✅ 200   구매 검증 ✅ 400
조각 stg      com.son0925.jogak.stg     카탈로그 ✅ 200   구매 검증 ❌ 401
배구명가       com.son0925.volleyball    카탈로그 ✅ 204   구매 검증 ✅ 400
LinkMemo     com.vivacegames.linkmemo  카탈로그 ❌ 403   구매 검증 ❌ 401   ← 권한 안 준 앱
```

- **같은 키로 한쪽만 실패하면 키·API·Cloud 프로젝트 문제일 수 없다.** 인가 문제다.
  대조군이 있어야 이 추론이 선다 — 그래서 스크립트가 **잘 되는 앱을 함께** 부른다.
- `204`는 정상이다. "구독 상품이 0개"일 뿐 읽을 권한은 있다.
- 구매 검증은 **가짜 토큰**으로 부른다. 권한이 있으면 `400 Invalid Value`, 없으면 `401/403`이
  온다 — 실제 구매 없이 인가만 가릴 수 있어서 아무 때나 돌린다.
- ⚠ **권한 반영에 시간이 걸린다.** 저장 직후 실패해도 잠시 뒤 다시 돌려본다. 카탈로그 권한이
  먼저 반영되고 구매 검증이 늦게 붙는 것을 실제로 봤다.

### 🔴 6.1.4 로컬에서 릴리스 AAB를 굽는 법 — 그리고 **왜 위험한가** (2026-08-18)

**EAS 무료 플랜의 월 안드로이드 빌드를 다 써서** 어쩔 수 없이 로컬로 구웠다
(*"reset in 13 days"*). 절차 자체는 되지만 **EAS가 공짜로 해주던 보호가 전부 사라진다** —
그 목록이 이 절의 본론이다.

#### 🔴 EAS가 대신 막아주던 것

| | EAS | 로컬 |
|---|---|---|
| `.env.local` | **없다** (깨끗한 컨테이너) | **읽는다** — `prebuild` 첫 줄에 `env: load .env.local .env` |
| 서명 키 | `Build Credentials`가 자동 | 템플릿 기본값이 **debug 키**다. 그대로 구우면 Play가 거부 |
| `versionCode` | 원격 카운터가 자동 증가 | `app.json`의 **`1`** 을 쓴다. Play에 2가 있으면 거부 |
| `APP_VARIANT` | 프로필 `env`에 있다 | 셸에 직접 넣어야 한다. 안 넣으면 **운영 패키지**로 구워진다 |

첫 줄이 제일 위험하다. `.env.local`에 있던 값이 그대로 번들에 박힐 뻔했다:

| 변수 | 개발용 값 | 박혔다면 |
|---|---|---|
| `EXPO_PUBLIC_BACKUP_SERVER_URL` | `http://10.0.2.2:3200` | 백업·AI가 **에뮬레이터 localhost**를 본다 |
| `EXPO_PUBLIC_REVENUECAT_ANDROID_KEY` | 다른 키 | **검증하려던 결제가 엉뚱한 앱에 붙는다** |
| `EXPO_PUBLIC_DEVICE_CHECK=1` | | 매 실행마다 5MB 업로드 + DB 스왑 |

🔴 **`EXPO_PUBLIC_*`는 빌드 시점에 문자열로 박힌다.** 올리고 나서 못 고친다.

→ `npm run check:release-env`가 이걸 막는다(`scripts/check-release-env.mjs`).
**AAB를 굽기 전에 반드시 돌린다.**

#### ⚠ `__DEV__` 가드로 풀려다 말았다 — 그게 답이 아니다

`EXPO_PUBLIC_DEV_LOGIN`은 이미 `__DEV__ && …`로 접힌다(`features/support/dev-auth.ts`).
`EXPO_PUBLIC_DEVICE_CHECK`에 같은 걸 붙이자고 했다가 **되물렸다** — 그 점검의 목적이
*"릴리스 번들에서 암호 처리량을 재는 것"* 이라 `__DEV__`로 막으면 **기능이 죽는다**
(dev 번들은 Hermes lazy 컴파일이라 수치가 틀린다). `app/_layout.tsx`가 그 이유를 적어놨고,
그 주석을 안 읽고 *"얘만 가드가 없네"* 로 읽은 것이 잘못이었다.

그리고 토큰은 **fail-closed**다 — 배포된 서버가 `dev-emulator`를 401로 거절하므로
권한이 뚫리는 문제가 아니다. 그래서 **소스가 아니라 빌드 게이트**에서 막는다.

#### 절차

```bash
# ① 릴리스에 섞이면 안 되는 것부터 치운다
npm run check:release-env          # 통과할 때까지. mv .env.local .env.local.aside

# ② stg 패키지로 네이티브를 다시 만든다 (⚠ android/ 를 통째로 지우고 새로 만든다)
APP_VARIANT=stg npx expo prebuild --platform android --clean --no-install

# ③ 생성된 gradle 두 곳을 손본다 — CNG 산출물이라 ②를 돌릴 때마다 다시 해야 한다
#    · android/app/build.gradle   versionCode 1 → 다음 번호
#                                 release signingConfig: debug → upload
#    · android/gradle.properties  JOGAK_UPLOAD_* 4개 (C:/project/secrets/jogak-keystore.env 에서)

# ④ eas.json 의 stg 프로필 env 를 셸에 명시적으로 넣고 굽는다
ANDROID_HOME=~/AppData/Local/Android/Sdk APP_VARIANT=stg \
EXPO_PUBLIC_...=... ./gradlew bundleRelease --no-daemon
#    → android/app/build/outputs/bundle/release/app-release.aab

# ⑤ 🔴 서명 지문이 Play에 등록된 업로드 키와 같은지 **올리기 전에** 확인한다
keytool -printcert -jarfile app-release.aab | grep SHA1
#    기대값: 3A:47:42:B7:30:A4:33:00:4A:AA:FD:61:AE:A8:69:CE:87:47:CA:F2

# ⑥ 끝나면 되돌린다
mv .env.local.aside .env.local
```

#### 키스토어

| | |
|---|---|
| 파일 | `C:\project\secrets\jogak-upload.jks` (EAS `Build Credentials oEjjkh-V_V`에서 내려받음) |
| 비밀번호 | `C:\project\secrets\jogak-keystore.env` — 저장소 **밖**이다 |
| SHA1 | `3A:47:42:B7:30:A4:33:00:4A:AA:FD:61:AE:A8:69:CE:87:47:CA:F2` (2026-08-14 생성) |

🔴 **이것이 앱의 신원 그 자체다.** 잃으면 되돌리는 길이 키 교체뿐이고, Play App Signing
등록을 다시 해야 한다. EAS에도 사본이 있지만 **EAS 계정을 잃으면 같이 잃는다.**

⚠ `npx eas-cli credentials`는 다운로드할 때 **비밀번호를 터미널에 그대로 출력한다.**
자동화·에이전트에게 시키지 말고 사람이 직접 받는다 — 로그에 남으면 회수할 방법이 없다.

#### ⏭ 반복할 거라면

③은 `prebuild`마다 손으로 다시 해야 한다. 두세 번 넘게 할 것 같으면
**config plugin으로 박는 편이 낫다** — 지금은 한 번뿐이라 손으로 했다.


### 6.2 등록한 상품 (2026-08-12)

```
조각 Pro  (jogak_pro)                     세금: 디지털 앱 판매 · 규정 준수: 서비스
├─ jogak-pro-monthly   매월 · ₩3,900   유예 7일 · 활성 · 174개국
│  └─ freetrial-7d     7일 무료        활성
└─ jogak-pro-yearly    매년 · ₩29,000  유예 14일 · 활성 · 174개국
   └─ freetrial-7d     7일 무료        활성
```

- **혜택은 2개만 적었다** — `광고 없이 조용하게` · `암호화 클라우드 백업·복원`.
  AI 리포트는 미구현이라 넣지 않았다(§5 "팔지 않는 것을 혜택으로 적지 않는다").
- **체험 자격은 `구독한 적 없음`(앱 전체)** 으로 잡았다. Play 기본값은 `이 구독을 이용한 적 없음`인데,
  나중에 상품을 늘려도 체험을 두 번 받지 못하게 하는 쪽이 낫다. 상품이 하나인 지금은 둘이 같다.
- 🔴 **대한민국 세금이 `VAT 없음`으로 표시된다.** 콘솔이 직접 확인해준 셈이다 —
  국내 개발자에게는 Google이 VAT를 대납하지 않는다(§5). **부가세는 우리가 낸다.**
- ⚠ **상품 ID는 재사용 불가다.** 지우고 같은 이름으로 다시 만들 수 없다.

### 🔴 6.3 stg도 운영과 **똑같이** 결제까지 검증한다 (2026-08-14 사용자 결정)

~~stg는 구독 화면까지만, 결제는 운영에서~~ → **stg를 운영과 동일한 환경으로 만든다.**
`pro`가 없으면 백업·AI 리포트·광고 제거가 **전부 막혀** 리허설이 성립하지 않기 때문이다
(엔타이틀먼트의 진실은 우리 서버이고, 서버는 RC 웹훅으로만 `pro`를 얻는다 — §4).

#### 검증은 **실제 결제**로 한다 — 라이선스 테스터를 쓰지 않는다

라이선스 테스터 결제는 RC가 `SANDBOX`로 보내고, 그걸 권한으로 인정하려면
`RC_SANDBOX_GRANT=all`이 필요하다. 그런데 그 플래그는 **앱별이 아니라 전역이다**:

```
common_server/lib/revenuecat.ts:72
sandboxGrantEnabled = () => (process.env.RC_SANDBOX_GRANT ?? '') === 'all'
```

→ **stg를 위해 켜면 운영에도 켜진다.** 바로 아래 체크리스트가 *"켜둔 채 출시하면 테스트
결제가 실권한이 된다"* 고 경고하는 그 스위치다. common_server는 수정 금지라 앱별로 못 가른다.

**그래서 실제 결제로 간다**(₩3,900, Play에서 환불 가능). 어차피 출시 전에 한 번은 전 구간
(RC 웹훅 → common_server → `pro` → 백업/AI)을 실제로 봐야 한다. 이 선택의 소득은 비용이
아니라 **`RC_SANDBOX_GRANT`를 한 번도 켜지 않는 것** — 사람의 기억에 의존하던 사고를
구조적으로 없앤다.

#### common_server 앱 코드는 **공유한다**(`jogak`)

`APP_CODE`가 `lib/common-server/client.ts:22`에 상수로 박혀 있다. stg도 그대로 쓴다.

| | |
|---|---|
| ✅ | 공지·문의가 운영과 같은 데이터 — **리허설의 목적에 맞다** |
| ✅ | 코드 변경 0. 분리하면 `APP_CODE`를 env로 빼고 common_server에 앱을 새로 등록해야 한다 |
| ⚠ | stg 테스트 결제가 **운영 엔타이틀먼트 행으로 들어간다.** 내부 테스터가 본인 하나라 실질은 *"내 계정이 pro가 된다"* 뿐 |

#### 필요한 것

| | 비고 |
|---|---|
| Play `조각 stg`에 구독 상품 등록 | **상품 ID는 앱마다 별개**라 `jogak_pro`를 그대로 재사용할 수 있다 |
| RevenueCat에 **stg 앱 추가**(`com.son0925.jogak.stg`) + 상품 import + `pro` attach + 오퍼링 | RC 앱이 Play 패키지에 매핑되므로 운영 앱을 그대로 못 쓴다 |
| RC 공개 키를 `eas.json` **stg 프로파일**에 넣기 | 지금은 **비워 뒀다** — 운영 키를 두면 패키지 불일치로 상품이 안 잡히고, 화면은 `subscribe.unavailable`로 떨어진다(설계된 상태) |
| Play 서비스 계정 키 | **계정 단위라 한 벌로 두 앱을 커버한다** ✅ |

### ⚠ 출시 체크리스트에 넣을 것 (§7.2 함정 3개)

- [ ] 웹훅 이력에 `anonymous-app-user-id`가 없는가 → `logIn()`을 안 불렀다는 뜻
- [ ] `entitlement_ids`가 빈 배열이 아닌가 → RC에서 **상품 attach를 빠뜨렸다**
- [ ] ~~`RC_SANDBOX_GRANT`를 껐는가~~ → 🔴 **애초에 켜지 않는다**(§6.3). 켤 이유가 없어졌다

---

## 7. 구현 현황

| 항목 | 상태 |
|---|---|
| `react-native-purchases` 설치 | ✅ 10.7.0 — ⚠ **번들 +1.21MB**(3.67 → 4.88MB). 단일 진입점 패키지라 서브패스로 줄일 수 없다 |
| SDK 연결 · `logIn` 게이트 | ✅ `features/subscription/api/purchases.ts` |
| 구독 화면 | ✅ `app/subscribe.tsx` — 로그인 게이트 2겹 |
| `restorePurchases` | ✅ |
| 엔타이틀먼트 조회·캐시 | ✅ `features/entitlement/store.ts` |
| 광고 제거 연결 | ✅ `adsEnabled()` |
| RevenueCat 프로젝트 | ✅ **조각** (`006d92c9`) · Lifestyle · React Native |
| 엔타이틀먼트 | ✅ **`pro`** — 프리셋("조각 Pro")을 쓰지 않고 직접 입력했다. 코드가 이 문자열을 그대로 본다 |
| 오퍼링 | ✅ Monthly · Yearly. **Lifetime은 뺐다**(단일 구독 2종) |
| Play 앱 설정 | ✅ `com.son0925.jogak` · 서비스 계정 **Valid credentials** |
| SDK 공개 키 | ✅ `goog_…` → `eas.json`(internal·production) + 개발 `.env.local` |
| **Play 상품 등록** | ✅ **완료(2026-08-12)** — `jogak_pro` + 요금제 2개 + 체험 2개, 전부 활성(§6.2) |
| AAB 내부 테스트 업로드 | ✅ v7(versionCode 7) — `eas submit`, 권한은 켰다 끄는 방식(§6.1) |
| RC 상품 import·attach | ❌ **남은 마지막 배선.** Play 상품은 준비됐다 |
| §13⑥ 전환 동의 플로우 | ✅ 체험이 붙은 상품에만. 체크박스 없이는 결제 버튼이 죽어 있다 |
| 체험 기간 계산(순수) | ✅ `features/subscription/trial.ts` · `npm run check:subscription` 8개 — **법적 고지의 근거라 Node에서 검사한다** |
| 탈퇴 다이얼로그 — 구독 경고 + Play 링크 | ✅ 15개 언어 |
| 로그인 후 자동 `restorePurchases` | ✅ `pro=false`일 때 **1회**. 계정이 바뀐 사람이 돈만 내는 것을 막는다 |
| AI 혜택 표기 | ✅ **표시 해제**(2026-08-19) — 배포된 라우트로 생성이 도는 것을 확인하고 뗐다(§5). ⚠ Play 콘솔 상품 설명은 아직 2개짜리 |
