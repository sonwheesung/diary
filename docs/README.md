# 조각 — 문서 색인

> 이 파일이 **문서 색인이자 구현 현황 정본**이다. 새 `*_SYSTEM.md`를 추가하면 반드시 아래 목록과
> 구현 현황표에 함께 등록한다([`DOC_DISCIPLINE.md`](./DOC_DISCIPLINE.md) 부록 체크리스트).
> 설계 원칙·기둥·MVP 범위·결정 로그는 루트 [`CLAUDE.md`](../CLAUDE.md).

---

## 1. 문서 목록

| 문서 | 범위 | 상태 |
|---|---|---|
| [`../CLAUDE.md`](../CLAUDE.md) | 설계 정본 — 기둥·MVP 범위·로그인/광고 정책·스택·결정 로그 | ✅ |
| [`ARCHITECTURE.md`](./ARCHITECTURE.md) | 서버 경계(common_server vs 조각 서버)·신원 흐름·연동 계약 | ✅ |
| [`DOC_DISCIPLINE.md`](./DOC_DISCIPLINE.md) | 문서 작업 규율 | ✅ |
| [`DIARY_SYSTEM.md`](./DIARY_SYSTEM.md) | 조각 도메인 — 하루 조각 수·날짜·감정·연속 작성일·검색·삭제 규칙 | ✅ |
| [`DATABASE.md`](./DATABASE.md) | 로컬(expo-sqlite) 스키마·마이그레이션 규약·백업 대비 | ✅ |
| [`SUPPORT_SYSTEM.md`](./SUPPORT_SYSTEM.md) | 공지·문의 — common_server 연동·SDK 복사 규약·읽음 관리 | ✅ |
| [`I18N_SYSTEM.md`](./I18N_SYSTEM.md) | 다국어 — 문자열 규약·날짜 현지화·언어 추가 절차 (정책은 CLAUDE.md §9.1) | ✅ |
| [`AUTH_SYSTEM.md`](./AUTH_SYSTEM.md) | 신원과 연령 — 구글 로그인·subject 토큰·**연령 게이트**(지역별 16/14/13) | ✅ 2026-08-27 신규 |
| `LOCK_SYSTEM.md` | 앱 잠금 — PIN·패턴(3×3)·생체·자동잠금·실패 처리 (정책은 CLAUDE.md §7.1) | ❌ 미작성 |
| [`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) | 구독 — 단일 상품·로그인 게이트·RevenueCat 연결·전자상거래법 (정책은 CLAUDE.md §7.2) | ✅ |
| [`BACKUP_SYSTEM.md`](./BACKUP_SYSTEM.md) | 백업/복원 — 키 유도·봉투·매니페스트·서버 계약·전체 교체 (정책은 CLAUDE.md §5.1) | ✅ |
| [`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) | AI 리포트 — 처리 경로·위기 정책·모델/원가·캡·기간/언어·서버 계약 (정책은 CLAUDE.md §5.1) | ✅ |
| [`ADMIN_SYSTEM.md`](./ADMIN_SYSTEM.md) | 운영 콘솔 — 배구 승계 범위·인가(fail-closed)·**개인 비특정 규칙**·읽기 전용 | ✅ |
| [`NOTIFICATION_SYSTEM.md`](./NOTIFICATION_SYSTEM.md) | 기록 리마인더 — 로컬 알림·7일 롤링 창·권한·예약 언어 고착 | ✅ |
| [`PLAY_DATA_SAFETY.md`](./PLAY_DATA_SAFETY.md) | Play 데이터 보안 선언 — 일시처리·E2EE·공유 제외 원문 · 아웃바운드 전수표 · 확정 선언표(diff) · 삭제 경로 · **콘솔 클릭 체크리스트** | ✅ 2026-08-24 재검증 |
| [`OPEN_SOURCE_NOTICE.md`](./OPEN_SOURCE_NOTICE.md) | 오픈소스 고지 — 폰트 OFL·npm 36개·생성기·드리프트 가드 | ✅ 2026-08-31 신규 |
| `UI_GUIDE.md` | 컬러·타이포·여백·공통 컴포넌트 사용법 | ❌ 미작성 |
| `CHANGELOG.md` | 릴리스 변경 이력 | ❌ 미작성(첫 빌드 시점부터) |
| `PROJECT_STRUCTURE.md` | 폴더 구조 상세 | ⏸ 보류 — 현재는 CLAUDE.md §8이 정본 |

---

## 2. 구현 현황

### 앱

| 영역 | 상태 | 비고 |
|---|---|---|
| Expo 부트(SDK 54 · expo-router · TS strict) | ✅ | dev 서버 기동 + 실기기 접속 확인(2026-08-07) |
| 테마(팔레트 · spacing · radius · typography) | ✅ | 라이트 + 다크. `theme/palettes.ts`에 팔레트를 더하면 스킨이 된다 |
| 다크모드 | ✅ | 설정 → 화면 → 시스템·라이트·다크. 선택은 `app_settings`에 저장 |
| 다국어(i18n) | ✅ | **15개 언어**. 기기 언어 기본, 폴백은 영어. 설정 → 언어 시트에서 변경. `npm run check:i18n` · `npm run check:subscription`으로 키 검사. ⚠ 원어민 검수 전 |
| Pretendard 폰트 | ✅ | 정적 OTF 3종. 로드 완료까지 스플래시 유지, 실패해도 앱은 진행 |
| expo-sqlite 스키마·마이그레이션 | ✅ | `db/` — user_version 기반. 실기기 스모크 테스트 통과 |
| 일기 CRUD·검색·streak·캘린더 집계 | ✅ | `features/diary/api/` |
| 하단 탭 네비게이션(홈·캘린더·⊕·**리포트**·설정) | ✅ | ⊕는 탭이 아니라 작성 화면을 띄우는 동작. **검색은 2026-08-12에 탭에서 강등**(AI_REPORT_SYSTEM §11.1) |
| 화면 틀 `components/Screen` | ✅ | 세이프에어리어·키보드 여백 단일 처리. CLAUDE.md §10 |
| 공통 컴포넌트 Button · Card · TextField · MonthGrid · DatePickerSheet · **BottomSheet · ImageViewer** | ✅ | 2026-08-13 정정 — BottomSheet는 있다(언어·리포트 언어 시트가 쓴다). Modal · Header · Avatar · Loading은 **필요한 적이 없어 안 만들었다** — 미착수가 아니라 불필요 |
| Splash · 아이콘 | ✅ | `scripts/make-assets.py`로 코드 생성. 라이트/다크 스플래시 분리. 적응형 아이콘 안전영역 확인 |
| Home | ✅ | 오늘 날짜·인사·연속 기록·조각 쓰기·최근 조각(썸네일) |
| Write / Edit | ✅ | 한 화면이 둘을 겸한다(`/write?id=`). 커서 위치 사진 삽입·태그·감정·날짜 변경. 저장 후 광고는 AdMob 대기 |
| **본문 텍스트 서식** | ✅ | 2026-08-24 — 문단마다 **정렬·크기(H1~H4/본문)·굵기·글자색**. 🔴 **색은 hex가 아니라 코드**(`rose`)로 저장하고 `theme/palettes.ts`의 `ink`가 테마별 실제 값을 준다 — hex로 저장하면 라이트에서 고른 색이 **다크에서 안 읽히고** 스킨을 갈아끼워도 옛 조각만 옛 색으로 남는다. 🚫 **글꼴 종류는 뺐다**(한글 폰트 굵기당 ~1.5MB · 나머지 14개 언어에서 조용히 폴백). `npm run check:diary-format` 42개. **에뮬레이터 실동작 확인**(2026-08-24, ⏭ 실기기는 v10부터 — dev 빌드가 스토어 서명과 충돌한다): 가운데 문단만 분할·저장 후 재열람·다크모드 색 반전·베트남어/독일어 시트. 🔴 그 과정에서 **서식보다 오래된 커서 버그**를 잡았다 — 프로그램이 블록을 쪼갤 때 RN이 쏘는 `onSelectionChange`가 커서를 앞 블록으로 끌어가 **사진을 연달아 넣으면 두 번째가 엉뚱한 자리**에 들어갔다([`DIARY_SYSTEM.md`](./DIARY_SYSTEM.md) §1.1) |
| 🔴 **빈 문단이 지워지지 않던 것** | ✅ | 2026-08-27 실기기 신고 — 목록을 넣었다 지우면 **빈 텍스트 블록이 남고 지울 손동작이 없었다**(빈 칸에서는 `onChangeText`가 안 불리고, 삭제 버튼은 사진에만 있고, 정리는 저장할 때만 돈다). 데이터는 멀쩡했고 **편집 중 화면만 저장 결과와 갈렸다.** `removeBlockAt()`이 걷어낸 자리에서 앞뒤 문단을 다시 붙인다 — 사진 삭제도 같은 경로. 🔴 **순수 계층 21개가 전부 통과한 채로** 일어났다(저장 결과는 처음부터 옳았다) — 2026-08-24 커서 버그와 같은 종류 **에뮬레이터 실동작 확인**(2026-08-27): 목록 삽입→삭제를 3회 반복해도 빈 블록이 **안 쌓인다**. 🔴 그 김에 **안드로이드 `onKeyPress`가 Backspace를 안 준다**는 것도 실측했다 — 백스페이스 병합은 붙였다가 **되돌렸다**(동작 안 하는 코드를 남기지 않는다) ([`DIARY_SYSTEM.md`](./DIARY_SYSTEM.md) §1.1) |
| **솔기 × — 남은 절반** | ✅ | 2026-08-31 — 위 줄이 못 닫은 경로. 서식으로 문단을 쪼갠 뒤 그 문단의 **글자를 다 지우면** 빈 문단이 남고, 그건 목록·사진과 달리 걷어낼 블록이 없어 `removeBlockAt`이 안 불린다. 🔴 보류했던 이유(*"빈 문단마다 버튼이 뜨는 그림"* 이 기둥 2와 충돌)는 **전제가 틀렸다** — 빈 텍스트 블록은 뜻이 둘이고(① 두 문단 사이의 **솔기** ② 사진·목록 뒤 **이어 쓸 자리**) ①에만 ×를 띄우면 보통 **0개**다. 가르는 것은 **이웃**(`isRemovableSeam`, 순수 계층). 🚫 *"비면 자동 병합"* 은 여전히 안 한다 — H1을 걸고 글자만 지운 사람의 **서식 의도를 지운다**. ⚠ 포커스로 가르지 않은 이유는 `onBlur`가 먼저 돌아 **버튼이 사라져 터치가 땅에 떨어지기** 때문이다(§1.1이 이미 타이밍으로 데인 자리). `check:diary-format` 28→**38개** ([`DIARY_SYSTEM.md`](./DIARY_SYSTEM.md) §1.1) |
| 🔴 **목록·사진 삽입의 포커스** | ✅ | 2026-09-04 **실기기 신고**(Galaxy S24 · vc18) — 목록이 **이미 있는** 조각에 목록을 하나 더 끼우면 글이 **앞 문단 한가운데**에 들어갔다. `key={`list-<인덱스>`}`가 배열 인덱스라 새 목록이 기존 목록과 같은 key를 받고, React가 인스턴스를 재사용해 포커스를 옮기는 유일한 장치인 `autoFocus`(마운트 때만)가 **안 돈다**. 🔴 `insertList`가 옮긴 것은 `caretRef` 하나뿐이라 *다음 사진이 들어갈 자리*는 맞고 **키보드가 치는 곳만 틀렸다** — 그래서 재현이 어려웠다. ⚠ **사진도 같은 병이었다**(사진 뒤에 이어 쓰면 사진 **위** 문단에 들어간다) — 신고는 목록으로 왔지만 형제를 함께 고쳤다. → 커서·잠금·포커스를 `moveCaret` 한 함수로 묶고, `focusRequest`에 목록 **항목**(`item`)을 더했다. 🚫 key를 고치지 않은 이유는 블록에 안정 id가 없어 **저장 형식이 바뀌기** 때문이다. `check:diary-format` 38→**42개**(소스 검사 4개 신설 · 3가지를 일부러 망가뜨려 확인 — 그중 하나가 처음엔 **안 빨개져서** 개수를 세도록 고쳤다) ([`DIARY_SYSTEM.md`](./DIARY_SYSTEM.md) §1.1) |
| **리포트 탭이 고른 종류를 안 따라갔다** | ✅ | 2026-09-04 실기기 — 비구독자 화면(`LockedPreview`)이 `'weekly'`를 박아둬 **연간 탭에서 *"매주 돌아보는 시간 · 8월 24일–30일"*** 이 떴다. 탭은 눌리는데 내용이 안 따라오면 고장이 아니라 **거짓말**이다. ⚠ 같은 규약이 **바로 아래 구독자 빈 화면에 이미 적혀 있었다**(`report.empty<종류>` — *"월간 탭에서 '한 주가 지나면…'을 보여주면 틀리게 알려주는 것"*) — **새 규칙이 아니라 무료 사용자에게만 안 지킨 것**이다. → `lockedTitle`을 종류별 3개로 쪼개고 본문은 `empty<종류>`를 그대로 쓴다. 🚫 **예시 카드는 주간에만** 둔다 — 문장이 요일을 말해서(*"월요일에는…"*) 연간 아래 놓으면 같은 거짓말이 된다. 보여줄 수 없는 것은 **안 보여주는 쪽**이 정직하고 번역도 안 는다 |
| **오늘 조각을 지우면 오늘 리마인더가 안 돌아왔다** | ✅ | 2026-09-04 실기기(`dumpsys alarm` 실측) — 저장할 때는 그날 예약을 지우는데 (`DiaryEditor`) **지울 때는 아무도 안 되돌렸다.** 다음에 앱을 열 때까지 그대로인데, **알림을 기다리는 사람은 앱을 안 연다**(그게 리마인더의 전제다). → 삭제 성공 뒤 `syncReminders()`. 방향만 반대일 뿐 저장과 같은 규약이다 — **조건이 확정되는 자리에서 다시 맞춘다** ([`NOTIFICATION_SYSTEM.md`](./NOTIFICATION_SYSTEM.md) §1) |
| **오픈소스 고지** | ✅ | 2026-08-31 — 설정 → 정보 → **오픈소스 라이선스**(`app/licenses.tsx`). 🔴 **앱 어디에도 고지가 한 줄도 없었다** — Pretendard OTF 3종(4.6MB)과 런타임 패키지 **38개**를 번들에 싣는데 OFL §2·MIT가 둘 다 *"저작권 고지와 라이선스를 동봉하라"* 를 조건으로 단다. 공용 [`GAME_ASSET_SOURCING.md`](file:///C:/project/common/GAME_ASSET_SOURCING.md) §3.1이 배구명가에서 같은 결함을 닫으며 *"다른 프로젝트도 같은 검사를 돌릴 것"* 이라 적었는데 **조각은 안 돌렸다**. 🔴 그리고 **베꼈으면 틀렸다** — 배구명가는 `Pretendard JP`(저작권자 4인)이고 조각은 plain Pretendard라 **1인**이다(OTF `name` 테이블 + 저장소 LICENSE `v1.3.9` 둘 다 실측). 패키지 목록은 **생성**한다(`licenses:build`) — 손으로 적으면 의존성 하나 늘린 날 조용히 거짓이 된다. `npm run check:licenses` **41개**(패키지 38), **4가지를 일부러 망가뜨려 전부 잡히는 것까지** 확인. 🟢 **그 가드가 실제로 값을 했다**(2026-09-09): R8 을 켜며 `expo-build-properties` 를 넣자 *"고지 목록에 없다"* 로 즉시 빨개졌다 — 고지의 유일한 실패 방식인 **조용히 낡는 것**을 그 자리에서 막았다 ([`OPEN_SOURCE_NOTICE.md`](./OPEN_SOURCE_NOTICE.md)) |
| Detail | ✅ | `app/diary/[id].tsx` — 조회·수정·삭제. 돌아올 때마다 다시 읽는다 |
| Calendar | ✅ | 월 격자(쓴 날 점 표시)·날짜 선택·그날 조각 카드·빈 날엔 그 날짜로 쓰기 |
| Search | ✅ | `app/search.tsx` — **탭이 아니다.** 홈·모든 조각의 돋보기가 입구. 250ms 디바운스, 빈 검색어면 자주 쓴 태그 |
| 모든 조각 목록(홈의 더보기) | ✅ | `app/diaries.tsx` — 달 머리글 + 20개씩 '더 보기' |
| Settings(다크모드·잠금·언어·알림 UI) | ✅ | 2026-08-11 정정 — ❌로 남아 있었다 |
| 앱 잠금(PIN·패턴 3×3·생체) | ✅ | `features/lock/` · 힌트 되찾기 · 실패 backoff · 복귀 지연 · 앱 스위처 가림(Android). iOS 가림만 남음 |
| 공지사항 | ✅ | `app/notice.tsx` — bootstrap 1회 조회. **서버에 `jogak` 등록 대기** |
| 문의하기 | ✅ | 화면·서버 전송·로그인·탈퇴까지. 로그인 게이트 화면에 처리방침 고지 추가(2026-08-11) |
| **문의 내역·답변** | ✅ | 2026-08-13 — `app/inquiries.tsx`. 서버·SDK는 있었는데 **앱이 부르지 않아** 답변을 볼 길이 없었다. 답변 배지는 설정 행에 점 하나(푸시가 없어 이게 통지의 전부다). [`SUPPORT_SYSTEM.md`](./SUPPORT_SYSTEM.md) §5.5 |
| 구글 로그인(선택적) | ✅ | 2026-08-11 정정 — Phase 7이 끝났고 `bootstrap?app=jogak`도 200이다 |
| **연령 게이트** | ✅ | 2026-08-27 — 처리방침 §2 가 *"만 14세 미만 아동은 로그인 기능을 이용할 수 없습니다"* 를 **현재형으로** 15개 언어·공개 웹에 게시했는데 **코드가 0줄**이었다(고지가 코드를 앞질렀다, CLAUDE.md §12 2026-08-24 와 같은 종류). ~~부팅이 아니라 `signIn()` 안에 세운다~~ → 🔴 **부팅으로 옼겼다**(2026-09-01). 비로그인 DAU 를 잡기로 하면서 부팅에 기기 UUID 를 발급하게 돼 **경계 자체가 부팅으로 이동했다**. 🔴 단 **벽이 아니다** — 미달 판정이 하는 일은 `registerDevice()` 를 안 부르고 `signIn()` 을 막는 둘뿐이고 **일기는 그대로 쓴다**. 미달은 365일 유예(닫음은 저장하지 않는다). 지역별 **16/14/13 · 판정 불가 16** · 보수 판정식 · **생년 미저장** · 서버 전송 0 ([`AUTH_SYSTEM.md`](./AUTH_SYSTEM.md) §1). `check:age-gate` 148개(4,750조합 전수 · 회귀 3종 주입 확인). **에뮬레이터 실동작 6개 통과**(`AUTH_SYSTEM.md` §1.9) — 미달 시 미저장 · 같은 세션 재입력 차단 · 저장값에 **생년 없음**(기기 DB 직접 조회) · 두 번째 호출처에서 재확인 없음 |
| 광고(AdMob) | ✅ | 전면=저장 완료 후 하루 1회 · 배너=탭 화면 상시. 개발은 테스트 단위(`EXPO_PUBLIC_ADS_REAL=1`일 때만 실제 단위) |
| **광고 제거(구독자)** | ✅ | `features/entitlement/store.ts` — 캐시 먼저·서버 나중. 조회 실패에 캐시를 지우지 않는다 |
| 🔴 **유예 배너 — 근거가 지워지고 있었다** | ✅ | 2026-09-04 결제 전수 검수. 구독이 끝나면 `revoke`가 `pro_until`을 비우는데 **파기 예정일의 유일한 근거가 그 값**이라, 배너가 콜드 스타트 직후 몇 백 ms만 보이고 **영영 사라졌다.** §7.2가 *"반드시 알린다"* 를 내려놓고 대신 약속한 *"상시 표시"* 가 코드에 없던 것이고, **환불이 만료보다 아프다**(더 갑작스럽고 CS 직행). → `ServerAnswer.none`이 `expiredAt`을 싣고 **별도 칸 `pro_expired_at`** 에 적는다. `check:subscription` 31 → **36개** |
| **침묵 실패 셋** | ✅ | 2026-09-04 — 🔴 검색 실패가 *"찾는 조각이 없어요"* 로 보였다(사용자는 **일기가 사라졌다고** 읽는다) · 🔴 사진을 하나도 못 받아도 *"복원 완료"* 만 떴다(⚠ 재시도 경로가 실제로 없어 **자동 재시도를 약속하지 않았다**) · 🟡 리마인더 시각 저장 실패가 안 되돌아왔다(형제 `chooseReportLanguage`는 되돌리고 있었다). `search.failed`·`backup.restorePhotosFailed` **15개 언어** |
| **백업/복원 — 앱 쪽** | ✅ | 암호 계층·매니페스트·클라이언트·화면. [`BACKUP_SYSTEM.md`](./BACKUP_SYSTEM.md) |
| 🔴 **켜기 버튼이 백업을 켜지 않았다** | ✅ | **2026-09-08 실기기 발견 · 같은 날 고침.** `app/backup.tsx` 의 `enable()` 이 비밀만 만들고 **`enableBackup()` 을 안 불러** `backup_enabled` 가 영영 0 이었다 — 켜기를 눌러도 화면이 *"아직 백업을 켜지 않았어요"* 로 돌아오고 `지금 백업` 버튼에 **도달할 수 없다.** `backup_enabled = 1` 을 쓰는 곳이 `restore.ts`(복원)와 `device-check.ts`(개발 점검)뿐이었으므로 **백업이 이미 있는 사람만** 켤 수 있는 순환이었다. 🔴 **vc19(0.2.6) 로 143개국에 나간 상태** — 유료 혜택 셋 중 하나가 처음부터 닿지 않았다. 순수 계층 46개·서버 e2e 32개·에뮬레이터 측정이 **전부 이 한 줄을 비껴간다**(경로 미주행) → 소스를 읽는 **배선 가드 7개** 신설([`BACKUP_SYSTEM.md`](./BACKUP_SYSTEM.md) §4.5) |
| **백업/복원 — 기기 검증** | ✅ | 2026-08-11 에뮬레이터 15/18. 5MB PUT 바이트 일치 · `backupDatabaseAsync` 9ms. ⏭ 남은 3은 **암호 처리량**이고 "순수 JS 유지"로 결론냈다 — 실기기(ARM) 재측정만 남음([`BACKUP_SYSTEM.md`](./BACKUP_SYSTEM.md) §8) |
| 사진 백업(2차) | ✅ | 이미지 하나 = blob 하나. 증분(`plan`) · 복원 후 못 받은 사진만 `'missing'` |
| 월 구독(RevenueCat) | ✅ | ~~Phase 9 대기~~ → ~~RC 상품 import·attach 남음~~ → **배선 완료**(2026-08-17). 운영·stg 두 앱 · Products → `pro` attach → 오퍼링 연결. 🔴 그 과정에서 **운영 앱도 Test Store 상품만 물고 있던 것**을 발견해 함께 고쳤다 — 결제해도 `pro`가 안 붙는 상태였다. 남은 것은 **실결제 확인**뿐([`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §6.1.3) |
| **Play 서비스 계정 권한** | ✅ | 새 계정(`sonwheesung925`)에 서비스 계정 초대 + 앱/계정 권한. `npm run check:play-access`가 진단한다. ⚠ **앱 권한과 계정 권한이 여는 것이 다르다** — 카탈로그는 앱 권한, **구매 검증은 계정 권한** |
| 백업/복원 — 서버 | ✅ | ~~조각 서버 대기~~ → `jogak-stg`(서울) + Vercel(`icn1`) 배포. 위 두 줄과 합쳐 읽는다 |
| **AI 리포트 — 앱 쪽** | ✅ | 리포트 탭·상세·홈 카드·설정(리포트 언어)·DB v5·백업 포함. [`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §11 |
| **AI 리포트 — 기간 선택(백필)** | ✅ | 2026-08-18 — 지평 **작년 1월 1일**까지 지난 기간을 고른다. 없을 땐 결제 직후 사용자가 첫 주들을 영영 잃었고, 1년 회고가 반쪽으로 굳었다([`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §6.4) |
| **AI 리포트 — 삭제는 묘비** | ✅ | 2026-08-18 — DB v6 · `MANIFEST_FORMAT` 3. 하드 삭제가 **로컬과 서버 캡(`uq_ai_usage_period`)을 갈라놔** 지운 기간이 되살아난 것처럼 보이고 서버가 막았다([`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §11.9). ⏭ 재설치·기기 2대는 서버 경로가 있어야 고쳐진다 |
| 🔴 **AI 리포트 — 지표 넷** | ✅ | 2026-08-25 — `stress·happiness·exercise·growth`(0~100 + 근거) + 그 밖의 주제 6종(날 수만). "
"**A/B/C를 실제로 호출해 보고** C(합친 것)를 골랐다 — 그냥 포개면 `운동 25점`과 `운동 1일`이 따로 떠서 *"뭐가 맞지"* 가 된다. "
"🔴 `stress`·`happiness`는 **날로 셀 수 없어 `days = null`**(모델이 넣어 와도 벤더 경계에서 버린다). "
"프롬프트 **v12** — *"평가하지 않습니다"* 를 **글에 한정**하고 병명 금지는 남겼다(`check:ai`가 문자열로 확인). "
"DB v7 · `MANIFEST_FORMAT` 3→4 · **NULL이 정상값**(v8 이전 리포트는 캡이 평생 1번이라 영원히 안 생긴다 → 블록을 안 그린다) "
"🔴 **상위 지표는 하위에서 합산한다**(점수는 평균·날 수는 합·`from`을 함께 보여준다) — 모델에게 물으면 근거가 없어 실제로 어긋났다 ([`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §8.4·§8.4.1) |
| **AI 리포트 — 그 기간의 모양** | ✅ | 2026-08-25 — 리포트 상세에 **요일 격자**(주간) · **주/달별 막대**(월간·연간) · 감정 범례. 🔴 **모델을 안 부르고 저장도 안 한다** — 화면이 그릴 때 로컬에서 세므로 **옛 리포트에도 붙는다**(모델이 만드는 지표는 캡이 평생 1번이라 소급 불가). 차트 라이브러리 0. 에뮬레이터에서 둘을 고쳤다: 막대를 감정색으로 칠했더니 `angry` 6일인 주가 **빨간 벽**이 됐고(→ 단색), 월간 축 `2026년 32주`가 다섯 칸에 안 들어갔다(→ 시작일 숫자) **지난 기간 비교**도 붙었다 — 주간은 같은 요일끼리 짝 막대, 나머지는 총계 한 줄. 🔴 **리포트가 아니라 조각만 있으면** 선다 ([`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §8.3·§8.3.1) |
| **개발용 일기 시드** | ✅ | 2026-08-25 — 설정 › DEV. 조각 436개 + 월간·연간 리포트 20개를 한 번에. ~~백업 복원으로 넣는다~~ 🚫 복원은 **전체 교체**라 기존 일기를 지우고 SecureStore 비밀까지 덮어쓴다. **이미 내 조각이 있는 날은 건너뛴다**(에뮬레이터 실측: 84일 건너뜀) ([`DIARY_SYSTEM.md`](./DIARY_SYSTEM.md) §8.3) |
| **AI 리포트 — 하위 완비 확인** | ✅ | 2026-08-18 — 안 끝난 하위는 차단, 안 만든 하위는 확인. `canCreate`가 하위 **0개인지만** 봐서 2개짜리 월간이 영구히 굳던 버그(§6.5) |
| 🔴 **AI 리포트 — 서버 배포** | ✅ | **2026-08-18에야 실제로 배포됐다.** 그전까지 `POST /api/v1/ai/report`가 배포본에서 **404** — `@shared/*`가 `../features/*`를 가리켰는데 Vercel CLI는 `server/`만 올린다. 순수 계층을 `server/shared/`로 생성 복사(`npm run sync:shared` · `check:shared`)해서 풀었다. ⚠ 그전의 "성공" 기록은 전부 **localhost** 기준이다 |
| **AI 리포트 — 서버** | ⏸ | 라우트·벤더 경계·캡·`ai_usage` 구현 완료(`e2e:ai` 15개). ✅ **모델 실호출 확인**(2026-08-13, `AI_EFFORT=medium` 확정 · 원가가 계획의 1/4). ~~🔴 그런데 `/api/v1/ai/report`를 통과한 성공 경로는 0회~~ → **3회 있다**(2026-08-25 stg DB 실측): 로컬 2회(8/14 `prompt_ver=1` · 8/21 `v4`) + **배포본 1회**(8/19 · `real` subject · W33 · ko · src=4 · 1355/277). ~~🔴 **셋 다 지금 프롬프트가 아니다**~~ ~~그리고 `kind`가 전부 `weekly`다 — 월간·연간은 한 번도 실행된 적이 없다~~ → **해소**(2026-08-25): `verify:hierarchy`가 주간 4 → 월간 → 연간을 **실호출**했다. 현재 `PROMPT_VERSION = 8`이고 그 6건이 v7·v8로 만들어졌다([`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §8.4·§12) |
| 🔴 **AI 원가가 재생성분을 잃고 있었다** | ✅ | 2026-09-07 — 공통서버가 자기 콘솔에서 찾은 결함(*목록은 거르는데 KPI는 안 거른다*)의 **형제를 훑다 나왔다**. `ai_reports`는 리비전을 **쌓는데** `ai_usage`는 같은 행을 **덮어쓴다** — 그 표에서 원가를 세니 **호출 2회가 1회로, 토큰은 두 번째 것만** 남고 `day`까지 갱신돼 **지난 30일 그래프가 조용히 바뀌었다.** 🔴 하필 재생성은 *"리포트가 별로예요"* 문의에 답하며 여는 것이라 **가장 비싼 호출에 몰린다**, 그리고 그 숫자를 보고 판단하기로 한 것이 `CLAUDE.md` §7.2의 월간·연간 확대 게이트다. → **`ai_calls` append-only 원장**을 따로 뒀다 — `ai_usage`(캡의 진실 · 기간당 1행)는 **한 줄도 안 바꿨다.** 🔴 근본 원인은 버그가 아니라 **단위가 둘인데 테이블이 하나**인 것이다. `check:admin` 30→**34** · `verify:regenerate` 11→**15**. ✅ **운영 배포까지 완료**(2026-09-07 12:07 · 사용자 승인) — DB는 순수 추가(DROP·ALTER 0), `uq_ai_usage_period` 그대로, 서버는 `vercel --prod`(Redeploy 아님)(**다섯을 일부러 망가뜨려 전부 확인**). 🟢 운영 재생성 **0건일 때** 잡아서 잃은 것이 없다 — 첫 재생성 뒤에는 되살릴 방법이 없었다([`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §6.6.1) |
| 🔴 **AI 리포트 — 재생성(리비전)** | ✅ | 2026-09-04 — 캡이 **평생 1회**라 *"리포트가 별로예요"* 문의에 할 수 있는 말이 **"죄송합니다" 뿐이었다**(프롬프트를 고쳐도 그 사람의 그 기간은 영원히 옛 판으로 굳는다). → 운영자가 콘솔에서 **한 번 열어주고** 답변한다. 🔴 **`ai_usage.regenerate` 불리언 하나**로 풀었다 — 행을 더 안 넣고 **기존 행을 갱신**하므로 `uq_ai_usage_period`를 **안 건드린다**(별도 grants 테이블 안은 인덱스 수술이 필요해 버렸다). 소모는 **모델 호출 전 조건부 UPDATE**(뒤로 가면 동시 요청이 모델을 두 번 부른다) · **실패하면 되돌린다** · 일일 캡은 그대로. 🔴 **서버는 리비전을 쌓고 사용자는 최종본만 본다** — `ai_reports`에 기간 UNIQUE가 없어 `revision`+`prompt_ver`로 *"v14는 이랬고 v15는 이렇다"* 를 같은 주로 비교할 수 있다. `check:ai` 108→**119개**(2가지를 일부러 망가뜨려 확인) ([`AI_REPORT_SYSTEM.md`](./AI_REPORT_SYSTEM.md) §6.6) |
| **AI — 탈퇴 시 파기** | ✅ | 2026-08-24 — `POST /api/v1/ai/purge`. `DELETE_ACCOUNT` §3이 *"탈퇴하면 요약문·이용기록이 파기된다"* 고 **게시돼 있었는데 그 코드가 없었다**(`purgeVault`가 AI 테이블을 import조차 안 했다). 서버에 남는 것 중 **유일하게 사람이 읽을 수 있는 일기 파생물**이라 문장이 아니라 코드를 고쳤다. 탈퇴 흐름에 백업 파기와 같은 **차단형**으로 — 계정이 사라지면 지울 권한이 있는 사람이 없어진다. `e2e:ai` 15개(파기 4개 신규) |
| AI 리포트 — 동의 2종 | ✅ | §23 민감정보 · §28-8 국외이전. 체크박스 2개, 묶지 않는다 |
| AI 사업자 **연락처** | ✅ | 2026-08-19 — `OpenAI OpCo, LLC` · `US` · `dpo@openai.com`(`features/ai/vendor.ts`). 처리방침 15개 언어 반영 · `check:ai` 통과. ~~출시 차단~~ 해제 |
| AI 리포트 — 처리방침 | ✅ | 2026-08-13 — 리포트 90일 저장을 반영해 **15개 언어 재작성**(`check:legal` 378개). ⚠ 30일 시계는 **애초에 해당 없었다**(공개 사용자 0명, CLAUDE.md §12) |
| **계정 삭제 안내 — 다국어** | ✅ | 2026-08-17 — 영어 하나였던 것을 **15개 언어**로. Play 데이터 보안 선언에 등록된 URL이라 심사자가 직접 연다 |
| **Play 데이터 보안 선언** | ✅ | 2026-08-24 — 값은 **확정**했다([`PLAY_DATA_SAFETY.md`](./PLAY_DATA_SAFETY.md) §3·§7): 2단계 `삭제 요청 = 예`로 변경 + 3단계 `사용자 ID`·`구매 내역`·`기타 사용자 제작 콘텐츠` **추가**. 기존 6개는 건드리지 않는다(과다 선언은 제재 대상이 아니다). 🚫 **사진은 선언하지 않는다** — Play의 E2EE 예외 원문(*"does not need to be disclosed"*)을 백업이 만족한다. ✅ **제출·출시 완료**(2026-08-24 21:57 제출 → 22:07 출시). 🔴 데이터 보안 폼은 **심사 대기열을 타지 않는다** — 관리형 게시가 꺼져 있으면 저장이 곧 게시다(그걸 AAB 규칙으로 착각해 하루를 미뤘고, 그 하루가 그대로 선언·배포본 불일치 기간이었다). ⏭ 남은 것은 **부분 삭제 요청 페이지**뿐이고 그건 선택사항이다([`PLAY_DATA_SAFETY.md`](./PLAY_DATA_SAFETY.md) §7) |
| **탈퇴 ↔ AI 테이블 정합** | ✅ | 2026-08-24 발견 — 승격된 `PRIVACY §4`·`DELETE_ACCOUNT §3`이 *"탈퇴하면 AI 요약문과 이용 기록을 파기한다"* 고 적는데 **`deleteAccount()`가 `ai_reports`·`ai_usage`를 건드리지 않는다**(`purgeVault`는 import조차 안 한다). 요약문은 `subject_id`에 묶인 채 90일, `ai_usage`는 **영구**로 남는다. 백업 쪽은 *"계정보다 백업을 먼저"* 를 차단형으로까지 설계했는데 **AI 쪽에 그 대칭이 없다.** Play 배지 조건은 `or`이라 폼 블로커는 아니지만 **문안이 거짓**이었다. → **닫았다**(같은 날) — 위 `AI — 탈퇴 시 파기` 줄과 합쳐 읽는다([`PLAY_DATA_SAFETY.md`](./PLAY_DATA_SAFETY.md) §4.4) |
| **처리방침 승격** | ✅ | 2026-08-23 — `pending`(개정 예고) 둘을 **본문으로** 옮겼다. v8·v9에 백업·AI가 들어 있는데 본문이 여전히 *"일기를 서버로 보내지 않으며"* 라 **배포본과 고지가 어긋나 있었다.** `PRIVACY` 13개 절에 엮고 `DELETE_ACCOUNT`는 5→**6개 절**(§5 제목까지 교체) · 15개 언어. ⚠ **Play 데이터 보안 폼보다 먼저**다 — 폼이 "수집함"인데 링크된 방침이 "수집 안 함"이면 그 자리에서 어긋난다 |
| **이용약관** | ✅ | 2026-08-17 신규 — 한국어 정본 22조(`TERMS`) · 앱 화면 · `docs/terms.html` · 설정/구독 링크. ~~번역 14개 진행 중~~ → **완료**(2026-08-21 정정 — `check:legal` 378개가 *이용약관 14개 언어*를 세고 있었다. 이 줄만 🔄로 남아 **끝난 일이 남은 일로 보였다**). 근거는 전자상거래법 §13②([`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §5.2) |
| **§13③ 미성년자 고지** | ✅ | 구독 동의 화면에 상시 표시. 나이를 모르므로 모두에게 보여주는 것이 유일한 이행 방법 |
| **§13⑥ 전환 동의** | ✅ | ~~미구현~~ → **이미 있었다**(2026-08-17 실측 정정). 시행령 §20조의2의 **전환 전 30일** 창도 충족(체험 7일) |
| **기록 리마인더(로컬 알림)** | ✅ | 2026-08-21 — `features/notification/`. **에뮬레이터 실동작 확인**: 예약 7건 → 오늘 저장 시 **오늘 것만 취소**(6건) → 시각 변경 시 전체 재예약 → 토글 OFF 시 0건. `npm run check:notification` 12개. 🔴 `expo-notifications`가 권한을 **13→37**로 늘려 21개를 걷어냈고, **걷어낸 뒤에도 앱이 뜬다**([`NOTIFICATION_SYSTEM.md`](./NOTIFICATION_SYSTEM.md) §5·§8) |
| **업로드 서명 config plugin** | ✅ | 2026-08-21 — `plugins/with-upload-signing.js`. v8은 `android/`를 **손으로 고쳐** 구웠는데 `prebuild`가 그걸 날린다. 알림이 정확히 그 경우였다 |
| ESLint · Prettier | ✅ | ESLint 9 flat config + eslint-config-expo@10. `any` 금지를 린트로 강제 |
| EAS 빌드 설정 | ✅ | 2026-08-13 정정 — `eas.json`(internal·production). v7 AAB를 `eas submit`으로 올렸다([`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §6.1) |

### 🟢 `21 (1.0.1)` 승인 — OTA + R8 (2026-09-09)

| | |
|---|---|
| 담는 것 | 🆕 **OTA(`expo-updates`)** · **R8 난독화** · 처리방침·이용약관 개정(15개 언어) |
| 왜 바로 내나 | 🟢 **실사용자 0**(설치 17은 doply 업체 인원 · 게시 후 신규 주체 0)이라 릴리스 비용이 가장 낮고, **OTA 는 깔려야 유효**해서 미루면 이득만 늦어진다. 형제 선례(*"그것만으로 릴리스를 만들 이유가 없다"*)를 여기서는 뒤집었다 |
| 🔴 법무가 먼저 나갔다 | 처리방침 §2·§6·§13 + 이용약관 제10조·부칙. **공개본(`main`)을 코드보다 먼저 게시**했다 — 그 과정에서 `main` 이 **69커밋 뒤처져** 있던 것도 함께 해소했다(2026-08-27 과 같은 자리) |
| 🔴 OTA E2E | **통과**(AVD `diary` · R8 켜진 릴리스 APK): `dev.expo.EASSharedPreferences.xml` 생성 · 업데이트 요청이 **네트워크까지 도달**(`Remote update request not successful` = 아직 발행된 업데이트가 없어서다) · **오프라인 부팅 오류 0건** · `expo-sqlite`·`SecureStore`·AdMob·연령 게이트 정상 · 앱 크래시 0.<br>🟢 스택트레이스에 `expo.modules.updates.*` 가 **난독화되지 않은 채** 찍혔다 — keep 이 먹은 증거다(옆의 `g9.b0`·`n9.i` 는 난독화됨) |
| ⏭ 다음 릴리스로 | 🔴 **`proguard-android-optimize.txt` 미적용.** 콘솔은 빌드 실물을 읽고 **줄 단위로** 판정한다 — 형제 실측(2026-09-09)에서 그걸 적용한 앱은 *"최적화가 사용 설정되지 않음"* 줄이 **안 떴다**. `expo-build-properties` 로는 못 바꾸므로 빌드 스크립트에서 `prebuild` 직후 `build.gradle` 을 패치해야 한다. **기한 없는 `권장` 등급이라 이것만으로 심사를 다시 타지 않는다**(정본 [`R8_OBFUSCATION.md`](file:///C:/project/common/R8_OBFUSCATION.md) §6.1) |
| ⚠ 크기 | AAB **66.09 → 66.59MB (+503KB)** — 🔴 **줄지 않고 늘었다.** 같은 릴리스에 OTA 가 함께 들어가서라고 **추정**하지만 분리 측정을 안 해 단정할 수 없다. 형제의 −2.2MB(AAB)·−9.8MB(설치)와 **단위가 달라 나란히 놓으면 안 된다** |
| 🟢 승인 | **2026-09-09 오후** — ⚠ 출처는 **사용자 확인**이고 세션 실측이 아니다. 관리형 게시 OFF 라 통과가 곧 게시다 |
| 🟢 **OTA 왕복 검증 완료** | **2026-09-09 · AVD `diary` · R8 켜진 릴리스 APK.** 같은 커밋(`825026d`)에서 발행해 내용이 배포본과 동일하다 — 빌드 이후 바뀐 파일이 문서 넷뿐이고 **앱 번들에 들어가는 것은 0개**임을 먼저 확인했다.<br>`1회차` **받는다**: `NEW_UPDATE_LOADED` · `Update available` · 번들 4.9MB 가 `files/.expo-internal/` 에 실재<br>`2회차` **적용됐다**: `No update available`(더 받을 게 없다)<br>🔴 **결정적 증거는 로그가 아니라 DB 다** — `databases/updates.db` 의 `updates` 행이 `01a084a3bb737ec1b833151b900c91a3` · `status=1`(READY) · `successful_launch_count=1` 이고, 이것이 **발행 결과의 `Android update ID` 와 정확히 일치한다.**<br>⚠ `expo-updates.db`(`files/`)가 아니라 **`databases/updates.db`** 다. 앞엣것은 빈 파일이라 *"테이블이 없다"* 로 오독하기 쉽다 |

---

### 🟢 `20 (1.0.0)` 게시 완료 — 2026-09-09 00:49

`0.2.6` → **`1.0.0`.** 올리는 이유가 숫자 관리가 아니다 — **정식 출시된 제품이 여태 `0.x` 였다.**
`0.x` 는 *"아직 안 끝났다"* 를 뜻하는데, 9/4 에 143개국(9/7)에 유료 구독까지 붙여 내보낸 것이
그 표기와 어긋난다. 그리고 이번 빌드가 **구독 세 혜택 중 하나(백업)를 처음으로 실제 동작시킨다** —
`1.0` 이 성립하는 첫 빌드가 맞다.

| | |
|---|---|
| `versionCode` | **20** (19 → 20) |
| `versionName` | **1.0.0** |
| 담는 것 | 🔴 **백업 켜기 결함**(§4.5) · `resetCodeConfirmation` 배선 · 한국어 문구 159개 정돈 + 내용 결함 5건 × 15언어 |
| AAB | ✅ **구웠다**(2026-09-08 23:07 · 로컬 `bundleRelease` 4분 31초 · 63MB) |
| 서명 | ✅ `B9:A7:29:0A:FF:E8:…` — 운영 업로드 키와 일치(`MONETIZATION_SYSTEM.md` §6.1.4) |
| 게이트 | ✅ `check:release-env`(셸) · ✅ `check:release-bundle` 7개(**AAB 안**) — 둘은 반대 방향이라 합치지 않는다 |
| 콘솔 | ✅ **임시 출시 생성 · 값 입력**(2026-09-08). `게시 개요` 0건 확인 → `새 버전 만들기` → 출시명 `20 (1.0.0)` · **출시 노트 13개 언어**. 새로고침 후에도 남는 것까지 확인.<br>~~🚫 App Bundle 업로드·검토 전송은 안 했다~~ → **둘 다 했다**(2026-09-09 · 사용자 지시). |
| 출시 노트 | ~~3,420자 · 수정 내역을 그대로 적었다~~ → **454자 · `앱을 최적화했습니다.` 한 줄**(2026-09-09 사용자 결정 *"그냥 앱 최적화 했다고만 해라"*). 13개 언어. ⚠ 대가를 적어 둔다 — **백업을 켜려다 실패했던 구독자는 이 노트로는 고쳐졌다는 것을 알 수 없다** |
| ⚠ 게시 방식 | `관리형 게시가 사용 중지됨` — **검토를 통과하면 그 즉시 게시**된다 |
| 제출 | 🟢 **검토 전송**(2026-09-09 00:0x · 사용자 결정 *"그냥 올려 버려"*) |
| 🟢 **게시** | **2026-09-09 00:49 · 143개국 · 전체 출시.** 콘솔 실측(2026-09-09) — 트랙 요약 *활성 · 최신 출시 버전 **20 (1.0.0)** · 국가/지역 143개 · 설치 수 17회* + `출시` 탭 *Google Play에 제공됨 · 버전 코드 1개 · 게시일 9월 9일 오전 12:49* + **게시 개요 0건**. 🔴 **검토가 약 40분**이었다 — 관리형 게시 OFF라 통과가 곧 게시다 |
| ✅ **실기기 왕복 검증 완료** | **2026-09-09 · Galaxy S24 · 스토어 배포본 `20 (1.0.0)`**(설치 경로 `com.android.vending` 확인). 🟢 **결함이 재현되지 않는다** — `백업 켜기` → 복구 코드 → `나중에 하기` 뒤 화면이 **켜진 분기**로 남고(예전엔 *"아직 백업을 켜지 않았어요"* 로 돌아갔다) `지금 백업` 버튼이 나타난다.<br>🔴 **서버까지 갔다**: `vaults` **0 → 1**(`seq=1` · `auth_hash` 있음) · `generations` `seq=1`·`part_count=1`(앱이 말한 *"1개 묶음"* 과 일치) · 매니페스트 5,123B · `vault_blobs` **1개 `committed` 61,760B — 사진까지 올라갔다**.<br>⚠ 복구 코드는 **화면에 뜬 것을 마스킹하고 출력하지 않았다**(하이픈 그룹 화이트리스트). 2026-09-08 에 공백 구분을 가정한 정규식으로 코드를 통째로 노출한 적이 있어 방식을 바꿨다 |
| 어디서 | 무엇이 |
|---|---|
| 조각 서버 DB | `vaults` **0** · `ai_reports` **0** · `ai_usage` **0** |
| common_server 주체 | 누적 11(user 6 + device 5). ⚠ user 5개는 8/10~8/20 생성인데 **활성 기록이 0** — 만들어진 시각이 곧 마지막 접속이다(구글 로그인이 죽어 있던 그 기간과 일치한다) |
| 일별 DAU | 9/2 **2** · 9/3 0 · 9/4 **4** · 9/5 0 · 9/6 0 · 9/7 0 |
| 🔴 게시(23:43) **이후** 생성 주체 | **0개.** 사흘째 아무 신호도 없다 |
| 9/4의 4개 | **한 사람의 저녁 한 세션**이다 — 18:15 device 가 21:37 까지 살아 있고 같은 21:37 에 user 주체가 생겼다(로그인 전환일 2행 모양). 그 user 의 마지막 접속이 **23:43, 게시 시각과 같다.** 21:49 문의도 그 사이다 → **우리 실기기 검증 세션**이다 |
| 자동화 흔적 | 없음. 생성 간격·시각 비트가 전부 제각각이라 `idea_repository` 의 15개 등간격 군집과 다르다(§5-1 판정법) |

🔴 **17이라는 설치 수를 사용자 수로 읽지 않는다.** 그건 doply 업체 인원이 비공개 테스트 요건
(12명·14일)을 채우려고 설치한 것이다 — **설치가 곧 사용이 아니고, 그들은 다시 안 연다.**
한국만 · 등록정보 최적화 0 · 광고 0인 상태로 사흘이라 **아직 아무도 이 앱을 찾지 못했다**가 정확하다.

⚠ **한 가지는 못 닫았다**(공통서버 지적). *"안 켰다"* 와 *"켰는데 그 버전이 기기 등록을 안 한다"* 는
서버가 구분하지 못한다 — 기기 등록은 **vc16(9/2)부터**다. Play `통계 → 앱 버전`을 열었더니
**데이터가 8/30에서 끊긴다**(6일 지연). 그 마지막 날 기준 **17명 전원이 `0.2.0 (13)`** 이었다 —
즉 8/30까지는 **등록할 코드가 아예 없었다.** 그 뒤 갱신 여부는 Play 지표가 따라잡아야 안다.
⏭ 2~3일 뒤 같은 화면을 다시 본다. `vc19` 비중이 높은데도 주체가 0이면 그때 ①이 확정된다.

#### 🌏 전 세계 출시 — 143개국 (2026-09-07)

유입 0(위)이 확정되자 **모수를 늘리는 쪽**으로 갔다(사용자 결정). 한국 1개국 → **143개국**.

| | |
|---|---|
| 스토어 등록정보 | 🔴 **`ko-KR` 하나였다.** 한국어 개선 + **`en-US` 신설** — 제목·간단·자세한 설명 + **영어 스크린샷 4장**(홈·캘린더·상세·리포트). 문안 정본은 `assets/store/build-listing.mjs` — **12개 언어를 다 써뒀고** 길이 검사를 통과한다(나머지 11개는 콘솔 입력만 남았다) |
| 출시 국가 | **143개** = 177 − 34. EEA 27 + EEA비EU 3 + 영국·스위스 = 32, 여기에 **지브롤터**(영국 GDPR 적용)와 **`기타 국가`**(무엇이 들어 있는지 알 수 없는 묶음)를 더 뺐다 |
| 구독 요금제 | 🟢 **이미 174개국이었다.** `CLAUDE.md` §9.1의 *"국가를 넓히면 요금제도 함께"* 는 이 앱엔 해당 없었다 — 월간·연간·`freetrial-7d` 셋 다 174개국 활성 |

🔴 **기본 언어는 `ko-KR` 그대로다 — 바꾸려면 그래픽이 필요하다.** `en-US`로 전환하면 콘솔이
*아이콘·그래픽 이미지·스크린샷*을 요구하고 텍스트만으로는 저장이 거부된다(실측). 그 이미지는
**콘솔에만 있고 레포에 없다**(8/20 생성분). 그래서 지금은 **번역이 없는 로케일이 한국어를 본다** —
영어·일본어 등 등록정보를 추가한 언어만 자기 언어를 본다.
✅ **en-US 가 자체 자산을 갖췄다**(2026-09-07) — 영어 스크린샷 4장 · 아이콘 · **영어 그래픽 이미지**
(`Jogak / One piece of each day`). 그래픽은 `scripts/make-feature-graphic.py` 가 **코드로 그린다** —
한국어판에는 문구가 그림 안에 박혀 있어 영어 등록정보에 그대로 나가고 있었다.

~~🔴 **그런데 기본 언어 전환은 또 막혔다 — 이번엔 구독이다.**~~ → ✅ **해소(2026-09-07).** 콘솔 원문:
*"기본 언어를 영어(미국) – en-US 로 변경하기 전에 **모든 정기 결제가 en-US 로 번역**되어야 합니다"*
→ `jogak_pro` 의 상품 이름·혜택이 한국어 하나뿐이었다. **등록정보를 다 채워도 상품이 막는다** —
등록정보와 상품은 **별개의 언어 목록**을 갖는다.

| en-US | 값 |
|---|---|
| 이름 | `Jogak Pro` |
| 혜택 | `Quiet, with no ads` · `Encrypted cloud backup & restore` |
| 설명 | 비움(한국어도 비어 있다 — Play 에 표시되지 않는 선택 항목) |

✅ **기본 언어 = `영어(미국) – en-US`** (2026-09-07 저장). 콘솔 헤더가 `Jogak - Private Diary
Journal` 로 바뀐 것이 반영 증거다. 이제 **번역이 없는 로케일이 영어를 본다** — 141개국이
한국어 페이지를 보던 상태가 닫혔다. ⚠ 게시 개요에 **검토 대기 4건**으로 올라가 있다(제출은 사용자).

✅ **나머지 11개 언어 등록정보도 입력했다**(2026-09-07) — ja-JP · zh-CN · zh-TW · es-419 ·
pt-BR · fr-FR · ru-RU · id · vi · th · tr-TR. 저장 후 **12개 언어를 다시 읽어 정본과 대조**했다
(`listing.json` 의 제목·간단·자세한 설명 길이 대 콘솔 실측 — **불일치 0**). 독일어·이탈리아어는
EEA 배포 제외라 넣지 않았다(§9.1).

🔴 **Play Console 폼은 프로그램으로 값을 넣을 수 없다 — 실제 키 입력만 모델에 닿는다.**
세 방법을 실측했다:

| 방법 | 문자 수 카운터 | Angular 모델 |
|---|---|---|
| `value` 프로퍼티 setter + `input` 이벤트 | ✅ 갱신됨 | 🔴 **비어 있다** |
| `document.execCommand('insertText')` | ✅ 갱신됨 | 🔴 **비어 있다** |
| `computer.type`(실제 키 입력) | ✅ | ✅ |

⚠ **카운터가 갱신되는 것이 함정이다** — `19/30` 이 떠 있는데 그 아래는 *"앱의 이름을
추가하세요"* 가 빨갛게 남는다. 카운터만 보고 넘어가면 **빈 값이 저장된다.**
→ 검증은 카운터가 아니라 **`추가하세요` 오류 문구 개수**로 한다.

🔴 **좌표 클릭은 이 페이지에서 신뢰할 수 없다.** 스크린샷 좌표와 `getBoundingClientRect()` 의
오프셋이 상태마다 달라졌고(0px ~ +93px), *"일부 언어에 오류가 있습니다"* 배너의 유무만으로도
칸이 한 줄씩 밀린다. 실제로 **두 번 한 칸 아래에 입력**했고 그중 한 번은 완성돼 있던 ja-JP 의
본문 890자를 덮어썼다. → **JS 로 `.focus()` + `setSelectionRange()` 한 뒤 타이핑**한다.
좌표가 개입하지 않고, 선택 범위가 잡혀 있어 기존 값도 안전하게 교체된다.

⚠ **매 언어마다 입력 직후 길이를 확인한다.** 이번에 그 확인이 두 사고를 **입력 직후에** 잡았다 —
저장 뒤였으면 143개국에 나간 다음에 알았다.

✅ **검토 제출까지 완료**(2026-09-07, 사용자). 게시 개요의 변경사항 — ① 143개국 확대
② 기본 언어 `en-US` ③ 12개 언어 등록정보 — 가 한 묶음으로 Google 검토에 들어갔다.
⚠ **게시는 콘솔에서 일어나 세션에 전달되지 않는다**(§2 첫 절의 그 사고). 결과는
[`common/PLAY_CONSOLE_STATUS.md`](file:///C:/project/common/PLAY_CONSOLE_STATUS.md) 로 확인한다.

🔴 **왜 두 번 막혔나 — "기본 언어"는 등록정보 한 곳의 설정이 아니다.** 전환은 ① 등록정보 자산
(아이콘·그래픽·스크린샷) ② **정기 결제 상품 번역** 둘을 모두 요구하고, 콘솔은 **하나씩 순서대로**
알려준다. ①을 채우기 전에는 ②를 말해주지 않아 *"이제 되겠지"* 가 두 번 틀렸다 —
상품·구독처럼 **등록정보 바깥에 있는 현지화 대상**을 먼저 훑는 것이 옳은 순서다.

⚠ **국가 34개를 뺄 때 일괄 자동화가 안전 가드에 막혔다**(운영 앱 배포 국가라 합당하다).
검색 → 해제를 하나씩 했고, **저장 전에 177행을 전수 대조**해 *제외 대상 0개 잔존 · 이름 못 찾음 0*
을 확인했다. 🔴 이 대조가 필수인 이유는 첫 시도에서 추출이 **172개만 잡아 덴마크·프랑스·핀란드가
조용히 빠졌기** 때문이다 — 그대로 눌렀으면 GDPR 미대응 상태로 세 나라에 나갔다.

| 출시 뒤 남은 것 | |
|---|---|
| ~~🔴 **첫 문의가 60시간 미답변**~~ | ✅ **실사용자가 아니다 — 우리 테스트다**(2026-09-07 사용자 확인). 2026-09-04 21:49 · `bug` · android · `app_version 0.2.5` = 그날 구글 로그인 복구를 검증하며 **실기기에서 보낸 문의**다. 서버 `tickets_stale` 알림이 이걸 물고 울고 있었다 — **닫으면 그친다**(콘솔). 🔴 남는 교훈은 §5.6: **검증용 문의에 표식이 없으면 운영 알림과 구분되지 않는다** |
| ✅ **난독화 2% — R8 을 켰다 · 릴리스 APK E2E 통과**(2026-09-09). ~~⏭ 스토어 미반영~~ → **`21 (1.0.1)` 로 나갔다**(2026-09-09 승인) | 정본은 [`common/R8_OBFUSCATION.md`](file:///C:/project/common/R8_OBFUSCATION.md) — **형제가 이미 풀었다**(My Word 1.4.0·vc20 게시로 경고 소멸 실측). 조각은 그 문서의 **B 방식**이다.<br>🔴 **`android/` 를 손으로 고치면 안 된다** — CNG 산출물이고 `scripts/release/build-release-aab.sh` ②가 **매 빌드 `prebuild`** 를 돈다. 2026-08-21 업로드 서명이 정확히 그 사고였다. → **`expo-build-properties` 로 `app.json` 에 넣는다.**<br>🔴 ~~조각은 OTA(`expo-updates`)가 없다~~ → **같은 릴리스에 함께 붙였다**(vc21). 그전까지는 R8 이 뭘 조용히 죽이면 되돌리는 길이 **새 AAB + 심사**뿐이었다. 심사는 실측 **약 40분**(2026-09-09)이지만 그 사이 사용자는 깨진 빌드를 쓴다. 그래서 **릴리스 APK E2E 를 통과하기 전에는 올리지 않는다** — 디버그 빌드는 R8 을 안 타서 검증이 성립하지 않는다.<br>⚠ 조각이 특히 볼 것: **`SecureStore` 의 백업 비밀**(`jogak.backup.secret`)과 **`expo-sqlite`** — 사용자 데이터 쪽이라 심사 40분으로도 못 되돌린다.<br>✅ **E2E 통과**(AVD `diary` · 릴리스 APK · 실측표는 정본 §5.1): 앱 크래시 0 · `expo-sqlite` `files/SQLite/jogak.db` v9 에 조각 저장·조회·streak · `SecureStore` 키 7개(**평문 PIN 없음**) · 재시작 후 잠금이 서고 **PIN 으로 해제**(해시 검증 동작) · `admob.xml` · 연령 게이트가 **취소해도 일기를 쓴다**.<br>난독화 실측: 클래스 **25,532 중 19,028개(74.5%) 개명** · `expo.modules.*` **2,785개 보존**. 빌드 4분31초 → **18분 6초**.<br>~~⏭ 기한 2027-02 라 급하지 않다. 다음 릴리스에 묶는다~~ → **묶지 않고 vc21 로 바로 냈다**(OTA 와 같은 판단 — 실사용자 0 이라 릴리스 비용이 가장 낮다). 🔴 **경고 소멸은 게시 후에만 확인된다** — 업로드만으로는 안 꺼진다. ⏭ 그리고 **`proguard-android-optimize.txt` 가 아직 빠져 있어** 콘솔 경고가 그대로일 수 있다(§`21 (1.0.1)` 표) |
| 🟡 권장 조치 2개 | 대형 화면 — 넓은 화면용 API/파라미터 · 크기 조절·방향 제한 삭제 |
| ~~⏭ **vc19 의 수정 셋이 실기기 미확인**~~ | ✅ **실기기 확인 완료**(2026-09-08 · Galaxy S24 `SM-S921N` · 스토어 설치본 `19 (0.2.6)`). 아래 표 |
| ⏭ 백업 · AI 리포트 · 재생성 | 실기기 미검증(구독이 필요하다).<br>🟢 **배포 라우트 전수 점검 통과**(2026-09-08) — 아래 §2-3.<br>🟢 **서버는 산다**(2026-09-08 실측 — `GET /api/health` → `{ok:true, db:"up", storage:"configured"}` 2회 + 같은 날 DB 직접 쿼리 성공). `CLAUDE.md` §5 의 *"`jogak-stg` 정지 중이라 백업·AI 가 실패한다"* 는 **낡은 서술**이었다 — 언젠가 재개됐고 아무 문서도 몰랐다(9/4 출시와 **같은 종류의 사각**).<br>⚠ 확인한 것은 *"배포본이 DB·Storage 에 닿는다"* 까지다. **백업 왕복·AI 실호출은 여전히 미검증** |
| ~~⏭ `RC_SANDBOX_GRANT`~~ | ✅ **해당 없음**(2026-09-07 정정) — 이 줄은 *"껐는지 확인 안 됨"* 으로 남아 있었지만 [`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §6.3 이 이미 **애초에 켜지 않는다**로 닫아 놨다(라이선스 테스터 대신 **실제 결제**로 검증한다 — 그 플래그가 common_server 전역이라 stg 를 위해 켜면 운영에도 켜지기 때문). 🔴 또 **끝난 일이 남은 일로 남아 있었다** — 이 레포의 지배적 결함이다 |
| ~~⏭ 스토어 등록정보가 `ko-KR` 하나~~ | ✅ **완전히 닫혔다**(2026-09-07) — `en-US` 신설 + **기본 언어로 승격** + **12개 언어 전부 입력**(ko·en + 11개). 저장 후 정본 대조 불일치 0. 🔴 다음 관문은 **콘솔 밖**이다 — 등록정보가 갖춰졌으니 이제 막는 것은 문안이 아니라 **유입**이다 |

#### 🟢 배포 라우트 전수 점검 — **404 가 하나도 없다** (2026-09-08)

2026-08-18 사고(*"AI 라우트는 배포된 적이 없었다 — `POST /api/v1/ai/report` 가 **404**"*)의
**재발 검사**다. 인증 없이 두드려 *존재하고 게이트가 도는지*만 본다(본문은 `{}` 만 보낸다).

| 라우트 | 응답 | 뜻 |
|---|---|---|
| `POST /api/v1/ai/report` · `ai/purge` | **401** | 배포됨 + 인증 게이트 정상 |
| `GET /api/v1/ai/regenerable` | **401** | 〃 (POST 는 405 = GET 전용) |
| `POST /api/v1/backup/{latest,reserve,commit,blobs,delete,rebind}` 6개 | **401** | 〃 |
| `GET /api/admin/{overview,ai,reports,vaults}` · `cron/reap` | **401** | 🔒 관리자 라우트가 **열려 있지 않다** |
| `GET /api/admin/regenerate` | **405** | POST 전용 |

🔴 **401 이 나오는 것이 성공이다.** 찾던 실패 신호는 **404**(라우트 자체가 없음)이고 하나도 없다.
⚠ 그리고 이건 *"라우트가 산다"* 까지다 — **인증을 통과한 뒤의 왕복은 여전히 미검증**이다(구독 필요).

#### ✅ vc19 수정 셋 — 실기기 확인 (2026-09-08)

**설치가 필요 없었다.** 실기기에 이미 `19 (0.2.6)` 이 깔려 있어 **스토어 배포본 그대로** 확인했다 —
`CLAUDE.md` §12(2026-08-24)가 *"실기기에는 dev 빌드를 넣을 수 없다"* 며 제시한 세 길 중
**③ 트랙에 올린 빌드로 확인**이 이미 충족돼 있었다.

| 수정 | 결과 | 무엇을 봤나 |
|---|---|---|
| **리포트 잠금 화면** | ✅ | 주간 `매주 돌아보는 시간` + 예시 카드 / 월간 `매달 돌아보는 시간` / 연간 `매년 돌아보는 시간` — **탭마다 제목이 갈리고 예시 카드는 주간에만** 뜬다. 9/4 신고(*연간을 눌러도 "매주 … 8월 24일–30일"*)가 재현되지 않는다 |
| **목록 포커스** | ✅ | `AAAAA` → 목록 삽입 → `111` → **앞 문단에 커서를 두고 두 번째 목록 삽입** → `222`. 결과가 `AAAAA / • 222 / • 111` 로, **222 가 새 목록 항목에 들어갔다.** 버그였다면 `AAAAA222` 가 된다 |
| **삭제 뒤 리마인더** | ✅ | `dumpsys alarm` 실측 — 저장 전 **7건**(오늘 21:00 포함) → 저장 후 **6건**(오늘 빠짐, 2026-08-21 규약) → **삭제 후 다시 7건**(오늘 복귀). 9/4 신고(*"지우면 안 돌아왔다"*)가 닫혔다 |

✅ **형제도 확인했다 — 사진 삽입 포커스**(2026-09-08, 에뮬레이터 `common_1`). 실기기에서는
사용자의 실제 갤러리를 열어야 해서 못 했던 것을, 테스트 이미지를 넣은 에뮬레이터로 닫았다.

| 절차 | 결과 |
|---|---|
| 사진① 삽입 → `111` 입력 | `111` 이 **사진① 뒤** 블록에 들어갔다 |
| 이어서 사진② 삽입 → `222` 입력 | `222` 가 **사진② 뒤** 블록에. 최종 구조 `사진① / 111 / 사진② / 222` |

9/4 신고의 두 증상 — *"사진 뒤에 이어 쓰면 **위** 문단에 들어간다"* · *"연달아 두 장 넣으면
**두 번째가 엉뚱한 자리**"* — 가 **둘 다 재현되지 않는다.**

🔴 **픽셀이 아니라 UI 트리로 읽었다.** 처음엔 화면만 보고 *"AAAAA 가 사라졌다"* 고 오독했는데,
`uiautomator dump` 로 보니 **한글 IME 가 켜져 있어 `AAAAA` → `ㅁㅁㅁㅁㅁ` 로 바뀐 채 제목 칸에**
들어가 있었다(작성 화면 진입 직후 본문 포커스 전에 타이핑한 탓). 스크린샷 산수로는 못 봤다 —
`§10` 의 *"UI 트리에는 온전한 문자열이 들어 있어 접근성 덤프로는 안 잡히고 픽셀을 봐야 한다"* 의
**반대 방향 사례**다: 이번엔 **픽셀이 거짓말했고 트리가 진실**이었다.

🟢 **덤으로 `File.copy` 도 통과했다.** `image-store.ts:117` 의 앱 내부 → 앱 내부 복사가
정상 동작한다(사진이 실제로 들어갔다). SnoreLess 세션이 같은 API 로 `Missing 'READ'` 를 겪어
공유해 준 건인데, 원인은 `FileSystemPath.copy()` 가 **목적지에도 READ 를 요구**하고
`FilePermissionModule` 이 *"`filesDir`/`cacheDir` 하위면 존재 여부와 무관하게 RW, 아니면
`File(path).canRead()`"* 로 판정하기 때문이다. 조각은 목적지가 **`Paths.document` 하위**라
첫 갈래에서 통과한다. ⏭ 나중에 `Paths.document` **밖**으로 내보내는 기능을 붙이면
`File.copy` 로는 안 되고 **SAF** 여야 한다.

🟢 **부수 확인 2건**: ① **연령 게이트가 부팅에 실제로 선다**(2026-09-01 결정) — 문구도
*"일기를 쓰는 데는 필요하지 않아요"* · *"고른 해는 저장하지 않아요"* 로 §4·§1.4 와 일치하고,
**`취소` 를 눌러도 일기는 그대로 쓸 수 있다**(*"벽이 아니다"* 의 실증). ② 홈 빈 상태에
**버튼이 없다**(2026-08-23 결정) · 테스트 배너 광고 정상 노출.

⚠ 검증용 조각은 **만들고 지웠다**(순 효과 0 — 삭제가 곧 검증 대상이었다). 홈 화면이
원래 상태로 복귀한 것까지 확인했다.

**`0.2.5` 는 프로덕션에 없다.** 트랙 이력을 정리해 둔다(공통서버 세션 질문에 대한 답):
`vc18(0.2.5)` 는 **어느 트랙에도 올린 적이 없다** — 로컬 빌드로 실기기에 넣어 구글 로그인 복구를
검증한 빌드이고(`git 264c5a6`), 그날의 문의는 그 기기에서 나갔다. 프로덕션에 올라간 것은
**그 뒤 같은 날 구운 `vc19(0.2.6)` 하나뿐**이다.

---

### 비공개 테스트 — 프로덕션까지의 경로 (2026-08-20 ~ 09-02)

**데이터 보안 선언 저장 완료(2026-08-24)** — `사용자 ID`·`구매 내역`·`기타 사용자 제작 콘텐츠` 3개 추가. `사진`은 E2EE 예외로 선언하지 않았다. 🔴 §7 체크리스트의 *"삭제 요청 → 예"* 는 **콘솔 질문을 잘못 짚은 것**이었다 — 실제 질문은 *"계정을 삭제하지 않고도"* 부분 삭제(선택사항)이고 전용 URL을 요구한다([`PLAY_DATA_SAFETY.md`](./PLAY_DATA_SAFETY.md) §7). ✅ **검토 전송도 끝났다** — 기다릴 필요가 없었다. 저장하자 **혼자 제출·출시**됐다(제출 3 · 8/24 21:57 → 22:07). 관리형 게시가 꺼져 있으면 폼 저장이 곧 게시다.

**v17 검토 중(2026-09-02)** — **웜 스타트 하트비트**. vc16 이 같은 날 11:46 에 게시된 뒤 올렸다
(doply 조건이 *"2~3일 간격으로 올려라"* 라 같은 날 두 번은 위반이 아니라 조건을 만족시키는 쪽이다 —
`common/CLOSED_TESTING.md` §4. 이걸 *"자주 올리지 마라"* 로 거꾸로 읽어 한 번 미룰 뻔했다).
권한 4→5→4 · `eas submit --profile closed` · 출시 노트(ko-KR) · 검토 전송 완료.
🔴 **번들 안을 확인하다 두 겹으로 틀렸다** — Hermes 라 비ASCII 가 UTF-16LE 인데 UTF-8 로 찾아서
한국어·일본어·중국어가 전부 0 으로 나왔고, **영어만 ASCII 라 잡혀** 마치 번역이 안 실린 것처럼 보였다.
대조군(`조각`)을 세고서야 방법이 틀린 걸 알았다([`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §6.1.4).

**v16 게시(2026-09-02 11:46)** — 비로그인 활성 지표(기기 subject) · **연령 게이트를 부팅으로** ·
처리방침 개정(30일 자기구속 제거 + 기기 식별자, 15개 언어). `versionName` 도 함께 올렸다(**0.2.3**) —
vc9~vc13 이 전부 `0.2.0` 이던 습관을 8/31 에 끊은 그 규약이다(`CLAUDE.md` §12).
권한 4→5→4 · `eas submit --profile closed` · 출시 노트(ko-KR) · 검토 전송 → **검토 44분 만에 게시**.
🔴 **권한을 되돌리다 `프로덕션으로 출시`를 켤 뻔했다** — 스크린샷을 찍고 그 좌표를 눌렀는데
다이얼로그가 그 사이 스크롤해 **바로 윗줄**을 눌렀다. Play 콘솔의 **2단계 저장**(늘 함정이던 그것)이
저장 전이라 막아줬고, 클릭 뒤 확대해서 상태를 읽어 잡았다. 이후 체크박스는 좌표가 아니라
`find` 로 얻은 ref 로 눌렀다 ([`common/PLAY_RELEASE_AUTOMATION.md`](file:///C:/project/common/PLAY_RELEASE_AUTOMATION.md) §5.13 신설).
⚠ 그리고 **Chrome 이 구 계정(`bjpio113` · `u/0`)으로 열린다** — 운영 계정은 `u/1` 이다.
`u/0` 으로 들어가면 구 조직 계정(`6530743120826503035`)만 보여 조각이 목록에 없다.

**v13 검토 중(2026-08-27)** — 로그인 **연령 게이트**. 권한 4→5→4 · `eas submit --profile closed` ·
출시 노트(ko-KR) · 검토 전송까지 완료. 게시 개요가 비어 있어 큐를 안 탔다(v12 는 8/25 22:58 게시됨).
🔴 **한 번 거절당했다** — `app.json` 의 `versionCode` 를 13 으로 고쳤는데 `android/app/build.gradle` 은
**12 그대로**였다. 그 값은 `prebuild` 때 옮겨지므로 app.json 만 고치면 빌드는 조용히 성공하고
`eas submit` 이 *"이미 제출된 버전"* 으로 거절한다. `prebuild` 후 재빌드해서 해결
(⚠ 서명 config plugin 은 prebuild 를 살아남았다 — v8 사고가 재발하지 않았다).

**v12 게시(2026-08-25 22:58)** — 리포트 시각화 3층(그 기간의 모양 · 지난 기간 비교 · 지표 넷) + 시간대 버그 수정.
🔴 굽는 데 **네 번 걸렸다** — 죽인 빌드가 남긴 산출물, 그리고 **gradle이 JS 번들을 다시 안 만드는 것**
(`createBundleReleaseJsAndAssets UP-TO-DATE`)에 두 번 막혔다. `check:release-bundle` 신설로 셋 다 잡았다
([`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §6.1.4).

~~v10 업로드 완료(2026-08-24)~~ → **v11로 대체** — 본문 텍스트 서식 · 필수 env 3개 복구(v9는 백업·AI·결제가 전부 죽어 있었다). alpha 트랙에 `draft`로 들어갔고 테스터가 받던 v9는 그대로다. v10 은 파기 코드 이전 빌드라(14:02 빌드 · 18:58 커밋) **v11 로 다시 구워 올렸다** — AI 파기가 들어갔고 조각 서버에도 배포·검증(401)이 끝났다. ✅ **v11 게시 완료**(2026-08-25 13:12) — 출시 노트(ko-KR) → 검토 전송 → **테스터에게 제공 중.** 테스터가 받던 v9 는 `제공됨` 그대로다. 업로드 권한은 켰다 껐다([`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §6.1.4).

운영 앱에서 **12명 · 14일** 요건을 채우는 중이다. versionCode 8 · 테스터 43명 · 대한민국.
상세는 [`MONETIZATION_SYSTEM.md`](./MONETIZATION_SYSTEM.md) §6.1.9, 트랙별 국가는 `CLAUDE.md` §9.1.

| 오늘 배운 것 | |
|---|---|
| 🔴 스토어 등록정보 | **비공개 테스트에도 필수**다. 프로덕션 전용이 아니다 |
| 🔴 트랙 국가 기본값 | *프로덕션과 동기화* = **활성 0개** — 그대로 두면 아무도 설치 못 한다 |
| 🔴 청구 통화 | 우리 가격이 아니라 **구매자 계정**을 따른다(§6.1.10). 요금제를 건드리면 안 된다 |
| 🟡 콘솔 이미지 업로드 | OS 창 없이 된다(`common/PLAY_RELEASE_AUTOMATION.md` §5.10) |

### ⚠ 출시 전에 반드시 처리할 것 — 🟢 **전부 닫힌 채로 출시했다**(2026-09-04)

| 항목 | 내용 |
|---|---|
| ~~🔴 **연령 게이트가 없는데 처리방침이 있다고 말한다**~~ | ✅ **해소(2026-08-27 구현 · 2026-09-01 부팅으로 이동).** 이 줄이 근거로 든 사실 둘이 **지금은 둘 다 거짓이다**(2026-09-04 실측): `features/auth/`(게이트·판정·저장·화면)가 있고 `app/_layout.tsx:228` 이 `AgeGateScreen` 을 세운다 · 연령 문자열은 `ageGate` 네임스페이스 **10개 × 15개 언어**다 · `check:age-gate` **148개** 통과. 🔴 **끝난 일이 블로커 표에 남아 있었다** — 조각에서 최소 일곱 번째다(`.claude/skills/doc-consistency` §⑥). 상세는 §2 현황표의 **연령 게이트** 줄 |
| ~~🔴 **공개 법무 페이지가 낡아 있다**~~ | ✅ **해소(2026-08-27)**. GitHub Pages 가 `main` 을 서빙하는데 `main` 이 develop 보다 **66커밋 뒤처져** 공개 처리방침이 **8/11 내용**이었다 — *"일기를 서버로 보내지 않으며"* 를 적고 `OpenAI` 를 한 번도 언급하지 않는 판본이, **Play 데이터 보안 폼(8/24 · 일기 본문 수집 = 예)에 등록된 바로 그 URL** 에 떠 있었다. `main` fast-forward + push 로 닫았다(terms.html 404 도 함께). 🔴 **교훈: 배포 브랜치와 게시 브랜치가 다르면 법무 문서는 조용히 낡는다** — 8/23 승격이 공개본에 닿기까지 4일이 걸렸고 그동안 **아무 검사도 울지 않았다**(`check:legal` 은 소스만 본다) |
| ~~Lucide 번들 비대~~ | ✅ **해소.** 2026-08-21 실측 — `from 'lucide-react-native'` 형태의 import가 **0건**이고 전부 `lucide-react-native/icons/<name>` 개별 경로다. 이 줄이 남아 **끝난 일이 출시 블로커로 보였다** |
| ~~🔴 **조각 서버 DB가 정지돼 있다 — 백업·AI가 죽어 있다**~~ | ✅ **해소(2026-09-04)**. 재개가 아니라 **Pro 조직에 운영 프로젝트를 신설**했다(`jogak` · 서울 · Micro · **+$10/월**, 사용자 결정) — 무료 조직은 슬롯 2개에 프로젝트 3개라 재개는 증상만 멈춘다. 🔴 **Vercel 은 `jogak-stg` 프로젝트를 그대로 쓴다 — 이름이 거짓말이다**: 새 프로젝트를 만들면 URL 이 바뀌는데 그 URL 이 **번들에 박혀 있어** 앱을 다시 구워야 한다. 그대로 두니 `eas.json` 을 안 고치고도 **테스터 43명의 백업·AI 가 즉시 살아났다**. 실측: `db:push` 8테이블 · Storage 왕복 · `/api/health` `db:"up"` · e2e 32 · e2e:ai 15 · **verify:regenerate 11**(신설) · 흔적 정리 후 DB 0행. 🔴 밟은 함정 넷(리전 기본값 · `Redeploy`≠새 코드 · `ADMIN_TOKEN` 미설정 · Data API 기본 ON)은 [`BACKUP_SYSTEM.md`](./BACKUP_SYSTEM.md) §6.5.1 |
| ~~🔴 **운영 앱 구글 로그인이 안 된다**~~ | ✅ **해소(2026-09-04)**. 기기 실측 SHA-1 `48:3A:B1:E9…`가 Cloud Console에 **등록돼 있지 않았다**(등록된 것은 `CD:86:BB:DB…` 하나뿐 · 8/19 앱 이전 뒤 만든 클라이언트 0개). → `jogak-android-play-2026`(`com.son0925.jogak` + 실측 지문)을 발급하니 **재빌드 없이, 몇 분 만에** 열렸다 — 같은 vc18에서 로그인 → 문의 전송 → 내 문의 조회까지 확인. ⚠ `CD:86…` 쪽은 **안 지운다**(Play가 그 키로 전환하는 날 필요하다). 🔴 남는 사실: **언제부터 죽어 있었는지 모른다** — 원격 관측 0이고 그 클라이언트에 *"사용 데이터가 전혀 기록되지 않았다"* 경고가 붙어 있었다. 상세는 [`SUPPORT_SYSTEM.md`](./SUPPORT_SYSTEM.md) |
| 패키지 설치 후 Metro 재시작 | 서버를 켠 채 설치하면 캐시가 낡아 `Unable to resolve module`이 난다(2026-08-07에 두 번 겪음). 설치 후 `--clear` 재시작이 기본 |

### 서버 (조각 밖 선행 작업 포함)

| 영역 | 상태 | 비고 |
|---|---|---|
| Metro `server/` blockList | ✅ | 선반영 완료 |
| common_server `apps`에 `jogak` 등록 | ✅ | 2026-08-13 정정 — `bootstrap?app=jogak`이 **200**이다(2026-08-11 실측) |
| common_server SDK 복사 | ✅ | `lib/common-server/{client,index,types}.ts`. **원본 `2026-09-01`**(활성 지표). 이제 본문이 원본과 **바이트로 같다** — `.prettierignore`에 넣어 형식 diff를 없앴다([`SUPPORT_SYSTEM.md`](./SUPPORT_SYSTEM.md) §2에 대조 명령) |
| **활성 지표(DAU/WAU/MAU)** | ⚠ | 2026-09-01 — `fetchBootstrap()`이 세션을 실어 보낸다. 호출부 변경 0. 🔴 **로그인이 선택이라 비로그인 사용자는 안 잡힌다** — 숫자를 "조각 전체 활성"으로 읽지 않는다([`SUPPORT_SYSTEM.md`](./SUPPORT_SYSTEM.md) §3.1) |
| **common_server Phase 7**(subjects·토큰) | ✅ | 2026-08-13 정정 — 로그인·문의·탈퇴가 실제로 돈다 |
| **common_server Phase 9**(RevenueCat) | ✅ | 2026-08-13 정정 — 2026-08-10 완료(결제 가드 37/37). ⏭ 남은 것은 RC 대시보드 상품 import·attach |
| 조각 서버(Next.js) 생성 | ✅ | 2026-08-13 정정 — 배포됨. 백업 6 라우트 + `cron/reap` + `ai/report` |
| 조각 Supabase 프로젝트 | ✅ | 2026-08-13 정정 — `jogak-stg`(서울). ⏭ **운영 프로젝트는 미생성**(Pro 조직 추가 시 +$9.8/월) |
| 백업 라우트 — 되찾기(`rebind`)·파기(`delete`)·리퍼(`cron/reap`) | ✅ | 2026-08-13 정정 — 셋 다 구현돼 있다. 크론은 `vercel.json`에 등록돼 있다(`0 18 * * *`) |
| **운영 콘솔** `/ops-7c1d94` | ✅ | 2026-08-13 — 대시보드·AI 사용량·**리포트 품질**·백업 금고·정리 5탭. **읽기 전용**. 🔴 *개인 비특정*은 2026-09-04에 **"입력으로는 쓰고 출력으로는 안 낸다"** 로 다시 그었다 — *"리포트가 별로예요"* 문의를 열 수 없으면 CS가 성립하지 않고, 처리방침이 이미 *"90일 전 삭제를 문의로 요청"* 까지 약속해뒀다. 훑어보기(subject 목록)는 여전히 안 만든다. `npm run check:admin` 34개. 🔴 **상수 단일화 2개**(2026-09-04) — 콘솔이 `aiEffort` 기본값을 따로 `low`로 적어놔 **실제로는 medium인데 화면이 low라고 말했다**(§5 *"정책은 화면이 아니라 코드 상수다"*). [`ADMIN_SYSTEM.md`](./ADMIN_SYSTEM.md) |
| ⚠ `ai_usage` 테이블 | ✅ | 2026-08-13 — **stg DB에 없었다**(`db:push` 누락). 콘솔을 붙이다 발견했고 push했다 — 그전까지 AI 라우트는 성공 경로에서 500이었을 것이다 |

🚫 = 안 하기로 결정 / ⏸ = 보류 / ❌ = 미착수 / ✅ = 완료

---

## 3. 검증 루틴

```bash
npm install                    # 의존성

# 커밋 전 필수 (CLAUDE.md §11 ④)
npm run typecheck              # tsc --noEmit
npm run lint                   # eslint
npm run check:i18n             # 키 누락·언어 간 불일치 + **다른 언어에 한글이 남았는가**
npm run check:legal            # 378개 — 절·줄 수 + **정본 지문**(문구만 바뀐 드리프트 방어).
                               #   처리방침·계정 삭제 안내·이용약관 × 14개 언어
npm run legal:stamp --check    # 지금 어긋난 언어만 본다. <lang>을 주면 다시 읽은 뒤 도장을 찍는다

# 백업을 건드렸으면
npm run check:backup-crypto    # 53개 — KAT(RFC 5869·XChaCha) + 봉투·매니페스트·전체 경로 + 배선(소스)
npm run check:i18n-roundtrip   # 54개 — 25개 스크립트의 UTF-8·매니페스트 왕복

# 구독·AI를 건드렸으면
npm run check:subscription     # 36개 — 체험 기간 계산(§13⑥ 고지의 근거) + **스토어 상태 전이**
                               #   + **유예 배너의 근거**(2026-09-04 추가 5개) — `revoke`가 만료 시각까지
                               #     지워서 삭제 예정일 배너가 첫 refresh 한 번에 사라졌다
                               #   ⚠ 전이 검사는 2026-08-19에 생겼다. 그전엔 0개였다(MONETIZATION §6.1.7 #2)
npm run check:timezone         # 252개 — 지역 7 × 단언 36. 기간 계산이 기기 시간대에 흔들리지 않는가
                               #   🔴 자식이 **몇 개를 썼는지**를 보고하고 부모가 지역마다 같은지 대조한다 —
                               #      그전엔 **지역 수**를 그대로 개수로 찍어 단언을 전부 지워도 초록불이었다(2026-09-02)
                               #   같은 단언을 TZ별 자식 프로세스로 돌린다(TZ는 시작 시점에만 읽힌다)
                               #   ⚠ 한국(UTC+9)에서는 이 버그가 절대 안 보인다 — 미주에서만 깨졌다
npm run check:ai               # 119개 — ISO 주차·주차↔날짜범위 왕복·프롬프트·인젝션 방어·스키마·동의
                               #   + 백필 지평·하위 완비(15개)
                               #   ⚠ 순수 계층이다. 프롬프트가 **실제로 어떤 글을 쓰는지**는 못 본다
                               #      → 그건 verify:hierarchy (아래, 돈이 나간다)

# 로그인·연령을 건드렸으면
npm run check:age-gate        # 148개 — 지역 38종 × 출생연도 125년 = 4,750조합 **전수**
                              #   🔴 막는 것 둘: ① 판정식이 관대식으로 되돌아가는 것(= 위법)
                              #      ② 지역표에서 나라가 빠지는 것(EEA 32개를 눈으로는 못 센다)
                              #   ⚠ 순수 계층이다. 화면이 실제로 뜨는지는 실기기에서만 안다
                              #   §⑥ 만 소스를 읽는다 — 하트비트가 세션을 만들거나(= 게이트 우회),
                              #      호출처가 둘이 되는 것(= 매일 2중 계상)을 막는다

# 알림을 건드렸으면
npm run check:notification     # 12개 — 예약 날짜 계산(이미 쓴 날 제외·지난 시각 제외·달/해 경계)
                               #   ⚠ 순수 계층만이다. 실제 발사·권한·재부팅 생존은 실기기에서만 안다

# 의존성이나 폰트를 바꿨으면
npm run licenses:build         # 고지 목록 재생성 (dependencies 를 더했으면)
npm run check:licenses         # 41개 — 패키지 38 · 폰트 · 화면 · 설정 입구
                               #   🔴 고지의 유일한 실패 방식은 **조용히 낡는 것**이다
                               #      화면은 멀쩡히 뜨는데 목록만 빠지고, 그게 라이선스 위반이다

# 본문 서식·블록을 건드렸으면
npm run check:diary-format     # 42개 — 저장 형태(기본값 미저장)·**서식이 다르면 병합 금지**·
                               #   문단 분할·커서 이동·읽기 시 모르는 값 세척
                               #   ⚠ 병합 검사가 핵심이다. 이게 없으면 화면에서는 멀쩡히 보이다가
                               #     저장하고 다시 열었을 때 서식이 사라진다

# 릴리스 AAB를 굽기 전 (로컬·EAS 둘 다)
eval "$(node scripts/release-env.mjs production)"   # 프로필 env를 셸로. 손으로 적지 않는다
npm run check:release-env      # 금지 플래그 + **필수 값 4개** + eas.json store 프로필
                               #   ⚠ **자기가 도는 셸만 본다.** 구운 셸이 다르면 못 잡는다
npm run check:release-bundle   # 🔴 구운 AAB **안을** 연다 — 프로필 값이 번들에 박혔는가
                               #   빌드 직후 반드시. 2026-08-25에 v9 사고가 그대로 재현됐다
                               #   (env 게이트는 통과했는데 다른 셸에서 구워 전부 빠졌다)
                               #   ⚠ v8·v9가 필수 값 3개를 빠뜨린 채 나갔다(MONETIZATION §6.1.4)
                               #   광고는 막지 않고 어느 단위로 나가는지만 찍는다

# R8(난독화) 설정을 건드렸으면 — 🔴 **릴리스 APK 로만 검증된다**
bash scripts/release/build-release-aab.sh    # AAB. E2E 용 APK 는 assembleRelease 로 따로 굽는다
#   정본 절차: C:/project/common/R8_OBFUSCATION.md §5
#   🔴 디버그 빌드는 R8 을 안 탄다 — "돌아간다"는 확인이 성립하지 않는다
#   🔴 파손은 크래시가 아니라 **조용한 기능 실종**(모듈 등록 실패)이라 로그로는 안 보인다
#      → 릴리스는 __DEV__ 가 false 라 로그가 안 찍힌다. **shared_prefs 실물**을 본다
#   🔴 ~~조각은 OTA 가 없다~~ → **있다**(vc21 · 2026-09-09). 정본 §5 의 expo-updates 줄이
#      **이제 그대로 해당된다** — R8 이 그걸 죽이면 다음 OTA 로 고칠 수단이 사라진다.
#      `shared_prefs/dev.expo.EASSharedPreferences.xml` 실물을 본다(vc21 E2E 에서 통과)
#   ⚠ 되돌릴 수 없는 자산은 그것과 **별개로** 본다: **SecureStore 백업 비밀 · expo-sqlite**
#      (틀리면 OTA 로도 심사로도 못 되돌린다 — 사용자 데이터다)
#   에뮬: AVD `diary` · 포트 **5568** · D:\emulators\diary (common/DEV_ALLOCATION.md §3)
#      ⚠ 콜드 부팅 실측 **425초**. 재시작이 잦으면 켜 두고 진행한다(common/EMULATOR_POOL.md §1)

# OTA(expo-updates)를 발행하려면 — 🔴 손으로 `eas update` 를 치지 않는다
bash scripts/release/publish-ota.sh "<무엇을 고쳤나>"
#   🔴 **`eas update` 는 `.env.local` 을 치우지 않는다.** 릴리스 AAB 스크립트와 다른 점이고,
#      2026-09-09 첫 발행 번들에 `http://10.0.2.2:3200`(개발 백업 서버)이 박힌 채 나갔다.
#      프로덕션 주소는 **0건**이었다. 발행 자체는 성공한다 — 조용한 실패다.
#      그 업데이트는 `eas update:delete` 로 지우고 이 스크립트를 만들었다.
#   스크립트가 하는 것: .env.local 치움 → 프로필 env → check:release-env →
#      변환 캐시 제거(파일만 치우면 캐시가 옛 값을 들고 있다) → 발행 → check:ota-bundle
npm run check:ota-bundle
#   🔴 **구운 번들 안을 연다.** `check:release-bundle` 은 AAB 를 보므로 OTA 를 못 본다.
#   🔴 Hermes 번들에 `strings` 를 쓰지 않는다 — 문자열 테이블을 못 읽어 **대조군까지 0** 이 나온다.
#      2026-09-09 에 실제로 그렇게 읽고 "깨끗하다"고 오판했다. 바이트로 찾고 UTF-16LE 도 함께 본다.
#   🔴 **`--platform android` 를 반드시 준다.** 기본값이 `all` 이라 **web 번들링에서 죽는다** —
#      react-native-google-mobile-ads 가 네이티브 전용 모듈을 import 해서다(2026-09-09 실측).
#      조각은 Android 전용이라 web 을 만들 이유가 없다.
#   🔴 **브랜치와 채널은 다르다. 브랜치만 만들면 앱에 안 닿는다.**
#      앱은 `expo-channel-name: production` 으로 요청하는데 `eas update` 는 **브랜치만** 만든다.
#      `eas channel:create production` 을 한 번 해서 채널→브랜치를 이어야 받는다(2026-09-09 실측:
#      채널 0개인 상태에서 앱 로그가 `Remote update request not successful` 이었다).
#   🔴 ① **DB 마이그레이션을 OTA 로 보내지 않는다.** `migrate()` 는 `current >= LATEST` 면
#      조용히 통과한다 — 롤백하면 JS 만 되돌아가고 `user_version` 은 앞선 채 남고,
#      `manifest-builder.ts` 가 원본 행을 직접 읽으므로 **백업에서 그 컬럼이 조용히 빠진다**
#      (db/migrations.ts 상단 주석이 경고하는 그 유실). 스키마 변경은 **스토어 릴리스로만**
#   🔴 ② **서버를 먼저 배포하고 그다음 OTA.** features/ai/{types,prompt,period}.ts 는
#      server/shared/ 로 복사돼 서버가 쓴다 — 순서가 뒤집히면 앱이 서버가 모르는
#      PROMPT_VERSION 을 보낸다
#   ⚠ 법무 문안(features/legal/legal-text.ts)도 JS 라 OTA 로 바뀐다 — **릴리스급으로 다룬다**
#   ⚠ 적용은 **다음 실행부터**다(LAUNCH_WAIT_MS=0). 1회차는 내장 번들이 돈다
#   ⚠ 채널: 프로덕션은 로컬 빌드라 app.json 의 requestHeaders 가 유일한 경로다.
#      eas build 로 굽는 internal·stg 는 eas.json 의 channel 이 덮는다(⏭ 우선순위 미확인)

# 운영 콘솔을 건드렸으면
npm run check:admin            # 34개 — **fail-closed** · 헤더 판정 · KST 집계 창 · 원가 추정
                               #   + **subject 경계**(2026-09-04 추가 5개) — 조회 조건으로는 쓰고
                               #     응답에는 끝까지 안 넣는다. 소스를 읽어 지킨다

# API 키가 있을 때만 (실제 과금이 발생한다)
cd server && npm run measure:ai   # P1 — 한국어 토큰·모델 비교·effort 스윕·refusal

# 프롬프트를 고쳤으면 — 계층 요약 실호출 (6회 · 약 ₩6)
#   AUTH_STUB=1 npm run dev 를 먼저 띄운다. AI_SPEND=1 없이는 돌지 않는다
cd server && AI_SPEND=1 npm run verify:hierarchy
#   주간 4(더미 일기) → 월간(그 4개의 진짜 요약문) → 연간(합성 월간 12).
#   prompt_ver 기대값을 **소스에서 읽어** 대조하고, 끝나면 ai/purge 로 stg 를 치운다
#   🔴 순수 계층 검사가 못 보는 것을 본다 — v6 이 "이번 주"·"올해"라고 쓰던 것을 여기서 잡았다

# 검사를 늘렸으면 / 릴리스 전 (검사 6개를 실제로 돌려 2~3분)
npm run check:doc-counts       # 문서에 박힌 "N개"가 실제와 같은가
                               #   ⚠ 개수가 스크립트와 문서 두 곳에 산다. 2026-08-20에 8군데가 낡아 있었다
                               #   🔴 e2e·e2e:ai 는 DB·dev 서버가 필요해 못 돌린다 → **선언된 검사 수를 센다**
                               #      (2026-08-24 추가). 안 보던 사이 e2e 9→32 · e2e:ai 7→15 로 낡아 있었다

# 새 런타임 의존성이 들어가는 커밋 / AAB 굽기 직전에만 (콜드 수 분)
npm run check:bundle           # 번들 상한. 기준선 3.67MB

npm run format                 # prettier --write (문서 *.md는 제외)
```

### 🔴 이 검사들이 **구조적으로 못 보는 것** — 두 곳을 대조하지 않는다 (2026-09-07)

공통서버가 자기 가드 70개에서 같은 구멍을 찾아 알려줬고(*목록은 `[_dv_public]`를 거르는데
KPI는 안 거른다 — 두 검사 다 통과한다*), **조각도 같은 모양인지 실제로 세어봤다.**

| 우리 대조형 검사가 하는 일 | 어디 |
|---|---|
| **왕복** — 한 계산과 그 역함수를 맞춘다 | `check:i18n-roundtrip`(UTF-8·매니페스트) · `check:backup-crypto`(KAT) |
| **환경 간** — 같은 계산을 지역·언어별로 돌려 같은지 본다 | `check:timezone`(7지역) · `check:i18n`(15언어) |
| 🔴 **경로 간** — 두 코드 경로가 같은 사실에 같은 답을 내는가 | **1개**(2026-09-07 신설) — `verify:regenerate`의 *라우트 집계 == 원장 독립 집계* |
| **문서 대조** — 문서에 박힌 수와 실제 수 | `check:doc-counts` |

🔴 2026-09-07까지 세 번째 칸이 **0개**였다. 앞의 둘로는 원리적으로 못 잡는다 — 공통서버가 정리해 준 한 문장이 이유를 다 설명한다:

> **앞의 둘은 한 계산을 여러 입력으로 돌리고, 빠진 셋째는 두 계산을 한 입력으로 돌린다.**

그래서 2026-09-07의 원가 누락(`AI_REPORT_SYSTEM` §6.6.1)이 `verify:regenerate` 11개와
`check:admin` 30개를 **둘 다 통과한 채로** 살아 있었다 — 각자 자기 화면만 봤기 때문이다.
⚠ 이름을 공통서버와 **같게 맞췄다**(왕복 / 환경 간 / 경로 간) — 두 레포가 같은 결함을
  다른 말로 부르면 다음에 같은 것인 줄 못 알아본다.

⚠ **개수 축으로는 안 잡힌다.** `check:doc-counts`가 세는 것은 *검사가 몇 개인가*이고,
허위 오라클(파괴 테스트)이 잡는 것은 *그 검사가 아무것도 안 보는가*다. 이건 셋 다 아니다 —
**검사는 진짜고 각자 옳은데, 둘을 이어 보는 사람이 없다.**

✅ **그 결함을 고치면서 첫 경로 간 검사를 세웠다**(같은 날). 옛 코드로 되돌리면 이렇게 운다 —
`라우트 1/0/0 vs 원장 2/3000/300`. **호출 2회·3,300토큰이 1회·0토큰으로 보이던 것**이다.
⚠ 하나가 생겼을 뿐이다. 나머지 자리는 여전히 비어 있다.

⚠ 그리고 이 목록에 `check:timezone`이 있는 것이 우연이 아니다 — 그 가드는 2026-09-02에
**자기가 지역 수를 단언 수로 세고 있어서** 단언을 전부 지워도 초록불이었다. 대조를 하려면
**대조하는 쪽도 대조당해야 한다.**

**가져온 규약**(공통서버 `CLAUDE.md`): *뱃지·대시보드·기본 필터는 같은 정의를 쓴다.*
🔴 다만 그쪽이 덧붙인 경고를 함께 적는다 — **그 규약은 스스로 지켜지지 않는다.**
그쪽은 그 문장을 적어놓고도 깨진 채였다. **적어두는 것과 대조하는 검사를 두는 것은 별개의 일이다.**

---

### 조각 서버 (`server/`)

```bash
cd server
npx supabase start   # Postgres :54422 · Storage :54421 · Studio :54423 (전부 Docker)
npm run db:push
npm run dev          # :3200
npm run e2e          # 32개 — reserve→서명 URL PUT→commit→latest→다운로드
npm run e2e:ai       # 15개 — AI 게이트(인가·구독·빈입력·과대입력·캡·지평) + **탈퇴 파기 4개**.
                     #   모델을 부르지 않는다
npm run measure:ai   # ⚠ 실제 과금. OPENAI_API_KEY 필요
```

#### 🔴 `.env.local` 이 **운영 DB** 를 가리킨다 — 가드를 세웠다 (2026-09-07)

위 절차는 **로컬 도커 스택**을 전제로 적혀 있는데, `server/.env.local` 의 `DATABASE_URL` 은
2026-09-04 운영 프로젝트 신설 검증 때 그쪽을 향해 두고 **안 되돌렸다.** 그래서 그때까지
`npm run db:push` 한 번이면 **사용자가 쓰는 스키마가 바뀌고**, `e2e:ai`·`verify:*` 가 운영 DB에 썼다.

⚠ **아는 사람이 있었는데 막지는 않고 있었다** — `verify-hierarchy.mjs` 주석이
*"`DATABASE_URL` 이 원격을 가리켜서 이 스크립트가 만든 행은 **테스터가 쓰는 DB** 에 남는다"* 고
경고만 했다. **주석은 다음 사람을 막지 못한다.**

```bash
# 원격을 향하면 넷 다 죽는다 — db:push · e2e:ai · verify:regenerate · verify:hierarchy
npm run db:push
#   db:push 가 원격 DB를 향한다 — aws-0-ap-northeast-2.pooler…

# 평소 (로컬)
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54422/postgres" npm run db:push

# 정말 운영에 해야 할 때만 — 배포 시 스키마 적용 등
ALLOW_REMOTE_DB=1 npm run db:push
```

🚫 **로컬로 폴백하지 않는다.** `drizzle.config.ts` 가 남긴 교훈이다 — 엉뚱한 DB를 보고
*"변경 없음"* 이라고 말하는 것이 가장 나쁜 실패다. 원격이면 **소리 내어 죽고** 손을 한 번 더 쓰게 한다.
⚠ 가드는 `.env.local` 을 **안 고친다** — 그 파일이 무엇을 가리켜야 하는지는 별개의 결정이다.
🔴 **기준은 "원격인가"가 아니라 "원격에 쓰는가"다.** `peek-reports.mjs`(읽기 전용)에는 일부러
안 걸었다 — 운영 리포트를 여는 것이 그 도구의 존재 이유이고(문의 답변), 묶으면 **CS 경로가 막힌다.**

⚠ 기본 포트(5432x)를 다른 프로젝트의 Supabase 스택이 쓰고 있어 **544xx로 옮겼다.**
자세한 것은 [`BACKUP_SYSTEM.md`](./BACKUP_SYSTEM.md) §7.

### ⚠ Expo Go를 떠났다 (2026-08-09)

광고 SDK가 네이티브 모듈이라 **Expo Go에서 돌지 않는다.** 이제 dev build로 개발한다.

```bash
npx expo run:android --port 8084   # 최초 1회: prebuild + gradle 빌드 + 설치 (수 분)
npm start                          # 이후에는 Metro만 (앱은 설치된 dev client)
```

- `android/`·`ios/`는 **CNG 산출물이라 커밋하지 않는다.** 새 클론에서는 위 명령이 알아서 만든다.
- 네이티브 의존성(`app.json` plugins, 네이티브 패키지)을 건드리면 **다시 빌드**해야 한다.
  JS만 고쳤으면 Metro 리로드로 충분하다.
- 빌드 환경: `JAVA_HOME`은 JDK 21, `ANDROID_HOME`은 `%LOCALAPPDATA%\Android\Sdk`.

🔴 **실기기가 붙어 있으면 `expo run:android`·`expo start --android` 를 쓰지 않는다** (2026-09-08,
volleyball 세션이 `EMULATOR_POOL.md` §2.1 에 남긴 실측). **Expo CLI 가 대상을 알아서 고르는데
실기기를 우선할 수 있고, `ANDROID_SERIAL` 을 줘도 무시한다.** 조각은 무선 디버깅으로 실기기를
붙여 두는 일이 잦아 그대로 물리는 자리다.

⚠ 조각에는 **우연한 안전망이 하나 있다** — 실기기의 `com.son0925.jogak` 은 **Play 서명**이라
debug 서명 빌드는 설치가 거부된다(`CLAUDE.md` §12 2026-08-24). 그래서 조용히 남의 폰에 깔리지는
않는다. 🔴 **그러나 그건 실패할 뿐이고, 실패 원인을 서명이 아니라 빌드 문제로 오해하기 쉽다.**

→ **에뮬레이터에 넣을 때는 대상을 손으로 고정한다**(2026-09-08 이 절차로 검증했다):

```bash
S=emulator-5568                                      # 조각 고정 포트(아래 참조)
adb -s $S install -r android/app/build/outputs/apk/debug/app-debug.apk
adb -s $S reverse tcp:8081 tcp:8081
EXPO_OFFLINE=1 CI=1 npx expo start --port 8081       # --android 를 붙이지 않는다
adb -s $S shell monkey -p com.son0925.jogak -c android.intent.category.LAUNCHER 1
```

🔴 **에뮬레이터 정책이 2026-09-08 에 또 바뀌었다 — 정본은
[`common/EMULATOR_POOL.md`](file:///C:/project/common/EMULATOR_POOL.md) 이고 여기에 베끼지 않는다.**

```
~~프로젝트별 AVD~~ → ~~공용 2대(common_1·common_2) + 클레임~~ → 프로젝트별 AVD(외장 D:)
   ~8/27              8/27 ~ 9/8                                 9/8 ~
```

| 조각 몫 | 값 |
|---|---|
| AVD | **`diary`** (2026-09-03 에 지웠으므로 **다시 만들어야 한다**) |
| 포트 | **5568** → serial `emulator-5568` (`DEV_ALLOCATION.md` §3) |
| 저장 위치 | `D:\emulators\diary` — `ANDROID_AVD_HOME` 로 지정한다 |

⚠ **외장은 7.8배 느리다**(콜드 부팅 259초 · 실측). 화면을 4~5번 확인하는 작업이면
**에뮬을 켜 둔 채 Metro 만 재시작**한다 — 2분짜리가 20분이 된다.
⚠ 이 절의 절차는 2026-09-08 에 **공용 `common_1`(5580)** 에서 돌린 것이다. 명령 자체는 같고
**serial 만 바뀐다.**

⚠ `EXPO_OFFLINE=1` 이 없으면 `expo start` 가 **원격 버전 조회 `fetch failed`** 로 죽는다(실측).
⚠ Git Bash 는 `/sdcard/...` 를 Windows 경로로 바꾼다 — `adb push`·`shell` 에는 `MSYS_NO_PATHCONV=1`.

> ⚠ **Kotlin 메타데이터 충돌**(2026-08-09 겪음): `react-native-google-mobile-ads` 16.4.0이 끌어오는
> `play-services-ads 25.4.0`은 Kotlin 2.3.0으로 컴파일돼 있고 Expo SDK 54는 2.1.20을 쓴다 →
> `compileDebugKotlin` 실패. **16.0.0으로 고정**(ads sdk 24.6.0)해서 해결했다.
> 이 라이브러리를 올릴 때는 pinned ads sdk의 Kotlin 버전을 먼저 확인한다.

> 실기기 접속이 안 되면 ① 폰이 Wi-Fi인지 ② Windows 방화벽 인바운드 ③ 포트를 잡고 있는 기존 expo
> 프로세스 순으로 확인한다(2026-08-07 실제로 셋 다 겪음).

---

## 4. 아키텍처 원칙

- 의존 방향: `app/`(라우트) → `features/` → `db/`·`lib/`·`store/`·`types/`. 역방향 import 금지.
- **일기의 진실은 기기 로컬**이다. 서버는 **읽을 수 없는** 백업 사본을 갖는다. 서버가 죽어도 앱은 동작해야 한다.
- **서버는 일기를 저장하지 않는다.** 백업은 암호문, AI 프록시는 무저장 — "저장하지 않습니다"(O) / "볼 수 없습니다"(X).
- **공통 기능은 common_server, 조각 도메인은 조각 서버.** 상세는 [`ARCHITECTURE.md`](./ARCHITECTURE.md).
- **신원은 common_server가 단일 진실.** 조각은 자체 로그인을 만들지 않는다.
- 공통 UI는 `components/`에만. Atomic Design 쓰지 않는다.
- `any` 금지, `strict` 유지.
- `server/`는 Metro 번들 대상이 아니다(`metro.config.js` blockList).
