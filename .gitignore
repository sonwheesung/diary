# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# CNG 네이티브 산출물(로컬 빌드용 — prebuild로 재생성)
/android
/ios

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# typescript
*.tsbuildinfo

# 환경변수(dev/prod 분리 — 절대 커밋 금지)
.env
.env.*
!.env.example

# 로컬 서명 자격(절대 커밋 금지)
credentials.json
credentials/

# 로컬 빌드 산출물
dist-builds/

.vercel
.gstack/

# EAS 빌드 산출물(AAB/APK). 용량이 크고 재생성 가능하다
/build

# 공개 저장소라 막는 쪽으로 넓힌다 (2026-09-09)
# 새는 것은 0건이었고, 없는 것은 가드였다
secrets/
*service-account*.json
*.pem
id_rsa*
*.aab
*.apk
