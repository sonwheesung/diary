# 조각 — 설계 정본

> 이 문서는 Claude Code가 개발을 진행하기 위한 단일 기준 문서다.
> 모호하면 이 문서의 "설계 원칙"을 우선한다. **새 결정은 코드보다 먼저 이 문서에 반영한 뒤** 진행한다.
> 서버 경계·신원 흐름은 [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md), 색인은 [`docs/README.md`](./docs/README.md),
> 문서 규율은 [`docs/DOC_DISCIPLINE.md`](./docs/DOC_DISCIPLINE.md).

---

## 1. 한 줄 요약

하루를 **하나의 조각**으로 기록하는 감성 일기 앱. 일기는 **기기에 저장**되고, 월 구독자에게
**암호화 클라우드 백업/복원**과 **AI 요약 리포트**, **광고 제거**를 제공한다.

**서버는 일기를 저장하지 않는다.** 백업은 기기에서 암호화되어 올라가고(우리는 복호화 키가 없다),
AI 리포트는 앱이 로컬 평문을 서버 프록시로 흘려보내 만들며 서버는 그것을 저장하지 않는다.

---

## 2. 핵심 기둥 (설계 충돌 시 판단 기준)

1. **쓰기 마찰 최소화** — 오늘의 조각 하나를 남기는 데 걸리는 손동작이 최소여야 한다.
   감정 선택은 탭 한 번, 홈에서 작성까지 1탭. **로그인 없이도 일기를 쓸 수 있어야 한다.**
2. **감성적 미니멀** — Apple 수준의 여백. 크림 + 하늘 + 베이지. Glass 금지, 과한 Gradient 금지,
   Shadow 최소. 화면이 조용해야 글이 보인다.
3. **내 기록은 내 기기에** — 일기 본문의 진실은 **기기 로컬(expo-sqlite)** 이다. 서버는 백업 사본을 갖되
   **그 사본을 읽지 못한다**(§5.1). 앱 잠금(PIN·패턴·생체)은 **무료** 기능이다 — 잠금은 유료 볼모가 아니다.
   데이터 반출은 **유료 백업/복원으로만** 제공한다(2026-08-08 사용자 결정).
4. **광고가 글쓰기를 막지 않는다** — 광고는 **저장이 끝난 뒤에만** 뜬다. 쓰러 들어가는 길은 절대 막지 않는다.

---

## 3. MVP 범위 (2026-08-07 확대 결정)

~~MVP = 일기 CRUD만. 월결제·AI는 전부 나중.~~
→ **정정(2026-08-07, 사용자 결정)**: 일기 CRUD는 테스트까지 1일이면 충분하다는 판단에 따라
**월 구독과 AI 기능까지 MVP 목표에 포함**한다.

| # | 기능 | 저장/의존 | 비고 |
|---|---|---|---|
| 1 | Splash | — | |
| 2 | Home | 로컬 | 오늘 날짜 · 조각 작성 버튼 · 최근 조각 · 연속 작성일 |
| 3 | Write | 로컬 | 제목(선택) · **본문(중간에 이미지 삽입)** · **태그** · 감정 선택. **저장 완료 후 광고** |
| 4 | Detail | 로컬 | 조회 · 수정 · 삭제 |
| 5 | Calendar | 로컬 | 작성 날짜 표시 |
| 6 | Search | 로컬 | 제목·내용 검색. ⚠ **탭이 아니다**(2026-08-12) — 홈·모든 조각의 돋보기가 입구 |
| 7 | Settings | 로컬 | 다크모드 · 잠금 설정 · **기록 리마인더**(로컬 알림 — ~~화면만~~ 2026-08-21 구현, [`docs/NOTIFICATION_SYSTEM.md`](./docs/NOTIFICATION_SYSTEM.md)) |
| 7-1 | **앱 잠금** | 로컬 | 진입 시 잠금. **PIN · 패턴(3×3) · 생체인증** 중 선택(§7.1) |
| 8 | 공지사항 | common_server | 인터넷 필수. bootstrap 응답 |
| 9 | 문의하기 | common_server | 인터넷 필수. **로그인 필수** — 로그인의 이유가 **답변**이므로 내역·답변 화면이 같은 기능의 절반이다(2026-08-13) |
| 10 | 구글 로그인 | common_server | **선택적** — 안 해도 일기는 쓴다 |
| 11 | 광고 | AdMob | 저장 완료 후 전면광고 + 빈도 캡 |
| 12 | 월 구독 | common_server + 스토어 | **단일 구독 `조각 Pro`** — 광고 제거 + 백업/복원 + AI 리포트(§7.2) |
| 13 | 백업/복원 | 조각 서버 | 구독자 전용. **클라이언트 암호화(E2EE)** — 텍스트 1차 ✅ / 사진 2차 ❌ ([`docs/BACKUP_SYSTEM.md`](./docs/BACKUP_SYSTEM.md)) |
| 14 | AI 요약 리포트 | 조각 서버(프록시) | 구독자 전용. **주간 먼저**, 단가 보고 월간 → 연간. **앱 화면 ✅ / 서버 ❌** ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md)) |

**범위 밖**: 기기간 실시간 동기화(양방향 머지)는 백업/복원과 다른 문제다 — MVP는 **백업/복원까지만** 한다.

---

## 4. 로그인 정책 (2026-08-07 결정 · 2026-08-08 재확인)

**기본은 선택적, 특정 기능에서만 필수.**

| 상황 | 로그인 |
|---|---|
| 일기 CRUD · 캘린더 · 검색 · 설정 · 공지 읽기 | 불필요 |
| 문의하기 | **필수** |
| 월 구독 결제 · 백업/복원 · AI 리포트 | **필수** |

- 로그인은 구글 소셜 로그인 하나로 간다.
- 필수 지점에서 로그인 화면을 띄우되, **일기 진입 경로에는 절대 세우지 않는다**(기둥 1).
- ⚠ common_server v1의 문의는 **익명 단방향**(신원 없음)으로 설계돼 있다. 조각의 "문의 로그인 필수"는
  common_server **Phase 7(subjects + 토큰 발급)** 이 선행돼야 성립한다 — `docs/ARCHITECTURE.md` §4.
- **구현 현황(2026-08-09)**: 공지·문의 화면과 서버 전송, **구글 로그인·로그아웃·탈퇴**까지 붙었다
  (`docs/SUPPORT_SYSTEM.md`). 세션 판정과 구글 SDK 호출은 `features/support/auth-gate.ts`
  **한 파일**이 갖고, 화면은 상태만 보고 갈라진다.
  ~~⚠ **서버에 `jogak`이 등록되기 전에는 로그인이 성공할 수 없다** — `bootstrap?app=jogak`이 아직 404다.~~
  → **해소(2026-08-11 실측)**: `bootstrap?app=jogak`이 **200**이다. 앱 등록이 끝나 로그인 경로가 열렸다.
- **탈퇴는 선택이 아니다.** 계정을 만드는 앱은 Play 정책상 앱 안에 삭제 경로가 있어야 한다.
  로그인을 붙이는 커밋에 탈퇴를 같이 넣는다 — 나중에 붙이면 심사에서 막힌다.

### 한 번 뒤집었다가 되돌린 기록 (2026-08-08)

"첫 실행부터 로그인 필수 + 잠금 분실 시 이메일 인증 초기화"로 갔다가 **같은 날 되돌렸다.**
다시 꺼낼 때를 위해 그때 확인한 사실만 남긴다.

| 확인한 것 | 내용 |
|---|---|
| 이메일 인증은 **되찾기가 아니다** | 일기는 기기에 있고 잠금은 UI 게이트다. 인증을 통과해도 할 수 있는 건 초기화뿐이다. 잊어도 안 사라지게 하는 건 **백업뿐** |
| **오프라인 첫 진입**이 막힌다 | 세션이 없는 상태로 네트워크가 없으면 앱이 안 열린다. 비행기·지하철에서 일기를 못 쓴다 |
| 구글 로그인은 **Expo Go에서 안 된다** | Expo 공식 문서: *"These libraries can't be used in Expo Go because they require custom native code."* → dev build 전환이 선행 |

⏭ 구글 로그인 자체는 **문의·구독 시점에 어차피 필요**하다. 그때 dev build 전환과
Google Cloud OAuth 클라이언트 발급을 함께 한다(§13).

---

## 5. 서버 경계 (핵심 — 어기면 되돌리기 비싸다)

| | common_server | 조각 서버 |
|---|---|---|
| 위치 | `C:\project\common_server` (이미 배포됨) | 미생성 |
| 성격 | **앱에 무관한 공통 기능**. 1배포 N앱 | **조각 도메인 전용** |
| 담당 | 공지 · 문의 · 신원(로그인·subject) · 엔타이틀먼트(구독·광고제거) | 일기 백업/복원 · AI 요약 리포트 |
| DB | Supabase `common-server` (공용) | Supabase 조각 전용(신규) |

**일기 본문과 AI 리포트를 common_server에 넣지 않는다.** 이유는 `docs/ARCHITECTURE.md` §2.
요약: ~~① common_server는 "개인정보 최소수집"이 명시적 설계 원칙이라 일기 본문이 들어가면 그 원칙이 무너진다~~
② 1배포 N앱이라 조각 스키마 변경이 my_word·배구 배포까지 흔든다 ③ AI는 LLM 비용·큐·긴 실행시간으로 성질이 다르다.

### 🔴 근거를 갈아끼운다 — 실제로 결정을 지탱하는 것은 **service_role 폭발 반경** (2026-08-11)

①은 **E2EE로 가면서 약해졌다.** 들어가는 것은 본문이 아니라 암호문이고 서버는 읽지 못한다 —
"개인정보 등급"만으로는 DB를 나누는 월 $9.8을 정당화하지 못한다. 이 상태로 두면 다음 사람이
*"암호문인데 왜 따로 두지?"* 로 뒤집는다. 그래서 진짜 이유를 적는다.

**조각 서버는 Storage 서명 URL을 만들려고 `service_role` 키를 쓴다**(`server/lib/storage.ts`).
이 키는 **프로젝트 전체의 RLS를 우회한다.** 같은 Supabase 프로젝트를 쓰면 그 키 하나가 열리는 것:

| 테이블 | 내용 |
|---|---|
| `subjects` | 이메일 · 구글 sub — **my_word·배구명가 사용자까지 전부** |
| `tickets` | 문의 본문 |
| `entitlements` · `purchase_events` | 구독 상태 · RC 웹훅 원문 |

→ **조각 서버 침해 = 전 앱 신원 유출.** 그리고 **스코프를 좁혀 피할 수 없다** —
`service_role`은 프로젝트 단위라 "조각 스키마만" 같은 제한이 안 걸리고, 서명 URL 발급에 그 키가 필요하다.
§13의 *"조각 서버에 ADMIN_TOKEN을 주면 조각 침해 = common_server 전권"* 과 **같은 문제**다. 토큰 대신 DB 키일 뿐.

**나중에 분할하는 길은 없다고 본다.** `vault_id`가 Storage 경로(`{vault_id}/manifests/…`)에 박혀 있어
프로젝트를 옮기려면 **모든 사용자의 암호문을 재업로드**하며 DB 행과 원자적으로 맞춰야 한다 —
"이용자 많아지면 분할"은 곧 "데이터가 가장 많을 때 가장 위험한 작업"이다.

### 배포 환경 — stg는 무료, 운영은 Pro (2026-08-11 사용자 결정 · 실측)

| 단계 | 어디에 | 비용 |
|---|---|---|
| 개발 | 로컬 Supabase 스택(도커) | $0 |
| **스테이징 · 클로즈드 테스트** | **Vivace Staging(Free) 조직에 신규 프로젝트** | **$0** |
| **운영** | **WheeSungSon(Pro) 조직에 프로젝트 추가** | **+약 $9.8/월** |

실측(2026-08-11 대시보드): Pro 조직은 프로젝트 4개(활성 2·정지 2)이고 컴퓨트 188시간=$2.53,
**저장 0/100GB · egress 0.018/250GB**로 쿼터가 거의 비어 있다. 무료 조직은 프로젝트 1개라 **자리가 하나 남는다**.

- **컴퓨트는 나눠 쓸 수 없다.** 프로젝트마다 전용 Postgres 서버라 추가하면 730시간 × $0.01344.
  정지된 프로젝트를 되살려도 같은 값이라 재활용 이득이 없다(정지 상태는 과금 0이므로 그냥 둬도 손해가 없다).
- **저장·전송 쿼터는 조직 단위라 그대로 나눠 쓴다.** 운영을 Pro 조직에 넣는 실질적 이유가 이것이다 —
  100GB가 비어 있어 사진 백업의 데이터 비용이 사실상 0이다.
- ⚠ **반드시 서울(`ap-northeast-2`)에 만든다.** 조직 간 이관은 **리전을 바꾸지 못한다**(공식 문서 확인).
  기존 `volleyball-stg`가 싱가포르인 걸 보면 기본값이 그쪽일 수 있으니 생성 시 명시적으로 고른다.
- ⚠ **무료 티어를 운영에 쓰지 않는다.** 7일 미사용 시 정지 · 500MB DB · 1GB 저장 · 5GB egress.
  폰을 잃고 2주 뒤 복원하려는 사람에게 서버가 자고 있으면 **백업의 존재 이유가 사라진다.**
- 새 Pro 조직을 파는 것은 최악이다 — $25 플랜 요금이 또 붙는다. **기존 조직에 넣으면 +$9.8**이다.

**신원만 공유한다.** common_server가 로그인의 단일 진실이고 서명 토큰을 발급한다. 조각 서버는 그 토큰을
**검증만** 하고 `subject_id`를 FK 없는 외래 식별자로 저장한다. 조각이 자체 로그인을 만들면 신원이 두 곳에
생겨 구독자 매칭이 깨진다 — 가장 비싼 실수다.

---

## 5.1 암호화와 AI 처리 경로 (2026-08-07 결정)

두 요구가 정면 충돌했다 — **백업은 암호화하고 싶고, AI 요약은 본문을 읽어야 한다.**
서버가 저장물을 읽는 대신, **앱이 로컬 평문을 그때만 흘려보내는** 구조로 푼다.

```
백업:      앱(로컬 평문) ──암호화──▶ 조각 서버 ──저장──▶ 암호문만 보관 (서버는 못 읽음)
AI 리포트: 앱(로컬 평문) ─────────▶ 조각 서버(프록시·무저장) ──▶ LLM ──▶ 리포트 ──▶ 앱 로컬 저장
```

| 항목 | 결정 |
|---|---|
| 백업 | 클라이언트 암호화(E2EE). 서버는 복호화 불가 |
| **암호화 키** | **기기 SecureStore + 사용자에게 복구 코드 1회 발급. 서버는 키를 갖지 않는다** |
| AI 리포트 | 앱이 로컬 평문을 조각 서버 프록시로 전송 → LLM. **서버 무저장** |
| 리포트 저장 | 로컬. 백업에 암호화되어 포함(서버에 남기지 않는다) |
| 생성 트리거 | 앱에서 사용자 트리거. **서버 배치 불가**(서버에 본문이 없다) |
| 월간·연간 | 하위 리포트를 입력으로 쓰는 **계층 요약**(원본 전량 재투입 금지 — 비용·품질) |
| 고지 문구 — **AI** | **"저장하지 않습니다"(O) / "볼 수 없습니다"(X)** — 본문이 평문으로 프록시를 지나간다 |
| 고지 문구 — **백업** | **"우리는 읽지 못합니다"(O) / "아무것도 저장하지 않습니다"(X)** — 암호문과 메타데이터는 **저장한다** |
| ⚠ 둘은 **정반대다** | 백업 = 저장하되 못 읽는다 / AI = 읽되 저장 안 한다. **한 화면에 섞지 않는다** — i18n 키도 `backup.notice.*` / `ai.notice.*`로 분리해 재사용을 막는다 |

### 반드시 지킬 것

1. **LLM API 키를 앱에 넣지 않는다.** 번들에 넣으면 즉시 추출된다. 반드시 조각 서버가 프록시한다.
   프록시라서 구독자 게이트·호출 캡·비용 통제가 가능해지는 이득도 같이 온다.
2. **표현을 흐리지 않는다.** 리포트 생성 순간 본문은 우리 서버 메모리를 지나간다.
   "우리는 절대 볼 수 없습니다"는 **거짓말**이다. "저장하지 않습니다"가 정확하다.
3. **백업을 켜지 않은 사용자의 일기는 서버로 단 한 글자도 가지 않는다.** 로컬 우선 설계의 실질적 가치가 여기다.
4. **AI 요약을 위해 서버가 본문을 읽는다는 사실을 구독 화면에 명시한다.** 법적 요구 이전에,
   나중에 알게 됐을 때 사용자가 가장 크게 배신감을 느끼는 지점이다.
5. **프록시는 본문을 로그에 남기지 않는다.** 에러 로깅·관측(Sentry 등)에 본문이 섞여 들어가는 사고가
   가장 흔한 유출 경로다. 프록시 경로는 요청 본문 로깅을 명시적으로 끈다.

### ⚠ 백업·AI가 붙는 릴리스에 **함께** 고쳐야 하는 것 (2026-08-09 기록)

지금(v3)은 일기가 기기 밖으로 나가지 않아서 **처리방침 §1과 Play 데이터 보안 선언 모두 "수집 안 함"** 이다.
백업이나 AI 리포트가 붙는 순간 그 진술이 **거짓이 된다.** 세 곳을 같은 릴리스에서 함께 고친다.

| 고칠 곳 | 무엇을 |
|---|---|
| ~~`features/legal/legal-text.ts` §1~~ | ✅ **완료(2026-08-23)** — 예고 둘을 본문으로 승격했다. §1 제목까지 바꿨고(`일기가 어디에 저장되는지 먼저 밝힙니다`), 15개 언어와 `DELETE_ACCOUNT`도 함께. ⚠ **v8·v9를 올린 뒤에 고쳤다** — 이 표가 막으려던 순서를 지키지 못했다 |
| Play **데이터 보안** 선언 | 일기 본문을 수집 항목에 추가(**사진은 제외** — E2EE 백업이 유일한 반출 경로라 예외에 걸린다). `User IDs`·`구매 내역`도 함께. **선택사항(opt-in)으로 표시** · `데이터 삭제 지원`을 **예**로. 명세는 [`docs/PLAY_DATA_SAFETY.md`](./docs/PLAY_DATA_SAFETY.md) |
| 앱 안 (백업 켜는 화면) | 무엇이 나가는지 그 자리에서 밝힌다 |

**순서가 중요하다.** Play는 배포되는 버전과 선언이 일치해야 한다 — 백업 코드가 담긴 AAB를
**올리기 전에** 선언을 고친다. 올린 뒤에 고치면 그 사이가 위반이다.

- **백업(암호문)보다 AI(평문)가 어렵다.** 백업은 "암호문이 나간다"로 설명이 끝나지만, AI는 본문이
  평문으로 우리 프록시를 지나간다. 여기서도 `"저장하지 않습니다"(O) / "볼 수 없습니다"(X)`(§5.1)를 지킨다.
- ~~⚠ Play에 "일시적으로만 처리되는 데이터"에 대한 예외가 있는 것으로 보이나 **적용 조건을 기억으로
  단정하지 않는다.** 착수 시점에 공식 문서로 확인한다(DOC_DISCIPLINE §6).~~
  → ✅ **확인 완료**(2026-08-23 실측 · 2026-08-24 재확인, `answer/10787469`).
  **조각의 AI 경로에는 적용되지 않는다** — ① 요약문 90일 저장 ② 벤더 30일 보관 ③ `ai_usage` 영구.
  🔴 그리고 예외의 뜻이 *"선언 안 함"* 이 아니었다 — 원문은 *"폼 응답에는 **포함해야 하고**
  기준을 만족하면 공개 섹션에 표시되지 않을 뿐"* 이다(폼의 `일시적으로 처리되나요?` 체크박스).
  ⚠ 같은 문서의 본문과 FAQ가 **서로 정반대**라 보수적 독법을 쓴다.
  반대로 **E2EE 예외는 백업에 그대로 적용**되어 `사진`을 선언하지 않는다.
  원문·판정은 [`docs/PLAY_DATA_SAFETY.md`](./docs/PLAY_DATA_SAFETY.md) §1.
- ~~**삭제 경로가 하나 더 필요하다.** 지금 탈퇴는 common_server의 신원만 지운다. 조각 서버에 백업이
  쌓이면 그 백업을 지우는 경로가 따로 있어야 하고, 데이터 보안 선언의 "삭제 요청 가능"이 그걸 전제한다.~~
  → ✅ **백업은 됐다**(2026-08-24 확인) — `deleteAccount()`가 `purgeBackup()`을 **먼저** 부르고
  실패하면 탈퇴 자체를 멈춘다. ~~🔴 그런데 AI에는 그 대칭이 없다~~
  → ✅ **AI도 됐다**(2026-08-24) — `POST /api/v1/ai/purge`가 `ai_reports`·`ai_usage`·`ai_cooldowns`를
  한 트랜잭션으로 지우고, `deleteAccount()`가 **백업 파기 뒤·`DELETE /auth/me` 앞**에서 부른다.
  순서가 규율이다 — 계정이 사라지면 **지울 권한이 있는 사람이 없어진다**
  ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §7.1 ·
  [`docs/PLAY_DATA_SAFETY.md`](./docs/PLAY_DATA_SAFETY.md) §4.4).
- **기존 사용자의 기대를 배신하지 않는다.** v3를 "기기에만 저장" 약속으로 깐 사람에게 스토어 카드가
  조용히 바뀌면 가장 크게 배신감을 느낀다. 그래서 opt-in 표시와 앱 내 고지가 형식이 아니라 실질이다.

상세·근거·미결정은 [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) §6.

---

## 6. 기술 스택 (2026-08-07 확정)

기준 프로젝트는 `C:\project\volleyball` — **버전 조합·구조·문서 방식·커밋 규칙을 그대로 승계**한다.

| 영역 | 선택 | 상태 |
|---|---|---|
| 앱 | Expo **SDK 54** · RN 0.81.5 · React 19.1.0 | ✅ 설치 |
| 언어 | TypeScript ~5.9.2 (`strict`) | ✅ `any` 금지 |
| 네비게이션 | expo-router ~6.0.24 | ✅ 설치 |
| 상태 | Zustand | ✅ 설치 |
| **로컬 DB** | **expo-sqlite** | ✅ 설치 — 검색·캘린더·연속일 집계에 쿼리가 필요(2026-08-07 결정) |
| 잠금 저장 | expo-secure-store | ✅ 설치 — PIN·패턴 해시, 암호화 키 보관 |
| ~~생체인증~~ | ~~expo-local-authentication~~ | 🚫 **제거(2026-08-10 결정 §7.1). 패키지도 미설치** — 이 줄이 "✅ 설치"로 남아 있었다(2026-08-11 정정) |
| 광고 | react-native-google-mobile-ads (AdMob) | ✅ 설치 — **네이티브 모듈이라 Expo Go 불가**. 이것 때문에 dev build로 전환(2026-08-09) |
| 개발 실행 | ~~Expo Go~~ → **dev build** (`npx expo run:android`) | 2026-08-09 전환. `android/`는 CNG 산출물이라 커밋 안 함 |
| 구독 | react-native-purchases (RevenueCat) | ❌ 미설치. common_server PLAN Phase 9와 짝 |
| 로그인 | @react-native-google-signin/google-signin | ✅ 16.1.4 — 문의하기에 연결(2026-08-09) |
| Server State | TanStack Query | ❌ 미설치 |
| 폼 | React Hook Form + Zod | ❌ 미설치 |
| 애니메이션 | Reanimated + Moti | ❌ 미설치 — SDK 54는 Reanimated 4 + worklets 설정 동반 |
| 날짜 | dayjs | ✅ 설치 |
| 다국어 | i18next · react-i18next · expo-localization | ✅ 적용 — **15개 언어** (§9.1) |
| 아이콘 | Lucide | ❌ 미설치 |
| 폰트 | Pretendard | ✅ 적용 — 정적 OTF 3종(Regular·Medium·SemiBold, 각 1.5MB) |
| 조각 서버 | Next.js(App Router) + Drizzle + Supabase Postgres · Vercel | ❌ **월 결제 착수 시 생성**(2026-08-07 결정) |
| 배포 | Expo EAS(앱) · Vercel(서버) · Supabase(DB) | ❌ 미설정 |

---

## 7. 광고 정책 (2026-08-07 결정)

### 지면과 시점 (2026-08-08 확정)

| 지면 | 어디에 | 언제 |
|---|---|---|
| **전면(이미지) 광고** | — | 조각 **등록(저장)이 완료된 순간**. **하루 1회만** |
| **하단 배너** | 홈 · 캘린더 · **리포트** · 설정 (**탭 화면만**) | 상시 |
| 광고 없음 | **작성 화면 · 상세 화면 · 검색**(2026-08-12 탭에서 강등) | — |

- **작성 화면에 배너를 두지 않는 이유**: 하단은 툴바 자리이고 키보드가 올라오면 자리 싸움이 난다.
- **상세 화면에 배너를 두지 않는 이유**: 일기를 **읽는 자리**다. 본문 아래 광고가 붙으면
  기둥 2("화면이 조용해야 글이 보인다")가 정면으로 무너진다.

### 지켜야 할 것

- 저장 자체는 광고와 무관하게 먼저 끝난다 (광고 로드 실패·오프라인이어도 저장은 성공해야 한다).
- **빈도 캡: 하루 1회.** 쿨다운은 하루 1회라 따로 두지 않는다.
- **구독자는 0회.**
- 작성 화면 **진입 시점에는 띄우지 않는다.** 기둥 1·4 위반이자 **AdMob 정책 위반 소지**다 —
  Google: *"Placing interstitial ads so that they suddenly appear when a user is focused on a task
  at hand (e.g. playing a game, **filling out a form**, reading content) may lead to accidental clicks."*
  작성 화면 진입은 정확히 "양식 작성에 집중하려는 순간"이다.
  저장 완료 시점은 반대로 정책이 권장하는 *logical break between content*에 해당한다.
- 광고 SDK 초기화 실패가 앱 사용을 막지 않는다.

### 구현 (2026-08-09)

| 것 | 어디 |
|---|---|
| SDK 초기화·캡·구독 게이트 | `features/ads/api/ads.ts` |
| 전면광고 | `features/ads/api/interstitial.ts` |
| 배너 | `features/ads/components/AdBanner.tsx` |
| 배너 자리 | `components/Screen`의 `footer` prop — **탭 화면만** 넘긴다 |

### AdMob 계정 (2026-08-09 등록)

| 것 | 값 |
|---|---|
| AdMob 앱 (Android) | `ca-app-pub-2731473780180274~6800808068` — `app.json` config plugin |
| 배너 `탭 하단 배너` | `ca-app-pub-2731473780180274/4105647957` |
| 전면 `저장 완료 전면` | `ca-app-pub-2731473780180274/5718112528` |

- **개발 빌드는 여전히 Google 테스트 단위를 쓴다**(`ads.ts`의 `__DEV__` 분기).
  실제 단위로 개발하면 내가 만든 노출·클릭이 무효 트래픽으로 잡혀 계정이 정지될 수 있다.
  이 분기를 없애지 않는다 — 실제 단위는 릴리스 빌드에서만 나간다.
- ⚠ 이 앱은 AdMob에서 **"검토 필요 · 광고 게재가 제한됨"** 상태다. 스토어에 없는 앱으로 만들었기
  때문이다. Play 출시 후 AdMob에서 **스토어 연결 → 승인**을 받아야 실제 광고가 나간다.
  그전까지는 릴리스 빌드에서도 노출이 0이다 — 코드 문제로 오해하지 않는다.
- ⚠ **iOS 앱 ID는 아직 Google 테스트 값이다**(`ca-app-pub-3940256099942544~1458002511`).
  AdMob에 iOS 앱을 만들지 않았다. iOS 착수 시 발급받아 `app.json`을 고친다.
- ⚠ `app.json`의 앱 ID는 **네이티브 매니페스트에 박힌다.** 바꾸면 `prebuild` + 재빌드가 필요하다
  (JS 리로드로는 반영되지 않는다).
- AdMob 대시보드의 **앱 이름("조각")은 내부 라벨**이라 스토어 이름과 무관하고 언제든 바꿀 수 있다.
  스토어에 낼 이름은 별도로 정한다(⚠ `조각일기`·`오늘조각`·`마음조각`·`조각케이크`가 이미 Play에 있다).
- 전면광고는 작성 화면 진입 시 **미리 로드**한다. 저장 직후 로드를 시작하면 몇 초 뒤에 뜨는데,
  늦게 뜨는 전면광고가 가장 짜증난다. 캡에 걸려 있으면 아예 부르지 않는다(트래픽 낭비).
- 띄우는 순서: **저장 → 화면 전환 → 광고.** 저장 앞에 두면 광고 실패가 저장을 막고,
  전환 앞에 두면 "저장됐나?" 싶은 채로 광고를 본다.
- **수정에는 띄우지 않는다.** '등록(저장)된 순간'만이다.
- 배너는 **키보드가 올라오면 숨긴다.** 입력창 위를 광고가 덮으면 최악이다.
- 배너를 못 받으면 **자리를 차지하지 않는다.** 빈 회색 띠가 남으면 고장 난 것처럼 보인다.
- 구독자 제외는 `adsEnabled()` 한 곳을 거친다 — 엔타이틀먼트가 생기면 그 함수만 바꾸면 된다.

⚠ **Expo Go를 떠났다.** `react-native-google-mobile-ads`는 네이티브 모듈이라 Expo Go에서 돌지 않는다.
2026-08-09부터 **dev build**(`npx expo run:android`)로 개발한다. `android/`·`ios/`는 CNG 산출물이라
커밋하지 않는다(`.gitignore`). 새 기기·새 클론에서는 `npx expo prebuild` 후 빌드가 필요하다.

⚠ **라이브러리 버전을 16.0.0으로 고정했다.** 16.4.0이 끌어오는 `play-services-ads 25.4.0`은
Kotlin 2.3.0으로 컴파일돼 있는데 Expo SDK 54는 2.1.20이라 `compileDebugKotlin`이 깨진다.
올릴 때는 **pinned ads sdk의 Kotlin 버전을 먼저 확인**한다.

---

## 7.1 앱 잠금 정책 (2026-08-07 결정)

일기 앱의 잠금은 부가 기능이 아니라 **신뢰의 핵심**이다. 무료 기능으로 간다(기둥 3).

| 항목 | 결정 |
|---|---|
| 방식 | **PIN · 패턴(3×3)** 중 사용자 선택. 잠금 자체를 끄는 것도 선택 |
| ~~생체인증~~ | 🚫 **제거**(2026-08-10 결정) — 아래 참조 |
| 저장 | PIN·패턴은 **절대 평문 저장 금지**. salt + 해시로 SecureStore에 보관 |
| 잠금 시점 | 앱 진입 시 + **백그라운드로 나가면 즉시**. 설정 없음(2026-08-10 결정) |
| 실패 처리 | 횟수 초과 시 **지연(backoff)**. 🚫 "N회 실패 시 데이터 삭제"는 MVP에서 하지 않는다 — 오작동 한 번에 일기가 사라진다 |
| 패턴 최소 길이 | 4점 이상 강제. 패턴은 PIN보다 추측 공간이 좁고 화면 얼룩(smudge)에 약하다 |
| 앱 스위처 가림 | **필수.** 잠금을 걸어도 최근 앱 미리보기에 일기가 보이면 무의미하다 |
| 비밀 변경·해제 | **현재 PIN·패턴을 먼저 확인**한다(2026-08-10 결정) — 아래 참조 |

### 생체인증 제거 · 지연 설정 제거 (2026-08-10 사용자 결정)

~~PIN·패턴·생체 중 선택, 지연은 즉시/1분/5분~~ → **PIN·패턴만, 지연 없음.**

고를 것을 줄인다. 일기 앱의 잠금은 하루 한두 번 지나는 문이라 4자리를 누르는 비용이 크지 않고,
선택지마다 실패 경로(센서 없음·등록 안 됨·인식 실패·지연 중 이탈)가 붙어 코드와 설명이 함께 늘어난다.

⚠ **"즉시"를 글자 그대로 구현하면 앱을 못 쓴다.** 두 가지를 함께 지켜야 한다:

| 지킬 것 | 왜 |
|---|---|
| `inactive`는 이탈로 보지 않는다. **`background`만** | iOS는 알림창·제어센터·전화 배너·앱 스위처 훑기에도 `inactive`가 뜬다. 이걸 이탈로 세면 알림만 내려도 잠긴다 |
| **앱이 부른 외출은 예외** (`features/lock/excursion.ts`) | 사진 선택·구글 로그인은 우리가 띄운 화면이다. 여기서 잠그면 사진을 넣을 때마다, 로그인할 때마다 잠금 화면을 만난다 |

외출 예외는 `withExcursion()`으로 감싸서 `finally`로 반드시 닫는다 — 열어둔 채 새면 그 뒤로 잠금이
영영 안 걸린다. 게이트는 돌아온 **한 번만** 무시하고 바로 다시 무장한다.

### 비밀 변경·해제 전 현재 비밀 확인 (2026-08-10 결정)

~~설정에서 바로 재설정·해제~~ → **현재 PIN·패턴을 통과해야 한다.**

이건 기밀이 아니라 **가용성** 문제다. 폰이 열려 있는 사람은 이미 일기를 볼 수 있어서 확인을 붙여도
그건 못 막는다. 막는 것은 **그 사람이 PIN을 바꿔 주인을 잠가버리는 것**이다 — 힌트 질문·답까지
새로 정해지므로 되찾기 경로도 함께 사라지고, 남는 건 초기화(전부 삭제)뿐이다.
§7.1이 "오작동 한 번에 일기가 사라진다"며 자동 삭제를 금지해 놓고 같은 결과를 수동으로 만드는 문을
열어둘 수는 없다.

- 확인 화면은 **잠금 화면과 같은 backoff를 공유**한다. 안 하면 설정이 더 약한 문이 되어
  무한 시도로 PIN을 찾아낼 수 있다(힌트 되찾기와 같은 원리).
- **비밀을 바꾸면 힌트 봉인도 반드시 다시 만든다.** 힌트는 옛 비밀을 XOR해 보관하므로,
  새 PIN만 저장하면 되찾기가 **옛 PIN을 보여준다.**
- 저장 순서는 **힌트 → 비밀**이다. 반대로 하면 힌트 저장 실패 시 *되찾을 수 없는 잠금*이 남는다.
  이 순서면 실패해도 잠금이 안 켜질 뿐이라 무해하다(`getLockConfig`는 해시 유무로 판정한다).

### 구현 현황 (2026-08-08)

| 항목 | 상태 |
|---|---|
| PIN(4자리) · 패턴(3×3, 4점 이상) | ✅ `features/lock/` |
| ~~생체인증~~ | 🚫 제거(2026-08-10) |
| salt + 해시로 SecureStore 보관 | ✅ 평문 저장 없음 |
| 앱 진입 시 잠금 + **나가면 즉시** | ✅ `LockGate` — 라우트가 아니라 앱 전체를 덮는 층 |
| 외출 예외(사진 선택·구글 로그인) | ✅ `features/lock/excursion.ts` |
| 비밀 변경·해제 전 현재 비밀 확인 | ✅ `/lock-setup?intent=change\|disable` — `UnlockView` 재사용(backoff 공유) |
| 설정 중 이탈 시 확인 | ✅ X · 안드로이드 뒤로가기 둘 다 |
| 실패 backoff (5회부터 30초씩, 최대 5분) | ✅ 데이터 삭제는 하지 않는다 |
| **힌트로 되찾기** | ✅ 설정 시 질문·답을 함께 받고, 답을 맞히면 PIN·패턴을 다시 보여준다 |
| **앱 스위처 가림** | ✅ Android (`expo-screen-capture` → `FLAG_SECURE`). ⚠ iOS는 아래 참고 |

### 힌트로 되찾기 (2026-08-08 사용자 결정)

잠금을 잊었을 때 **힌트 질문에 답하면 원래 PIN·패턴을 보여준다.** 새로 정하게 하지 않는다 —
잊은 사람이 원하는 건 "내가 뭘로 잠갔더라"이지 "또 하나 외우기"가 아니다.

되돌리려면 비밀을 복원 가능하게 보관해야 하는데, **평문으로 두지는 않는다**:
힌트 답에서 유도한 키스트림으로 XOR해 보관하고 답이 맞을 때만 푼다. 답을 모르면 열 수 없다.

| 지킨 것 | 방법 |
|---|---|
| 평문 저장 금지 | 답에서 유도한 키로 봉인. SecureStore에 봉인된 값만 남는다 |
| 무한 시도 금지 | 잠금 화면과 **같은 backoff를 공유**한다. 여기가 뚫리면 잠금이 뚫린 것이다 |
| 오타로 못 여는 일 방지 | 답은 앞뒤 공백·중복 공백·대소문자를 무시하고 비교한다 |

질문은 **고정 목록에서 고른다**(`HINT_QUESTIONS`). 자유 입력은 "ㅁㄴㅇㄹ" 같은 질문이나
답이 그대로 적힌 질문("내 PIN은 1234")을 만든다. 저장은 고른 **문구 그대로** 한다 —
나중에 목록을 손봐도 이미 저장된 힌트는 그대로 보여야 한다(Expand-only와 같은 이유).

⚠ **이 문은 힌트 답의 강도만큼만 강하다.** 답이 짐작하기 쉬우면 잠금도 그만큼 약해진다.
설정 화면에 "이 답을 아는 사람은 잠금을 열 수 있어요"를 그대로 적는다.

### 힌트도 못 맞히면? — 초기화가 유일한 길 (2026-08-08 결정)

**기존 조각을 여는 방법은 없다.** 그게 잠금의 정의다. 남는 길은 전부 지우고 새로 시작하는 것뿐이다.

- 잠금 화면 → *잠금을 잊으셨나요?* → *답도 기억나지 않아요* → **두 번 확인 후 초기화**
  (`features/settings/api/reset-app.ts` — 조각·사진·태그·설정·잠금 전부).
- 앱을 지웠다 다시 깔면 어차피 같은 일이 벌어진다. 앱 안에 길을 두는 게 정직하다 —
  막다른 화면에 세워두고 "재설치하세요"를 알아서 깨닫게 하지 않는다.
- 훔친 폰으로 초기화해도 **읽을 수 있게 되는 건 없다.** 잃는 건 가용성뿐이고,
  그건 앱 삭제로도 똑같이 잃는다. 그래서 이 길을 여는 것이 잠금을 약하게 만들지 않는다.
- 초기화 후에는 **화면 트리를 새 키로 다시 그린다.** 잠금 덮개 뒤 화면들은 포커스를 잃은 적이 없어
  스스로 다시 읽지 않는다 — 지웠는데 지운 조각이 그대로 보이면 지워진 게 맞나 싶어진다.

⏭ 구독 백업이 붙으면 여기에 **"백업에서 복원"** 이 하나 더 생긴다. 그게 진짜 답이다.

### 앱 스위처 가림 (2026-08-08 구현)

`expo-screen-capture`의 `preventScreenCaptureAsync()`를 **잠금을 켠 사람에게만** 건다.

- 이 플래그는 **스크린샷도 함께 막는다.** 잠그지도 않은 사람이 자기 일기를 캡처하지 못하게 되는 건
  부당하므로, 잠금과 묶는다. 잠금을 켠 건 "남에게 안 보이게 해달라"는 뜻이니 그 대가는 감수할 만하다.
- 설정 화면에 **켜기 전에** 알려준다 — "켜면 최근 앱 미리보기와 스크린샷도 가려져요".
  켜고 나서 "왜 캡처가 안 되지"를 겪게 두지 않는다.
- ⚠ **iOS는 스크린샷·녹화만 막힌다.** 앱 스위처 가림은 별도 오버레이가 필요하고 아직 없다.
  iOS 착수 시 `resignActive`에서 화면을 덮는 처리를 추가해야 한다.

⚠ **한 번 놓쳤던 것**: 게이트와 설정 화면이 잠금 설정을 각자 읽어서, **잠금을 켜도 가림이 걸리지
않았다**(게이트는 앱이 백그라운드에 다녀와야 다시 읽었다). `features/lock/store.ts`로 출처를
하나로 모아 해결했다. 설정 상태를 두 곳에서 읽으면 반드시 이런 틈이 생긴다.

**해시에 대한 정직한 한 줄**: 반복 해시를 쓰지만 이건 KDF가 아니다. 4자리 PIN은 해시를
탈취당하면 전수 대입에 버티지 못한다. 해시의 목적은 "평문을 저장하지 않는다"까지다.

### 안드로이드 권한은 **빼는 쪽으로** 관리한다 (2026-08-10)

라이브러리가 자기 매니페스트에 권한을 넣으면 우리가 안 써도 앱에 박힌다. 일기 앱에 마이크·카메라가
보이면 그 자체로 신뢰를 깎고, Play 정책에도 걸린다.

`app.json`의 `android.blockedPermissions`와 플러그인 옵션으로 5개를 걷어냈다:

| 뺀 것 | 어디서 들어왔나 |
|---|---|
| `READ_MEDIA_IMAGES` | expo-image-picker. **Play가 "빈번하지 않은 접근은 사진 선택 도구를 쓰라"고 명시한 그 권한** |
| `RECORD_AUDIO` · `CAMERA` | expo-image-picker (`microphonePermission`·`cameraPermission: false`) |
| `USE_BIOMETRIC` · `USE_FINGERPRINT` | **`expo-secure-store`가 `androidx.biometric`을 끌어온다.** 생체인증 코드를 지워도 남는다 |

**검증은 EAS 빌드 전에 로컬에서 한다** — 20분을 쓰고 매니페스트를 확인할 이유가 없다:

```
npx expo prebuild --platform android --no-install
grep uses-permission android/app/src/main/AndroidManifest.xml     # tools:node="remove" 확인
cd android && ANDROID_HOME=<sdk> ./gradlew processDebugMainManifest --rerun-tasks
grep -o 'android:name="[a-z.]*permission[^"]*"'   app/build/intermediates/merged_manifest/debug/processDebugMainManifest/AndroidManifest.xml
```

⚠ **소스 매니페스트만 보고 판단하지 않는다.** `blockedPermissions`는 지우는 게 아니라
`tools:node="remove"`를 붙이므로 **문자열은 그대로 남는다** — 병합 결과를 봐야 실제로 빠졌는지 안다.
(실제로 한 번 "아직 있음"으로 잘못 읽었다.)

#### 🔴 `expo-notifications` 하나가 권한을 **13 → 37**로 늘렸다 (2026-08-21)

리마인더를 붙이며 실측했다. **24개가 들어왔고 쓰는 것은 3개**였다 — 나머지는 런처 배지 20개와
FCM 푸시다. `INSTALL_SHORTCUT`·런처 설정 읽기/쓰기가 **일기 앱** 권한 목록에 뜨는 그림이라
`blockedPermissions`에 21개를 더해 **16개로 되돌렸다**(순수 증가 3).

이 절의 규약이 실제로 값을 한 이유가 여기다 — 라이브러리 하나가 **우리가 부르지도 않는**
배지 라이브러리를 끌고 왔고, 병합 매니페스트를 안 봤으면 그대로 나갔다.
⚠ 차집합은 **양방향으로** 본다. 넓게 차단하다 멀쩡한 권한을 같이 지우는 것이 더 큰 사고다.
⚠ 릴리스는 **릴리스끼리** 비교한다 — 디버그와 대조하면 디버그 전용 항목이 섞여 답이 오염된다.
상세는 [`docs/NOTIFICATION_SYSTEM.md`](./docs/NOTIFICATION_SYSTEM.md) §5.

⏭ `READ_EXTERNAL_STORAGE`·`WRITE_EXTERNAL_STORAGE`는 남겼다. 구버전 안드로이드에서 사진 선택에
필요할 수 있어 확인 전까지 건드리지 않는다.

### ⚠ 잠금은 암호화가 아니다

PIN·패턴은 **UI 게이트**일 뿐이다. 로컬 SQLite 파일 자체는 평문이므로, 루팅·탈옥 기기나 기기 백업 추출로는
잠금을 우회해 읽을 수 있다. 진짜로 막으려면 **로컬 DB 암호화**(SQLCipher류)가 별도로 필요하다.

- MVP는 **UI 게이트까지만** 한다. 일반 사용자의 "폰을 잠깐 남에게 보여줄 때"라는 실제 위협은 이걸로 막힌다.
- 로컬 DB 암호화는 미결정 항목으로 둔다(§12). **사용자에게 "암호화된다"고 표기하지 않는다** — 잠금은 잠금이라고만 쓴다.

---

## 7.2 구독 정책 (2026-08-10 결정)

### 상품은 **단일 구독 하나**다

~~광고 제거·백업/복원·AI 리포트를 기능별로 쪼갤지~~ → **하나로 묶는다**(§12 미결정 #1 해소).

```
조각 Pro
  ├─ 광고 제거      (구현됨 — adsEnabled() 한 곳)
  ├─ 백업/복원      (미구현 · 조각 서버 필요)
  └─ AI 요약 리포트  (미구현 · 조각 서버 프록시 필요)
```

| 항목 | 값 |
|---|---|
| 월간 | **₩3,900** |
| 연간 | **₩29,000** (월 환산 ₩2,417 · 약 38% 할인) |
| 무료 체험 | **7일** |
| 엔타이틀먼트 키 | **`pro` 하나.** `remove_ads`로 쪼개지 않는다 |

쪼개지 않는 이유:

1. **제일 싼 상품이 제일 비싼 상품을 잡아먹는다.** "광고 제거만 ₩1,500"을 만들면 대부분 그걸 사고,
   그 사람이 ₩3,900으로 올라오지 않는다.
2. **백업과 AI는 같은 일기를 다룬다.** 사용자에게 "이 둘이 왜 별개 상품이지?"로 읽힌다.
3. **되돌리기가 비대칭이다.** 나중에 상품을 늘리는 건 쉽고, 이미 판 상품을 없애는 건 어렵다
   (기존 구독자를 어디로 옮길지가 전부 문제가 된다). 적게 시작해서 데이터를 보고 늘린다.

~~⚠ **가격은 Play에 상품을 등록하기 전까지만 자유롭다.** 등록 직전에 한 번 더 확정한다.~~
→ **확정(2026-08-11 재확인)**: 월 ₩3,900 / 연 ₩29,000 / 체험 7일. 이 값으로 Play에 등록한다.

### 원가 구조 — 가격이 아니라 **구독자 수**가 문제다

**Vercel·Supabase는 이미 구독 중이다**(2026-08-10 확인). 그래서 조각 서버를 새로 만들 때 드는 것은
플랜 요금 전체가 아니라 **한계비용**뿐이다.

| | |
|---|---|
| Vercel 프로젝트 추가 | **$0** — Pro는 seat 과금이고 프로젝트는 무제한 |
| Supabase 프로젝트 추가 | **약 $10/월** — Pro의 $10 컴퓨트 크레딧은 **조직당 1개**만 덮는다(Micro `$0.01344/시간`) |
| 백업 저장 | 사진 1600px 축소 후 장당 ~300KB → 1GB 쓰는 사용자가 **월 2센트**. 100GB는 조직 공용 |
| AI 리포트 | 주간 4회. 모델에 따라 월 수 센트~10센트대. **원가 실측 전까지 월간·연간 리포트로 확대하지 않는다** |
| 실수령(₩3,900) | 부가세 제외 ₩3,545 → 스토어 수수료 15% 제외 **약 ₩3,013** |

**손익분기 약 5명**(한계비용 ₩14,000 ÷ ₩3,013). ₩2,900이든 ₩4,900이든 BEP는 5~7명이라
차이가 없다 — 그래서 가격은 수익 최적화가 아니라 **인식(positioning)** 기준으로 정했다.

⚠ 처음 쓴 "고정비 월 ₩62,000 · BEP 21명"은 **플랜을 새로 여는 경우**의 숫자였다. 이미 구독 중이라
그 계산은 조각에 해당하지 않는다 — 서버 생성을 미룰 비용상의 이유가 거의 없다는 뜻이다.

### ⚠ 착수 전에 매듭 — 구독을 끊으면 백업은?

가격보다 중요하다. 일기 백업을 지우는 건 잔인하고 CS가 가장 많이 들어올 지점이다.

| 안 | 판단 |
|---|---|
| 즉시 삭제 | 🚫 배신감이 크다 |
| **90일 유예 후 삭제 · 유예 중 복원 가능** | ✅ **제안.** 기기를 잃은 사람이 되찾을 시간이 충분하다 |
| 복원은 영구 무료, 새 백업만 중단 | 신뢰는 최고. 저장 단가가 싸서 가능하지만 무한 누적이 위험 |

- ~~유예 만료 전 **반드시 알린다.** 조용히 지우면 그게 최악이다.~~
  → **정정(2026-08-11)**: 지킬 수 없는 약속이었다. **통지 채널이 없다** — 푸시 미구현,
  원격 관측 0, 이메일 발송 경로 0. 남는 건 앱 내 배너뿐인데 **폰을 잃은 사람은 앱을 안 연다.**
  할 수 있는 것으로 바꿨다: **백업을 켤 때 미리 고지 + 유예 중 앱 내 배너·삭제 예정일 상시 표시
  + "앱을 열지 않으면 이 안내가 닿지 않는다"를 그 자리에 적기**
  ([`docs/BACKUP_SYSTEM.md`](./docs/BACKUP_SYSTEM.md) §5).
  ⏭ 푸시가 붙으면 진짜 통지가 되고, 그때 이 약속을 다시 강하게 쓸 수 있다.
- **백업 삭제 경로가 따로 필요하다.** 지금 탈퇴는 common_server의 신원만 지운다 —
  Play 데이터 보안 선언의 "삭제 요청 가능"이 백업까지 지워지는 것을 전제한다(§5.1).

### ⚠ 구독은 subject가 아니라 **구글 Play 계정**이 소유한다 (2026-08-10, common_server 확인)

`softDeleteSubject()`가 `provider_id`를 `deleted:<uuid>`로 가명화하므로 **탈퇴 후 재가입하면
`subject_id`가 바뀐다**(같은 계정으로 다시 가입하려면 UNIQUE가 풀려야 해서 의도된 설계다).

```
탈퇴 전   subject A  ← RC appUserId = A (구독 소유)
재가입    subject B  ← RC는 B에 아무것도 없음 → 돈은 나가는데 pro가 아니다
```

그래서 다음 둘이 **필수**다:

| | |
|---|---|
| 서버 | `TRANSFER` 웹훅 처리 (선택이 아니다) |
| 앱 | 로그인 후 서버가 `pro=false`인데 스토어에 구독이 있을 수 있으면 **`Purchases.restorePurchases()`** 를 부른다 |

### 제품 결정 — **1 구독 = 동시에 1 계정. 마지막에 복원한 쪽이 갖는다**

RC의 이동은 *공유*가 아니라 *이동*이다. 탈퇴가 아니어도 **같은 기기에서 다른 구글 계정으로 로그인하면
구독이 그쪽으로 옮겨가고 원래 계정은 잃는다.**

조각은 혼자 쓰는 일기 앱이라 계정 간 공유가 쓰임새가 아니고, Play 자체가 "구독은 구글 계정 소유" 모델이라
이 동작이 스토어와 일관된다. **그대로 간다** — 대신 구독 화면에 이 사실을 적는다.
가족·기기 공유를 기대하고 결제한 사람에게 조용히 뺏기는 것이 가장 나쁘다.

### ⚠ 탈퇴해도 구글 구독은 계속 청구된다

우리가 해지시킬 수 없다. 지금 탈퇴 문구에는 구독 얘기가 **한 글자도 없다** —
구독이 붙는 릴리스에서 반드시 함께 고친다:

- 탈퇴 다이얼로그에 **"구독은 따로 해지해야 한다"** 경고
- **Play 구독 관리로 보내는 링크** (`https://play.google.com/store/account/subscriptions`)
- 15개 언어 문구

안 하면 "탈퇴했는데 돈이 나간다" 문의와 환불 요청이 온다.

### 서버는 준비됐다 (2026-08-10, common_server Phase 9 완료)

웹훅 수신·엔타이틀먼트·앱용 조회까지 배포됐고 결제 가드 37/37 통과.
`active`는 저장하지 않고 `expiresAt`·`graceUntil`·`revokedAt`에서 **계산**한다 —
갱신 계열은 `max()`라 도착 순서와 무관하게 수렴하고, 뒤로 당길 수 있는
`PRODUCT_CHANGE`·`EXPIRATION`에만 시각 가드가 붙는다.

#### ⚠ 조각이 책임지는 함정 3개

초기 셋업에서 제일 자주 나오는 것들이다. 셋 다 **결제는 정상인데 권한만 안 붙는다**.

| 증상 | 원인 | 조각이 할 일 |
|---|---|---|
| 웹훅 이력에 `anonymous-app-user-id` | `Purchases.logIn(subject_id)`를 안 불렀다 | **로그인 성공 후에만 결제 화면을 연다.** RC가 익명 ID를 만들면 subject와 영영 매칭 안 된다 |
| `entitlement_ids`가 빈 배열 | RC에서 엔타이틀먼트에 **상품 attach를 빠뜨렸다** | 첫 실결제에서 `pro`가 실려 오는지 **눈으로 확인**한다 |
| 테스트 결제가 실권한이 됨 | `RC_SANDBOX_GRANT`를 켜둔 채 출시 | 라이선스 테스터는 내부 테스트에서 실제 결제해도 RC가 `SANDBOX`로 보낸다 → 테스트하려면 켜고 **출시 전에 반드시 끈다** |

세 번째는 콘솔 대시보드 타일로 노출돼 있다. **출시 체크리스트에 넣는다.**

#### 시크릿 발급

콘솔 구독 탭 → 발급 → **43자 값이 한 번만** 표시 → RC 대시보드 Integrations → Webhooks에 붙여넣기.
서버는 sha256만 저장하고 원문을 버린다 — RC가 원본을 들고 있으니 우리 쪽에 남길 이유가 없다.

### ⚠ 전자상거래법 — 착수 전에 매듭 (2026-08-11 법령 원문 확인)

**한국에서는 Google이 아니라 우리가 판매자(merchant of record)다.** Google의 MOR 국가 목록
(`answer/7645364`)은 EEA+영국 54개뿐이고 한국은 없다. DDA §3.4가 *"You are the merchant of
record for Products You sell … to all other users"*, §11.3이 *"as the principal to the
transaction with the user"* 라고 못박는다. 그래서 아래가 전부 **우리 책임**이다.

| | |
|---|---|
| ✅ **통신판매업 신고 — 완료** | **2026-07-14 신고**(공정위 공개 조회 실측 2026-08-14): 제2026-울산중구-0170호 · 울산광역시 중구 · 사업자등록번호 749-25-02260. ⚠ 면제 기준을 따질 필요가 없어졌다 — 신고를 마쳤다. (참고: 면제는 직전년도 거래 50회 미만 **또는** 간이과세자였고, 구독은 갱신마다 결제라 5명×12개월=60회로 첫해에 넘긴다) |
| ✅ **§13⑥ 무료체험 → 유료 전환** | **2024-02-13 신설.** 전환 일시·전후 가격·결제방법에 대한 **별도 동의** + 해지 조건·방법·효과 고지. ~~동의 플로우가 없다~~ → **있다**(2026-08-17 실측 정정). `app/subscribe.tsx` 전용 동의 화면 + `features/subscription/trial.ts` + 15개 언어. ⚠ 이 줄이 오래 🔴로 남아 **없는 블로커를 크리티컬 패스에 올려놨었다** |
| 🔴 **§17⑥ 단서 — 청약철회 제한** | 디지털콘텐츠는 *"표시 **와 함께** 시험 사용 상품 제공"* 이다 — **둘 다** 해야 한다(다른 재화는 택일). 안 하면 §17②5에 해당해도 **청약철회가 그대로 인정**된다. ✅ **7일 무료 체험이 시행령 §21조의2 2호("한시적 이용의 허용")를 그대로 충족한다** — 체험은 마케팅이 아니라 **법적 자산**이다. 없애면 청약철회 제한 근거를 잃는다 |
| 🟡 **§13① 표시 6종** | 상호 · 대표자 성명 · 주소 · 전화번호 · 전자우편주소 · **신고번호 + 신고받은 기관 이름**. ⚠ 사업자등록번호는 §13이 아니라 §10과 Play 요구사항이다(Play Console 계정 세부정보 → 한국 사용자에게만 노출) |
| 🟡 **VAT 10%를 우리가 낸다** | Google 문서: *"If you're located in South Korea, **you're responsible** for determining, charging, and remitting VAT"* — 국외 개발자는 Google이 대납하지만 국내는 아니다. §7.2의 실수령 계산과는 맞으나 **납부 주체가 본인**이다 |
| 🟡 **환불 1차 책임도 우리** | Play 약관: *"환불은 원칙적으로 **계약 당사자의 환불 정책**을 따릅니다."* 한국의 계약 당사자는 개발자다 |

⏭ 미확인: 공정위가 **구독 자동갱신을 별개 거래로 세는지**에 대한 유권해석 1차 자료를 못 찾았다.
고시가 감산을 명시한 것이 청약철회뿐이라 **결제 1회 = 1거래로 보수적으로 가정**한다.

### 착수 순서

```
1. common_server Phase 9   RevenueCat 엔타이틀먼트 + 웹훅   ← 조각 밖 선행(§13)
2. 조각 서버 신규           Next.js + Supabase              ← 여기서 과금이 시작된다
3. RevenueCat · Play        상품 등록(월·연) · 가격 최종 확정
4. 앱                       react-native-purchases + 구독 화면
5. 백업 → AI                AI는 원가 실측 후 확대
```

⚠ **AI 요약을 위해 서버가 본문을 읽는다는 사실을 구독 화면에 명시한다**(§5.1).
`"저장하지 않습니다"(O) / "볼 수 없습니다"(X)`.

---

## 8. 프로젝트 구조 (2026-08-07 결정 — 모노레포 아님)

~~apps/mobile · apps/web · packages/{ui,config,types,lib,hooks} 모노레포~~
→ **정정(2026-08-07, 사용자 결정)**: volleyball과 동일한 배치. 모노레포 도구(workspaces·Metro monorepo)의
초기 공수와 번들 트러블슈팅을 피하고 검증된 구조를 쓴다.

```
/                 ← Expo 앱 루트 (main: expo-router/entry)
  app/            ← expo-router 라우트 (화면)
  features/       ← 기능별 폴더 (api · components · hooks · types)
  components/     ← 공통 UI (Button·Card·TextField·Modal·Header·BottomSheet·Avatar·Loading)
  db/             ← expo-sqlite 스키마·마이그레이션·쿼리
  lib/            ← common_server SDK 사본 · 조각 서버 클라이언트 · 유틸
  store/          ← Zustand 스토어
  theme/          ← 컬러·여백·radius 토큰
  types/          ← 공용 타입
  docs/           ← 설계 문서 (docs/README.md가 색인)
  server/         ← (예정) 조각 서버. Metro blockList로 번들 제외 완료
```

- Atomic Design 쓰지 않는다. **Feature 폴더 + 공통 컴포넌트**만.
- 각 Feature는 `api · components · hooks · types`로 분리한다. 화면 자체는 `app/`의 라우트 파일.
- common_server SDK는 **복사해서** 쓴다(`common_server/client/` → `lib/common-server/`). monorepo·npm 패키지
  안 쓴다 — common_server 규약. 복사 시점을 주석으로 남긴다.
- `server/`는 Metro가 크롤하면 중복 React·Haste 충돌로 번들이 깨진다 → `metro.config.js` `blockList`로 제외 완료.

---

## 9. 디자인 원칙

- Apple 수준 미니멀. **여백을 많이** 쓴다(`theme/spacing.ts` — lg(24) 이상 기본).
- 부드러운 Radius(`theme/spacing.ts`의 `radius`).
- 폰트 Pretendard. `fontWeight` 대신 굵기별 `fontFamily`(`theme/typography.ts`).
- **Glass 금지 · 과한 Gradient 금지 · Shadow 최소.**
- 컬러: ~~차분한 크림 + 하늘 + 베이지~~ → **정정(2026-08-07, 시안 수령)**: 옅은 블루그레이 바탕 +
  진한 네이비 강조, 크림/베이지는 **종이 조각 표현에만**. 정본은 `theme/palettes.ts`.

### 테마 (2026-08-08)

**팔레트를 여러 벌 둘 수 있는 구조로 짰다.** 라이트/다크 둘만 상정하면 스킨을 붙일 때 갈아엎게 된다 —
스킨 추가는 `theme/palettes.ts`에 팔레트를 하나 더 넣는 일이어야 한다.

| 것 | 어디 |
|---|---|
| 팔레트 정의 | `theme/palettes.ts` (`light` · `dark`) |
| 적용·저장 | `theme/theme.tsx` (`ThemeProvider` · `useColors` · `useTheme`) |
| 스타일 | `theme/use-styles.ts` (`useStyles(createStyles)`) |
| 사용자 선택 | 설정 → 화면 → 시스템 · 라이트 · 다크 (`app_settings.theme_mode`) |

**다크 팔레트는 시안이 없어 라이트에서 유도했다.** 남색 정체성은 유지하되 **강조색의 명암을 뒤집었다** —
어두운 바탕에서 진한 남색은 버튼으로도 글자로도 읽히지 않는다. `accent`를 밝게 올리고
`textOnAccent`를 어둡게 내렸다. 시안이 나오면 이 두 값부터 다시 본다.

### ⏭ 테마 스킨 (계획, MVP 밖)

유료/보상 스킨을 붙일 예정이다. 위 구조는 이걸 전제로 만들었다.

| 여는 방법 | 기간 |
|---|---|
| 보상형 동영상 광고 시청 | **1개월** |
| 월 구독 | 구독 중 계속 (+ 광고 없음) |

⚠ 착수 전에 매듭지을 것: 스킨 소유 기간을 **어디에 저장**하나(기기 vs 서버).
기기에만 두면 재설치로 리셋되고, 서버에 두면 구독처럼 신원(common_server Phase 7)이 필요하다.

### 디자인 시안 사용법 (2026-08-07)

수령한 시안은 **테마(색감·톤·분위기) 참고용**이다. **화면 구조를 그대로 따르지 않는다.**

- 작성 화면은 시안과 다르다 — **제목 + 본문(중간에 이미지 삽입) + 태그** 구조로 간다(DIARY_SYSTEM §1).
- 시안의 위치·음악 첨부, 그리기 도구는 **도입하지 않는다.**
- 시안의 타임라인 화면은 별도 화면으로 만들지 않는다 — 홈의 "더보기" 목록으로 충분하다.
- ~~시안의 알림 설정은 **화면만 만들고 기능은 구현하지 않는다**(2026-08-07 결정).~~
  → **정정(2026-08-21)**: **로컬 알림으로 구현했다**([`docs/NOTIFICATION_SYSTEM.md`](./docs/NOTIFICATION_SYSTEM.md)).
  **푸시(FCM)는 여전히 범위 밖**이다 — 서버에 보낼 수단도 근거도 없다. 리마인더는 기기가
  시간만 알면 되는 일이라 서버가 기여할 것이 없다.

---

## 9.1 다국어 (2026-08-09 결정 — 전 세계 출시)

**앱은 처음부터 다국어다.** 기본 언어는 **기기 언어**, 지원하지 않으면 **영어**로 떨어진다.
한국어를 폴백으로 두지 않는다 — 전 세계 사용자 대부분이 못 읽는 화면이 된다.

| 항목 | 결정 |
|---|---|
| 라이브러리 | i18next + react-i18next + expo-localization |
| 언어 | **15개** — ko · en · ja · zh-Hans · zh-Hant · es · pt-BR · fr · de · it · ru · id · vi · th · tr |
| 추가 | `locales/<code>.json` 하나 + `lib/i18n.ts`의 `LANGUAGES`·`LANGUAGE_LABELS`·`LANGUAGE_ORDER` 등록. **설정 화면은 안 건드린다** |
| RTL | ❌ 아랍어·히브리어 없음. JSON만으로 안 되고 `I18nManager`+레이아웃 반전이 필요하다 |
| 사용자 선택 | 설정 → 화면 → 언어 → **선택 시트**(목록). `app_settings.language`에 저장 |
| 검사 | `npm run check:i18n` — 키 누락·언어 간 불일치를 잡는다 |

### ⚠ 배포 지역 제약 — EEA·영국·스위스 제외 (2026-08-09 결정)

**AdMob UMP(동의 메시지)를 붙이지 않았다.** EEA·영국·스위스는 맞춤형 광고 전에 GDPR 동의를
받아야 하므로, 그 지역을 **배포 국가에서 뺀다.** my_word가 같은 이유로 같은 선택을 했다
(`my_word/docs/RELEASE_NOTES_1.2.0.md` — "EEA·영국·스위스 배포 (AdMob 동의 UMP 미연동)").

그 결과 **독일어·이탈리아어는 사실상 사용처가 없다**(독일·오스트리아·스위스·이탈리아가 전부 대상 밖).
그래도 **지우지 않는다** — UMP를 붙이는 순간 바로 살아나고, 지웠다 다시 만드는 비용이 더 크다.
프랑스어는 캐나다·아프리카가, 스페인어는 중남미가, 포르투갈어는 브라질이 남아 타격이 적다.

⏭ UMP는 라이브러리(`react-native-google-mobile-ads`)에 `AdsConsent`로 **이미 들어 있다.**
   붙이는 순간 EEA가 열린다 — 유럽 사용자를 노릴 때 가장 먼저 할 일이다.

#### 🔴 이유가 광고 하나가 아니다 (2026-08-20 확인)

*"테스트 광고를 쓰니 EEA를 넣어도 되지 않나"* 로 한 번 뒤집으려다 잡혔다. **광고는 셋 중 하나다.**

| 걸리는 것 | 테스트 광고로 무마되나 |
|---|---|
| AdMob UMP 미연동 | ✅ |
| **처리방침이 GDPR을 다루지 않는다** — [`I18N_SYSTEM.md`](./docs/I18N_SYSTEM.md) *"EEA를 뺐다. 즉 GDPR은 지금 적용되지 않는다"* | ❌ |
| **AI 국외이전 고지의 법인명** — EEA·스위스는 `OpenAI Ireland Limited`인데 앱은 `OpenAI OpCo, LLC`를 보여준다(§12 2026-08-19) | ❌ |

EEA 사용자가 리포트를 한 번 만들면 **§28-8 법정 고지가 틀린 법인명을 표시한다.**
광고를 끄든 켜든 이건 남는다. **UMP를 붙여도 EEA가 바로 열리지 않는다** — 위 두 줄이 같이 풀려야 한다.

#### 트랙별 국가 설정

| 트랙 | 국가 | 근거 |
|---|---|---|
| **비공개 테스트(Alpha)** | **대한민국만** | 2026-08-20 사용자 결정. 테스터(doply 41명)가 한국이고, 초대제라 넓힐 이유가 없다 |
| **정식 출시(프로덕션)** | **EEA·영국·스위스를 뺀 나머지 전부** | 위 결정 그대로 |

⚠ **비공개 트랙 국가는 프로덕션과 동기화되지 않는다**(콘솔 다이얼로그 실측). 한국만 골라도
   출시 국가가 한국으로 굳지 않는다. 반대로 **출시 때 따로 골라야 한다** — 자동으로 안 넓어진다.

⚠ **콘솔에 대륙 묶음이 없다.** 전체 177개에서 아래를 **손으로** 빼야 한다(2026-08-20 실측).
   필터는 `모든 국가/지역` · `타겟팅됨` · `타겟팅되지 않음` 셋뿐이다.

```
EU 27   그리스 네덜란드 덴마크 독일 라트비아 루마니아 룩셈부르크 리투아니아 몰타 벨기에
        불가리아 슬로바키아 슬로베니아 스웨덴 스페인 아일랜드 에스토니아 오스트리아 이탈리아
        체코 크로아티아 키프로스 포르투갈 폴란드 프랑스 핀란드 헝가리
EEA +3  노르웨이 리히텐슈타인 아이슬란드
그 외    영국 스위스
```

⚠ 이 목록을 **그대로 믿고 저장하지 않는다.** Play 국가 목록에는 해외영토·속령이 별도 항목으로
   들어 있을 수 있어(예: 프랑스령 기아나) EEA에 속하는데 이름이 달라 빠질 수 있다. 출시 시점에
   저장 전 `타겟팅됨` 필터로 **한 번 훑어 눈으로 확인**한다 — 하나 빠뜨리면 위 두 법정 고지가
   틀린 채로 노출된다.

⚠ 구독 상품은 **국가별로 가격이 설정돼 있어야 한다**(콘솔 경고 문구). 국가를 넓히면
   `jogak_pro` 가격이 그 국가에 있는지 함께 본다.

### 반드시 지킬 것

1. **화면에 보일 문자열을 코드에 직접 쓰지 않는다.** 전부 `t()`로 꺼낸다.
2. **DB·SecureStore에는 코드/id만 저장한다.** 감정은 `joy`, 힌트 질문은 `highschool`.
   문구를 저장하면 언어를 바꾼 순간 **옛 데이터만 옛 언어로** 남는다.
3. **날짜는 조각을 갈아끼우지 말고 문장 틀을 통째로 번역한다.** "2026년 8월 8일"과
   "August 8, 2026"은 순서가 다르다 — `lib/format.ts`는 조각만 넘기고 틀은 `date.*` 키가 갖는다.
4. **언어 이름은 그 언어로 적는다.** 영어 화면에서 'Korean'이면 한국어 사용자가 자기 언어를 못 찾는다.
5. **문자열 길이를 전제한 레이아웃을 만들지 않는다.** 독일어는 한국어의 두 배까지 길어진다 —
   고정 폭 칸과 `numberOfLines={1}`은 잘린 라벨을 만든다. 새 UI는 **독일어로 한 번 보고** 넘어간다.
   실제로 겪음(2026-08-09): 720px 기기 독일어에서 탭 라벨이 `Einstellun…`으로 잘렸다.
   탭 라벨은 자르지 않고 **줄여서** 넣는다(`adjustsFontSizeToFit`, `app/(tabs)/_layout.tsx`).
6. **선택지가 늘면 세그먼트를 버린다.** 테마 3개는 늘어놓지만 언어 15개는 시트 목록이다 —
   가로로 늘어놓으면 글자가 잘리고, 옆으로 밀어야 보이는 항목은 사실상 안 고른다(§10).

상세 규약·언어 추가 절차는 [`docs/I18N_SYSTEM.md`](./docs/I18N_SYSTEM.md).

---

## 10. 코드 스타일

### 화면 작성 규칙 (2026-08-07 — 반드시 지킨다)

**모든 화면은 `components/Screen`을 바깥 틀로 쓴다. 화면에서 `SafeAreaView`·`ScrollView`를 직접 쓰지 않는다.**

세이프에어리어와 키보드 가림을 화면마다 따로 처리하면 반드시 빠뜨린다 —
실제로 탭바가 시스템 내비게이션 아래로 깔리고 입력창이 키보드에 가리는 문제를 겪고 도입했다(2026-08-07).

| 상황 | `edges` |
|---|---|
| 탭 화면(홈·캘린더·리포트·설정) | 기본값 `['top','left','right']` — **아래를 넣지 않는다.** 탭바가 이미 아래 인셋을 먹는다 |
| 탭 없는 화면(작성·상세·잠금·**검색**) | `['top','bottom','left','right']` |

- 고정 헤더는 `header` prop으로 넘긴다(스크롤에 딸려 올라가지 않는다).
- 스크롤이 필요 없는 화면은 `scroll={false}`.
- 하단 탭바 높이는 **고정값이 아니라 `useSafeAreaInsets()`로 계산**한다. 기기마다 다르다.
- **키보드 대응에 `KeyboardAvoidingView`를 쓰지 않는다.** 안드로이드는 `adjustResize`라 사실상
  아무 일도 하지 않고, 플랫폼별 오프셋을 손으로 맞추는 방식은 어긋난다.
  `hooks/use-keyboard`가 키보드 윗변의 실제 좌표를 재고, `Screen`이 **가려지는 만큼 스크롤 영역을
  줄인다.** 아래 여백만 더하면 손으로 밀어야 닿는다 — 영역이 짧아져야 포커스된 입력창이 저절로 올라온다.
- **가로 스크롤로 선택지를 숨기지 않는다.** 옆으로 밀어야 보이는 항목은 사실상 안 고른다.
  감정 8개처럼 개수가 정해진 선택지는 `flexWrap`으로 줄바꿈한다(2026-08-08, 감정 선택에서 겪음).
- **스타일 배열 뒤쪽에서 배경색만 바꿀 때는 `borderRadius`를 함께 다시 적는다.**
  안드로이드에서 앞 스타일의 반지름이 먹지 않고 네모로 그려지는 경우를 겪었다
  (2026-08-08, 캘린더에서 선택된 날짜가 원이 아니라 사각형으로 나옴).
- 🔴 **행(`flexDirection: 'row'`) 안의 `Text`에는 `flexShrink: 1`을 준다.** 없으면 **마지막 단어가
  안 그려진다** — Fabric이 측정과 페인트에 다른 폭을 줘서, 상자는 전체 문자열 폭으로 잡히는데
  내부 텍스트는 더 좁은 폭으로 줄바꿈하고 2행이 1행 높이 밖으로 밀려 사라진다.
  2026-08-23 영어 홈에서 실측: 상자 `New entry`(196px) · 칠해진 것 `New`(85px),
  `Sun, August 23, 2026` → `Sun, August 23,`. **UI 트리에는 온전한 문자열이 들어 있어**
  접근성 덤프로는 안 잡히고 **픽셀을 봐야** 보인다.
  - ⚠ **한국어에서는 거의 안 보인다** — 문자열이 짧아 줄바꿈 지점에 안 닿는다.
    **영어가 14개 언어의 폴백**이라 여기가 가장 넓게 새는 자리다(§9.1 규칙 5의 실제 사례).
  - 🚫 `numberOfLines={1}`로 막지 않는다. 그건 잘린 라벨을 *의도적으로* 만드는 것이다.
  - 아이콘·뱃지·셰브론과 나란한 라벨이 전형적인 자리다. `components/Button`은 고쳤으므로
    **버튼은 전부 해결됐다** — 나머지는 화면이 직접 만든 행이다.

- **모바일에서 키보드 엔터에만 의존하는 확정 동작을 만들지 않는다.** 눈에 보이는 버튼을 기본 경로로 두고
  엔터는 보조로 받는다(태그 입력에서 겪음).

---

## 10.1 코드 스타일

- **색을 직접 import하지 않는다.** `StyleSheet.create`를 모듈 최상단에 두면 색이 앱 시작 시점에
  굳어 테마를 못 바꾼다. 스타일은 `const createStyles = (colors: Palette) => StyleSheet.create({...})`로
  만들고 컴포넌트에서 `useStyles(createStyles)`로 받는다. JSX 안(아이콘 색 등)에서는 `useColors()`.
- **화면 문자열을 코드에 직접 쓰지 않는다.** `t('...')`로 꺼낸다(§9.1). 새 문자열은 `locales/*.json` **전 언어**에 넣는다.
- **`any` 절대 금지.** 타입 안전성 유지.
- props drilling 남발 금지 — 깊어지면 Zustand 스토어나 Context로.
- 폴더명 **kebab-case**, 컴포넌트 **PascalCase**.
- ESLint · Prettier 적용(도입 예정).
- 주석은 "왜"를 남긴다. 자명한 "무엇"은 적지 않는다.

---

## 11. 표준 작업 순서

① **플랜** — 상충 결정 확인 + 작업 분해
② **문서** — 설계 결정을 코드보다 **먼저** 문서에(`CLAUDE.md` 또는 `docs/<SYS>_SYSTEM.md` + `docs/README.md` 색인)
③ **개발**
④ **검증** — `npm run typecheck` · `npm run lint` · `npm run check:i18n` 통과 후 커밋
  ⚠ **릴리스 AAB를 로컬에서 구울 때는 `eval "$(npm run -s release:env <profile>)"` 로 프로필 env를 셸에 넣은 뒤 `npm run check:release-env`를 돌린다** —
  EAS는 깨끗한 컨테이너라 없던 문제인데, 로컬은 `.env.local`을 읽어 개발용 값이
  **번들에 문자열로 박힌다**(올린 뒤에는 못 고친다). `docs/MONETIZATION_SYSTEM.md` §6.1.4
  ⚠ `features/ai/{types,prompt,period}.ts`를 고쳤으면 **`npm run sync:shared`도 돌린다** —
  서버가 그 복사본을 쓴다. `npm run check:shared`가 어긋남을 잡는다(§5)
  ⚠ **검사를 늘렸으면 `npm run check:doc-counts`** — 문서에 박힌 "N개"가 같이 늙는다.
  낡은 숫자는 *"이만큼 검사한다"* 는 거짓 안심을 준다(2026-08-20에 8군데가 어긋나 있었다)

- 커밋 메시지: `YYMMDD :: [태그] 한국어 작업 요약` (예: `260807 :: [세팅] Expo SDK 54 부트 스캐폴드`)
  — Conventional Commit은 쓰지 않는다(2026-08-07 결정, volleyball 방식 승계).
- 멀티라인 메시지는 heredoc/임시파일 + `git commit -F`로 — 셸 따옴표 오염 회피.
- 브랜치: `main` · `develop` · `feature/*`.
- 문서 갱신을 코드 커밋과 **함께**(또는 먼저) 한다.

---

## 12. 결정 로그

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-07 | 모노레포 ❌ → 루트 Expo + `server/` | volleyball 검증 구조 승계, 초기 공수·번들 리스크 회피 |
| 2026-08-07 | Expo SDK **54** (최신 57 아님) | volleyball과 동일 조합 → 트러블슈팅 자산 재사용 |
| 2026-08-07 | ~~데이터는 처음부터 Supabase~~ → **로컬(expo-sqlite) 우선 + 유료 백업** | 사용자 정정. 일기의 진실은 기기, 서버는 백업 사본 |
| 2026-08-07 | 로컬 DB는 expo-sqlite | 검색·캘린더·연속 작성일에 쿼리 필요. AsyncStorage는 조각 수천 개에서 느려짐 |
| 2026-08-07 | MVP에 월 구독·AI 포함 | 일기 CRUD는 1일이면 충분하다는 판단 |
| 2026-08-07 | 로그인은 선택적, 문의·결제에서만 필수 | 기둥 1(쓰기 마찰 최소화) 보존 |
| 2026-08-08 | ~~첫 실행부터 로그인 필수 + 이메일 인증 초기화~~ → **철회, 위 정책 유지** | 같은 날 되돌림. 확인한 사실은 §4에 남김 |
| 2026-08-08 | dev build 전환은 **문의·구독 착수 시점으로** | 구글 로그인은 Expo Go에서 안 된다. 지금은 필요 없으므로 미룬다 |
| 2026-08-07 | 광고는 저장 완료 후 + 빈도 캡 | 기둥 4. 작성 진입 차단은 AdMob 정책상도 위험 |
| 2026-08-08 | 광고 지면 확정 — **전면은 저장 완료 시 하루 1회, 배너는 탭 화면만 상시** | 작성·상세는 쓰고 읽는 자리라 비운다(기둥 2). 진입 전면광고는 AdMob 정책 위반 소지(§7) |
| 2026-08-08 | 다크모드 + **팔레트 N개 구조**(`theme/palettes.ts`) | 스킨(보상형 광고 1개월·구독)이 예정돼 있어 라이트/다크 이분법으로 짜면 갈아엎게 된다(§9) |
| 2026-08-09 | **Expo Go → dev build 전환** | 광고 SDK가 네이티브 모듈이다. 미루려던 전환을 광고 착수가 앞당겼다 |
| 2026-08-07 | 공통(공지·문의·신원·엔타이틀먼트)은 common_server, 도메인(백업·AI)은 조각 서버 | 개인정보 등급·배포 반경·워크로드 성질이 다름 |
| 2026-08-07 | ~~백업 평문 저장(AI를 위해)~~ → **클라이언트 암호화 + AI는 앱이 평문을 프록시로 전송** | 저장물 유출 리스크 제거와 AI 상품을 동시에 살림(§5.1) |
| 2026-08-07 | 암호화 키 = 기기 SecureStore + 복구 코드. 서버 미보유 | 아래 근거 참조 |
| 2026-08-07 | AI 리포트는 **주간부터**, 단가 확인 후 월간·연간 | 비용 실측 후 확장 |
| 2026-08-07 | Supabase·조각 서버는 **월 결제 착수 시** 생성 | MVP 초반은 서버를 안 쓴다 — 미리 만들면 과금만 발생. 로컬 스키마를 백업 가능한 형태로 미리 잡아 마이그레이션을 없앤다 |
| 2026-08-07 | 커밋 `YYMMDD :: [태그] 요약` | volleyball 방식 승계 |
| 2026-08-09 | **다국어(ko·en) 도입, 폴백은 영어** | 전 세계 출시 목표(사용자 결정). 문자열이 흩어진 뒤 걷어내는 비용이 두 번째 언어에서 이미 늦다 |
| 2026-08-09 | **EEA·영국·스위스 배포 제외** (AdMob UMP 미연동) | my_word와 같은 선택. UMP를 붙이면 열린다 — 독일어·이탈리아어가 그때까지 놀지만 지우지 않는다 |
| 2026-08-09 | ~~처리방침은 **한국어 단일**~~ (2026-08-12 뒤집힘, 아래 참조). **정본에서 HTML을 뽑는다**는 유지 | 배구명가에서 웹/앱 드리프트로 필수 항목이 누락된 사고가 있었다 |
| 2026-08-09 | 감정·힌트 질문은 **코드/id로 저장**(문구 아님) | 문구를 저장하면 언어를 바꾼 뒤 옛 데이터만 옛 언어로 남는다 |
| 2026-08-10 | 잠금에서 **생체인증·지연 설정 제거** — PIN·패턴만, 나가면 즉시 | 고를 것을 줄인다. 선택지마다 실패 경로가 붙어 코드와 설명이 함께 는다(§7.1) |
| 2026-08-10 | 잠금 **변경·해제 전 현재 비밀 확인** | 기밀이 아니라 가용성 문제 — 남이 PIN을 바꾸면 주인이 잠기고 초기화만 남는다(§7.1) |
| 2026-08-10 | 구독은 **단일 상품** `조각 Pro`, 월 ₩3,900 / 연 ₩29,000 / 7일 체험 | 싼 티어가 비싼 티어를 잡아먹는다. 상품은 늘리기 쉽고 없애기 어렵다(§7.2) |
| 2026-08-11 | 복구 코드 = **128비트 27자**(256비트 53자 아님) | 128비트는 무차별 대입으로 안 깨지고, 실제 실패 모드는 해독이 아니라 **오타와 분실**이다. 손으로 옮겨 적는 글자 수를 절반으로 줄이는 쪽이 크게 이긴다 |
| 2026-08-11 | 키 유도는 HKDF **Extract + Expand**(~~Expand only~~) | RFC 5869 §2.3이 PRK를 **at least HashLen**으로 요구해 16바이트 코드는 PRK가 될 수 없다. KAT가 잡았다 — 자기 왕복만 돌렸으면 통과했을 오류다 |
| 2026-08-11 | 복원은 **전체 교체 + 차집합 경고**(스냅샷 없이) | 확인 화면이 선택이 아니라 조건이다. ⏭ 스크래치 DB 스왑은 `backupDatabaseAsync` 실기기 확인 후 |
| 2026-08-11 | 최초 grant는 **선착순** | 되찾기만 있으면 "새 폰이 처음 잡는" 경로가 없어 주 시나리오가 계약상 불가능했다. `vault_id`가 복구 코드에서 유도되므로 새로 여는 문이 없다 |
| 2026-08-11 | 조각 DB는 **별도 Supabase 프로젝트**. 근거는 개인정보 등급이 아니라 **service_role 폭발 반경** | E2EE라 "본문이 들어간다"는 논거가 약해졌다. 실제로 결정을 지탱하는 건 `service_role`이 프로젝트 전체 RLS를 우회한다는 사실 — 공유하면 조각 침해가 my_word·배구명가 신원까지 연다(§5) |
| 2026-08-11 | **stg는 무료 조직, 운영은 Pro 조직**(+$9.8/월) | 컴퓨트는 프로젝트마다 전용 서버라 못 나눈다. 반대로 저장 100GB·egress 250GB는 조직 단위라 그대로 나눠 쓴다(현재 0GB 사용). 무료 티어는 7일 정지라 **운영 불가** |
| 2026-08-11 | 로컬 개발은 **Supabase 로컬 스택**(Postgres+MinIO 아님) | 가장 불확실한 항목이 "RN이 5MB를 서명 URL에 PUT하는가"인데, MinIO는 API가 달라 **답이 거짓 확신이 된다** |
| 2026-08-12 | **검색 탭 → AI 리포트 탭.** 검색은 푸시 화면으로 강등 | 6번째 탭을 만들면 독일어 라벨이 다시 잘린다(§9.1). 검색은 찾을 때만 여는 화면이라 돋보기로 충분하지만, 리포트는 **탭에 없으면 존재를 모른다** — 구독의 세 기둥 중 하나가 안 보이는 상품이 된다 |
| 2026-08-12 | 주간 표기는 **날짜 범위 + 주차 부제**(~~"8월 둘째 주"~~) | 서수는 세는 규칙이 언어마다 다르고 달 경계에서 어색해진다(8월 31일부터인 주가 "다섯째 주"). `weekRange()`가 이미 있어 구현도 싸다. 주차는 **원격 관측 0인 앱의 진단 식별자**로 남긴다 |
| 2026-08-12 | 🔴 **구독이 끝나도 발행된 리포트는 계속 본다** | 로컬 DB에 있는 사용자의 기록을 결제 상태로 뺏지 않는다. 게이팅은 **[만들기] 버튼 하나**이고 저장소는 구독을 아예 모른다 — 저장소가 알면 규칙이 두 곳으로 갈라진다 |
| 2026-08-12 | 리포트도 **백업 매니페스트에 싣는다**(`MANIFEST_FORMAT` 2) | 캡이 주 1회이고 같은 조각으로도 같은 문장이 안 나온다 — **다시 만들 수 없다.** 조각만 복원하면 폰을 바꾼 사람이 1년치 회고를 잃는다 |
| 2026-08-12 | ~~처리방침은 한국어 단일~~ → **15개 언어로 번역.** 한국어가 정본 | 옛 근거는 "번역본이 원문과 어긋나면 어느 쪽이 효력인지 다툼"이었는데 **답이 틀렸다** — 다툼은 `한국어본이 우선한다` 한 줄로 막히고, 안 쓴 대가는 **자기 정보 처리를 못 읽는 사용자**다. 어긋남은 문구가 아니라 **구조**로 막는다(`check:legal`이 절·줄 수를 대조) |
| 2026-08-13 | 🔴 **30일 자기구속은 애초에 해당 없었다** | §13의 "불리한 변경 30일 전 고지"가 보호하는 것은 **이미 쓰고 있던 이용자**다. 조각은 **공개 출시 0·공개 사용자 0**이고 백업·AI가 첫 출시에 들어가므로 그건 변경이 아니라 **최초 처리방침**이다. 백업 9/10·AI 9/11 시계가 **둘 다 무효** — 크리티컬 패스에서 빠진다 |
| 2026-08-13 | 🔴 **AI 리포트 본문을 서버에 90일 저장**(§5.1을 뒤집는다) | 리포트가 좋은지 나쁜지 볼 방법이 없으면 **프롬프트를 고칠 근거가 없다.** 원격 관측 0인 앱이라 사용자 문의 외에 신호가 없었다. ⚠ 저장하는 것은 **모델이 쓴 요약문**이지 일기 원문이 아니다 — 그 구분이 처리방침 문안의 전부다([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §5.2) |
| 2026-08-13 | AI 호출 실패 시 **1시간 잠금 + Discord 알림** | 일일 캡이 `ai_usage`(성공만) 행을 세어 **실패 루프가 무제한이었다** — 그런데 비싼 쪽이 실패다(모델이 돌고 나서 거부). ⚠ 캡이 아니라 잠금이라 한 시간 뒤 같은 기간을 그대로 만들 수 있다 |
| 2026-08-13 | 사진 blob은 **Supabase Storage 유지**(R2로 안 옮긴다) | "이미지는 Supabase에 두지 마라"의 근거는 **egress**인데 조각의 blob은 복원 때만 읽는 **cold 암호문**이라 egress가 250GB 중 9%도 안 찬다. 이미 낸 100GB가 비어 있어 **한계비용 0원**이고 교차점은 구독자 ~4,000명이다. ⚠ 단 `vault_id`가 경로에 박혀 지금이 가장 싼 결정 시점이고, 옮길 실익은 비용이 아니라 **`service_role` 제거**다([`docs/BACKUP_SYSTEM.md`](./docs/BACKUP_SYSTEM.md) §5.5-⑦) |
| 2026-08-18 | **로컬 릴리스 빌드에는 `check:release-env` 게이트를 먼저 통과한다** | EAS 무료 빌드 쿼터가 떨어져 로컬로 굽자마자 드러났다 — `prebuild` 첫 줄이 `env: load .env.local .env`다. `.env.local`의 `EXPO_PUBLIC_BACKUP_SERVER_URL=http://10.0.2.2:3200` · 다른 RevenueCat 키 · `DEVICE_CHECK=1`이 **번들에 문자열로 박힐 뻔했다**(올린 뒤엔 못 고친다). ⚠ ~~`EXPO_PUBLIC_DEVICE_CHECK`에 `__DEV__` 가드를 붙인다~~ → **되물렸다.** 그 점검의 목적이 *"릴리스 번들에서 암호 처리량을 재는 것"* 이라 가드를 붙이면 기능이 죽는다 — `app/_layout.tsx` 주석이 그 이유를 적어놨는데 안 읽고 `DEV_LOGIN`과 나란히 놓고 판단했다. 토큰도 fail-closed다(서버가 401). **소스가 아니라 빌드 게이트가 맞는 자리다** ([`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.4) |
| 2026-08-18 | 🔴 **서버가 쓰는 순수 계층은 `server/shared/`로 생성 복사** | `@shared/*` → `../features/*`(tsconfig paths)가 **로컬에서만 됐다.** Vercel CLI는 `.vercel`이 있는 `server/`만 업로드해서 `../features`가 배포본에 없다 — **AI 라우트가 만든 이래 배포된 적이 없었고**(404) 로컬 빌드·e2e·typecheck가 전부 통과해 7일간 안 보였다. 레포 루트 배포는 대시보드 설정 2개에 의존하고 Expo 소스 전체를 매번 올린다. §8의 *"복사해서 쓴다"* 규약을 쓰되 **`check:shared`가 드리프트를 잡는다** — 라우트 주석이 막으려던 것은 복사 자체가 아니라 *"**조용히** 갈라지는 것"* 이었다 (`scripts/sync-shared.mjs`) |
| 2026-08-18 | 🔴 **리포트 삭제는 묘비다** — 본문만 지우고 `(kind, period_key)`는 남긴다 | 하드 삭제가 **로컬(리포트의 진실)과 서버 캡(`uq_ai_usage_period`)을 갈라놨다.** 지운 뒤 기간 시트가 그 주를 다시 고르게 하고, 서버가 `cap-exceeded`로 막고, 화면은 *"이미 있어요"* 라고 **거짓말**했다 — 되돌릴 수도 없다. ~~삭제를 막는다~~는 증상만 가리고(재설치·기기 2대에서 재발) 기둥 3·§11.3(*"만든 것은 그의 것"*)과 어긋난다. `diaries`의 `emptyBodyIfDeleted`가 이미 쓰는 패턴을 한 테이블에 더 적용한 것뿐이다 ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §11.9) |
| 2026-08-18 | 🔴 **리포트 기간을 고른다 — 지평은 작년 1월 1일** | `targetPeriodKey()`가 `now`에서 하나만 유도해 **놓친 주는 영영 못 만들었다.** 8/16 결제자는 8/1~8/9을 잃고, 3년 무료로 쓰다 결제하면 주간 4개가 전부다(월간은 그 달 주간이, 연간은 그 해 월간이 필요해 **연쇄로 막힌다**). 2027-01의 2026 연간이 8~12월만 든 채 굳는 것이 결정적이었다. **서버는 처음부터 허용하고 있었다** — 캡이 시계가 아니라 `uq_ai_usage_period`(기간 키 UNIQUE)라 *"평생 1번"*이고, 그래서 반복 착취 경로가 없다(최악 ₩117 = 한 달 실수령의 4%). ~~rolling 1년~~이 아닌 이유는 2027-03에 2026-01 월간이 지평 밖으로 나가 **같은 병이 재발**하기 때문 ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §6.4) |
| 2026-08-18 | 🔴 **상위 리포트는 하위가 다 모인 뒤에 만든다** | `canCreate`가 하위 **0개인지만** 봐서 주간 2개짜리 8월 월간이 만들어지고 **영구히 굳었다**(재생성 없음). 특히 W36(8/31~9/6)은 9/7부터인데 8월 월간은 9/1부터라 **9/1~9/6에 누르면 반드시 빠진다**. 연간은 12월 월간과 같은 날 열려 더 크다. **안 끝난 기간은 차단**하고 **안 만든 기간은 확인**한다 — §10.1과 같은 규약. 백필(§6.4)이 있어야 이 경고가 **행동으로 이어진다** |
| 2026-08-19 | ✅ **AI 사업자 고지 완성 — `OpenAI OpCo, LLC` · `dpo@openai.com`** | §28-8② 3호 연락처가 비어 있어 `check:ai`가 출시를 막고 있었다. 8/12에 비워둔 이유는 공식 페이지가 **403**이었고 *"법정 고지에 틀린 연락처를 적는 건 안 적느니만 못하다"* 였는데, **403은 `WebFetch`에서만 난다 — 브라우저로는 읽힌다.** 원문 실측 결과 ① 법인명이 브랜드 `OpenAI`가 아니라 **`OpenAI OpCo, LLC`**(1호는 *법인명*을 요구) ② **지역마다 법인이 다르다** (EEA는 `OpenAI Ireland Limited`) ③ 연락처는 지정된 책임자 `dpo@openai.com`이 *"이전받는 자의 연락처"* 라는 문언에 가장 곧다. ⚠ 이건 **우리 연락처가 아니다** — 사용자가 OpenAI에 직접 요구하는 창구이고, 휘성게임즈 연락처는 전자상거래법 §13①로 따로 들어간다 |
| 2026-08-19 | 🔴 **Play는 결제를 90초짜리로 발급하고 17분 뒤 확정한다 — 권한 창과 안내 창을 나눈다** | 첫 실결제 실측: `14:07 INITIAL_PURCHASE → 만료 14:07:51`(**90초**) → `14:24 RENEWAL → 만료 9/19`. 그 사이 17분은 **Play·RC·서버가 전부 "만료"라고 답하는 것이 정상**이다. ⚠ 나는 이걸 **서버 버그로 오진**해 리포트를 넘겼는데, `purchase_events.raw`의 웹훅 원문이 처음부터 90초였다 — **파생 상태만 보고 원문을 안 봤다.** 그리고 내가 제안한 *"최소 주기 가드"* 는 거절된 것이 옳았다(`PRODUCT_CHANGE`·조기 만료에서 더 긴 옛 값을 붙들어 **과다 지급**). → 조각은 **권한 창 3분 / 안내 창 30분**으로 나눈다. 합치면 둘 중 하나가 반드시 틀린다 — 권한을 17분 열면 근거 없는 부여, 안내를 3분에 닫으면 **방금 돈 낸 사람에게 "구독하면 이용할 수 있어요"**. `confirmWithServer` 백오프도 3분 → 25분 ([`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.8) |
| 2026-08-19 | 🔴 **웹훅은 약속이 아니다 — 서버가 "없다"고 하면 RC에 되묻는다** | 구독 전수조사. 공식 수치: 정상 도착 **5~60초**, **취소는 약 2시간**, 실패 시 5·10·20·40·80분 재시도 후 **포기**(실측 6시간+ 지연 사례). 그런데 common_server의 입력은 **웹훅 push 하나뿐**이라 유실되면 그 사용자는 **영구히 `pro`가 아니다** — RC 공식 권고는 *"웹훅을 받으면 `GET /subscribers`를 호출하라"* 인데 그 경로가 없다(**A1, common_server라 수정 금지**). 앱에서 할 수 있는 것: ① **포그라운드 복귀 시 `refresh()`**(전엔 마운트 1회뿐이라 앱을 재시작해야 반영됐다) ② 서버 게이트 기능은 낙관 구간에 **"확인 중"** 으로 말한다 ③ **서버가 "없다"고 할 때만 RC `CustomerInfo`에 되묻고, RC가 "있다"면 로컬 혜택(광고 제거)은 연다** — §4 불변식을 이만큼 수정한다. fail-closed를 안 깨는 이유는 RC가 **스토어의 기록**이지 우리 추측이 아니기 때문이다. ⚠ 그래도 AI·백업은 안 열린다(서버가 막는다) ([`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.7) |
| 2026-08-19 | 🔴 **결제 성공 직후에는 낙관 구간을 연다 — RC 성공 ≠ 서버 권한** | 앱이 *"구독이 시작됐어요"* 를 띄운 직후 설정은 `이용 안 함`이고 광고도 그대로였다. `purchase()`가 *"낙관적으로 켠다"* 는 주석을 달고 **`refresh()`(서버 조회)** 를 불렀는데, 서버는 **RC 웹훅이 와야** `pro`를 안다 — 결제 직후엔 거의 항상 모르고, 그러면 `false`를 쓰고 캐시까지 지운다. **주석이 코드와 반대였다.** 샌드박스라 드러난 게 아니라 **운영에서도 웹훅 지연만큼 같은 창**이 열리고, 거기가 환불·문의로 직행하는 자리다. → `grantPending()`(3분 낙관) + `refresh()` 가드 + 백오프 재조회. ⚠ 낙관은 **메모리에만** 둔다(캐시에 쓰면 웹훅이 안 와도 한 달 무료가 되어 fail-closed가 뒤집힌다) ([`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.6) |
| 2026-08-19 | 🔴 **stg 구글 로그인 코드 10 — 서명 지문은 콘솔이 아니라 기기에서 잰다** | OAuth 클라이언트는 **이미 있었고 패키지명도 맞았다.** 등록된 SHA-1이 Play Console `기존 키`였는데 **Play가 배포하는 빌드는 아직 `이전 앱 서명 키`로 서명**하고 있었다 — 그리고 콘솔은 이전 키 지문을 **화면에 안 보여준다.** 그래서 콘솔끼리 대조하면 영원히 못 찾는다. `adb pull` + `apksigner verify --print-certs`로 실측해 `D6:D9…`를 얻고 클라이언트를 추가해 해결(기존 `BE:D3…`는 전환일을 위해 유지). ⚠ `keytool -printcert -jarfile`은 **v1 전용**이라 APK에 `Not a signed jar file`을 뱉는다 — AAB에는 통해서 더 헷갈린다. ⚠ 재빌드는 필요 없었다 — `google-services.json`을 안 써서 번들에 든 것은 `webClientId` 하나뿐이다 ([`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.5) |
| 2026-08-20 | 🔴 **스토어 등록정보는 비공개 테스트에도 필수다** — §14의 *"프로덕션 출시 전 필수"* 가 틀렸다 | 내부 테스트는 없어도 올라가서(v7이 그랬다) 프로덕션용인 줄 알았다. 비공개부터는 *"전체 설명을 추가하세요"* 로 막히고, `eas submit`도 `releaseStatus: completed`를 *"missing required metadata"* 로 거부한다. → **`draft`로 번들만 먼저 넣고** 등록정보를 채운 뒤 게시하면 병렬로 간다. ⚠ 12명·14일 시계의 **선행 조건**이라, 스크린샷·그래픽을 미루면 시계가 그만큼 늦게 돈다 ([`MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.9) |
| 2026-08-20 | 🔴 **청구 통화는 우리 가격이 아니라 구매자 계정을 따른다** | 첫 실결제가 `USD 2.81`로 찍혀 가격 설정을 의심했는데 **우리 설정은 정상이었다**(`KRW 3,900` · 한국 1개국). 결정적 증거는 주문의 **예상 수익 `KRW 3,313` = ₩3,900 × 0.85** — Google은 ₩3,900을 상품가로 잡아 정산했고 달라진 건 **카드 청구 통화**뿐이다. `테스트:` 주문이 전부 KRW인 것도 같은 설명이다(테스트는 설정가를 그대로 보여주고 실결제만 환산된다). ⚠ **여기서 요금제를 고치면 멀쩡한 것을 망가뜨린다.** ⏭ 트랙 국가 ≠ 상품 국가라 출시 국가를 넓힐 때 **요금제 국가도 같이** 넓혀야 한다 ([`MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.10) |
| 2026-08-20 | **비공개 테스트는 한국만 · 정식 출시는 EEA·영국·스위스 제외** | EEA 제외의 근거가 **광고 하나인 줄 알고** *"테스트 광고면 넣어도 된다"* 로 뒤집으려다 잡혔다. 실제로는 셋이고 둘은 광고와 무관하다 — **처리방침이 GDPR을 안 다루고**, **AI 국외이전 고지의 법인명이 EEA에서 `OpenAI Ireland Limited`로 갈라진다**(§12 2026-08-19이 이미 적어놨다). EEA 사용자가 리포트를 한 번 만들면 §28-8 고지가 틀린 법인명을 표시한다. ⚠ 그래서 **UMP만 붙여서는 EEA가 안 열린다.** 비공개 트랙 국가는 프로덕션과 **동기화되지 않아**(콘솔 실측) 한국만 골라도 출시가 묶이지 않는다 — 대신 출시 때 145개국을 **손으로** 골라야 한다(대륙 묶음이 없다, §9.1) |
| 2026-08-19 | **제목은 다시 선택이다** — 2026-08-08 필수화를 되돌린다 | 필수화의 근거(*"목록에서 알아보는 단서"*)는 **읽는 쪽 편의**인데 대가는 **쓰는 쪽 마찰**이라 기둥 1이 이긴다. 목록은 날짜·감정·본문 앞부분을 이미 갖고 있어 단서가 사라지지도 않는다. ⚠ 되돌림이 **UI 한 줄**로 끝난 이유는 필수이던 동안에도 `title` 컬럼이 NULL을 허용하고 화면 4곳이 `title !== null`로 가려 그렸기 때문이다 — 강제를 데이터 계층에 박았으면 마이그레이션이 필요했다 ([`docs/DIARY_SYSTEM.md`](./docs/DIARY_SYSTEM.md) §1) |
| 2026-08-21 | **기록 리마인더를 로컬 알림으로 구현.** 푸시(FCM)는 여전히 안 한다 | 2026-08-07에 *"화면만"* 으로 비워둔 항목이 **토글 아래 "준비 중이에요"** 로 남아 있었고, 비공개 테스트로 41명이 동시에 켜는 시점에 그게 첫인상이 된다. 🔴 이 기능의 전부는 **"이미 쓴 사람에게 안 가는 것"** 인데, 로컬 알림은 **발송 시점에 조건을 볼 수 없다** — 그래서 조건이 확정되는 **저장 시점에 예약을 지운다**(방향을 뒤집는다). ⚠ 반복 트리거를 안 쓰는 이유가 이것이고(하나짜리라 "오늘 것만" 취소가 불가능), 1건만 예약하지 않는 이유는 **알림 뒤 앱을 안 열면 체인이 끊기는데 그게 주 시나리오**라서다 → **7일 롤링 창** ([`docs/NOTIFICATION_SYSTEM.md`](./docs/NOTIFICATION_SYSTEM.md)) |
| 2026-08-21 | 🔴 **`expo-notifications` 하나가 권한을 13 → 37로 늘렸다 — 21개를 걷어내 16으로** | 필요한 것은 `POST_NOTIFICATIONS`·`RECEIVE_BOOT_COMPLETED` **둘뿐**인데 런처 배지 20개와 FCM 푸시가 딸려 왔다. `INSTALL_SHORTCUT`·런처 설정 읽기/쓰기가 **일기 앱** 권한 목록에 뜨는 그림이라 §7.1(*"권한은 빼는 쪽으로"*)이 정확히 막으려던 것이다. ⚠ 처음엔 **릴리스(전) vs 디버그(후)** 로 비교해 답이 오염됐다 — 릴리스끼리 다시 쟀다. ⚠ `BIND_GET_INSTALL_REFERRER_SERVICE`는 **안 지웠다**: AdMob 어트리뷰션이 쓸 수 있는데 확인하지 못했고, **광고 수익에 닿는 것을 "안 쓸 것 같다"로 지우지 않는다** |
| 2026-08-21 | 🔴 **업로드 서명을 config plugin으로 옮긴다**(`plugins/with-upload-signing.js`) | v8은 `android/app/build.gradle`과 `gradle.properties`를 **손으로 고쳐** 구웠는데, `android/`는 CNG 산출물이라 **네이티브 모듈이 하나만 늘어도 `prebuild`가 그 둘을 함께 날린다.** 알림이 정확히 그 경우였다. 안 옮겼으면 다음 릴리스가 **디버그 키로 서명되거나 깨지는데 둘 다 20분을 태운 뒤에 드러난다.** ⚠ 앵커 정규식이 **CRLF 때문에** 조용히 안 맞아 한 번 던졌다 — 실패가 시끄러웠던 것이 이득이었다(조용히 넘어갔으면 서명 없이 구웠다) |
| 2026-08-21 | 시각 표기는 번역이 아니라 **틀**(`date.timeOfDay`) — 일본어는 24시간제 | 자리표시자 4개(`hour24`·`hour12`·`minute`·`meridiem`)를 주고 언어가 쓸 것만 쓰게 한다(§9.1 규칙 3과 같은 규약). 🔴 **일본어를 12시간제로 뒀다가 자정에 틀렸다** — `formatTimeOfDay()`가 0시를 `hour12=12`로 접는데 ko `오전 12:30`·en `12:30 AM`·zh `上午12:30`은 맞고 **일본어만 `午前0:30`이 맞다.** 언어별 0-vs-12 분기를 코드에 넣는 대신 **일본어를 24시간제로** 바꿨다 — 일본 표준 표기이고 분기가 하나도 안 는다 |
| 2026-08-23 | 🔴 **처리방침 예고 둘을 본문으로 승격했다 — 배포본과 고지가 어긋나 있었다** | v8·v9에 백업과 AI가 들어 있는데 본문은 여전히 *"일기를 서버로 보내지 않으며"* 였다. §5.1이 *"올리기 전에 고친다. 올린 뒤에 고치면 그 사이가 위반"* 이라 적어둔 그 항목을 **우리가 어긴 상태**였고, Play 데이터 보안 폼보다 이것이 먼저다 — 폼이 "수집함"인데 링크된 방침이 "수집 안 함"이면 그 자리에서 어긋난다. 붙여넣기가 아니라 13개 절에 엮었고 `DELETE_ACCOUNT`는 **§5 제목부터** 고쳤다(*"삭제 대상이 아닌 것"* 은 백업을 켠 사람에게 첫 줄부터 거짓이다). 🔴 **백업과 AI가 한 문서에서 만나게 되어** §1에 *"백업은 저장하되 못 읽고, AI는 읽되 저장 안 한다"* 를 **명시**했다 — 예고로 떨어져 있을 땐 안 부딪혔지만 이제는 안 적으면 독자가 뭉갠다. 30일은 공개 사용자 0이라 해당 없다(§12 2026-08-13) |
| 2026-08-23 | **`legal:stamp`가 세 문서를 다 찍는다** — 여태 PRIVACY 하나였다 | `/sourceFingerprint: '...'/`가 **non-global**이라 파일의 첫 지문만 바뀌고 `DELETE_ACCOUNT`·`TERMS`는 영영 낡은 채였다. 그런데 `check:legal`은 **셋을 각각** 대조한다 — **도구가 만들 수 없는 상태를 검사가 요구**하고 있었다. 승격 때 번역 에이전트 셋이 *각자* 같은 곳에 걸려 손으로 넣은 것이 증거다. 손으로 넣는다는 건 언젠가 틀린 값을 넣는다는 뜻이고, 지문이 틀리면 **낡은 번역이 최신으로 보인다** — 아무 장치도 없는 것보다 나쁘다. ⚠ 등장 순서가 아니라 `export const <PREFIX><LANG>` 선언 블록으로 찾는다 |
| 2026-08-23 | **온보딩 화면을 세우지 않는다 — 홈의 빈 상태가 그 자리다** | 3장짜리 안내는 쓰러 들어온 사람 앞에 문을 하나 더 다는 일이라 기둥 1과 정면으로 부딪힌다. 대신 빈 상태에서 ① **없는 목록의 제목**(`최근의 조각들`)을 감추고 ② 카드를 벗겨 가운데로 모으고 ③ **본문을 CTA에서 설명으로** 바꿨다 — 옛 문구 *"오늘의 첫 조각을 남겨보세요"* 는 바로 위 버튼의 반복이라 한 줄을 쓰고 아무것도 말하지 않았다. ⚠ 빈 상태에 **버튼을 넣지 않는다**(CTA는 하나) ([`docs/DIARY_SYSTEM.md`](./docs/DIARY_SYSTEM.md) §8.2) |
| 2026-08-23 | 🔴 **리포트 생성 중에는 화면을 덮는다 — 취소는 없다** | 버튼 라벨만 바뀌어 **5초 남짓 눌렸는지 알 수 없었다.** 더 큰 이유는 이탈 방지다 — `createReport()`가 **서버 응답 뒤에** 로컬 저장이라 그 사이 앱이 죽으면 캡(`uq_ai_usage_period` = 평생 1번)만 소모되고 리포트가 없다. 재생성도 회수 라우트도 없어 **그 기간을 영구히 잃는다.** 🚫 취소 버튼은 두지 않는다(모델이 이미 돌아 돈은 나가고 캡은 준다). ⚠ 그런데 **문구는 그 사실을 말하지 않는다** — 회수 경로가 없는 건 우리 미구현이고, 그 대가를 경고문으로 사용자에게 떠넘기지 않는다 ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §11.8) |
| 2026-08-24 | **텍스트 서식을 넣는다 — 정렬·크기·굵기·색. 단 색은 hex가 아니라 코드** | 사용자 요청(자주 쓰는 일기장의 `Tt`). 🚫 **글꼴 종류는 뺐다** — 한글 폰트는 글리프가 많아 Pretendard 실측 기준 굵기당 약 1.5MB라 몇 종만 넣어도 45MB APK가 눈에 띄게 붇고, 더 나쁜 것은 **한글 전용 글리프라 나머지 14개 언어에서 조용히 기본 글꼴로 떨어진다**(고를 수는 있는데 안 바뀐다, §9.1 규칙 5). 색을 **코드로 저장**하는 것이 이 결정의 핵심이다 — hex를 저장하면 라이트에서 고른 색이 **다크모드에서 안 읽히고**, 스킨을 갈아끼워도 **옛 조각만 옛 색으로 남는다**(감정을 코드로 저장한 것과 같은 이유). ⚠ `DIARY_SYSTEM §1.1`의 *"불릿 모양은 저장하지 않는다"* 와 부딪히는 것처럼 보이나 **다르다**: 불릿은 아무도 안 고른 렌더러 기본값이고 이건 **글쓴이가 고른 것**이다. 🔴 그리고 **서식을 적용하면 그 문단이 자기 블록으로 떨어진다** — 취향이 아니라 강제다. RN `TextInput`은 한 입력창 안에서 문단마다 다른 크기를 못 그린다 ([`docs/DIARY_SYSTEM.md`](./docs/DIARY_SYSTEM.md) §1.1) |
| 2026-08-24 | 🔴 **프로그램이 만든 블록 변경을 사용자 입력으로 읽지 않는다 — 커서 잠금** | 에뮬레이터 검증 중에 잡았고 **서식이 생기기 전부터 있던 버그**다. 블록을 프로그램으로 쪼개면 원래 입력창의 `value`가 짧아지는데 **RN은 그때도 `onSelectionChange`를 쏜다** — 그걸 믿으면 방금 옮겨 둔 커서가 도로 앞 블록으로 끌려간다. 실측: `AAAAA BBBBB` 가운데에 목록을 끼운 뒤 서식을 걸면 뒤가 아니라 **앞 문단**에 걸렸고, 같은 원리로 **사진을 연달아 두 장 넣으면 두 번째가 엉뚱한 자리**에 들어간다. → `onCaretChange`에 `focus`/`program`/`selection` 구분을 붙이고, 쪼갤 때 잠갔다가 **사용자가 칸을 다시 잡을 때만** 푼다. ⚠ 처음엔 *"포커스를 가진 칸의 보고만 받는다"* 로 고쳤는데 **안 됐다** — 목록 항목이 포커스를 가져가도 그건 텍스트 블록이 아니라 추적값이 낡은 채 가드를 통과했다. ⚠ **순수 계층 검사로는 원리적으로 못 잡는다**(RN 이벤트 타이밍이다). 억지 가드를 만들지 않고 실기기 확인 항목으로 남긴다 ([`docs/DIARY_SYSTEM.md`](./docs/DIARY_SYSTEM.md) §1.1) |
| 2026-08-24 | **실기기에는 dev 빌드를 넣을 수 없다 — 스토어 서명과 충돌한다.** 실기기 확인은 **트랙에 올린 빌드로** 한다 | 무선 디버깅을 켜고 붙였는데 막혔다. 기기의 `com.son0925.jogak`은 **Play가 서명**했고 로컬 dev 빌드는 **디버그 서명**이라 패키지명이 같아 덮어쓰기가 안 된다. 안드로이드에 우회가 없다 — 서명은 패키지 단위라 사용자 프로필을 나눠도 피하지 못한다. 남는 길은 셋뿐이고 **①만 무해하다**: ① `com.son0925.jogak.dev` 같은 **별도 패키지 변형**을 만들어 나란히 설치(=`stg`와 같은 수법, prebuild+gradle 10~20분) ② 기존 앱 삭제 후 설치 — 🚫 **그 앱의 일기가 영구히 사라진다**(실측 데이터 14.73MB) ③ 다음 AAB를 내부 테스트 트랙에 올려 Play로 받아 확인. **2026-08-24는 ③을 골랐다**(사용자 결정) — 어차피 doply 주기로 AAB를 올려야 해서 빌드가 중복되지 않는다. ⚠ 그래서 **에뮬레이터로 확인할 수 없는 것**(One UI 글꼴 크기 설정 · 실제 ARM의 Fabric 텍스트 측정 = §10 잘림 규칙)은 **v10 이후로 미뤄져 있다** |
| 2026-08-24 | 🔴 **Play 데이터 보안 — 3건 추가 · 삭제 배지 켜기 · 사진은 안 넣는다** | 일시처리 예외를 원문으로 확인했더니 두 가지가 예상과 달랐다. ① 예외의 뜻이 *"선언 안 함"* 이 아니다 — *"폼에는 **포함하고** 기준을 만족하면 공개 섹션에 안 뜰 뿐"* 이고 콘솔에는 `일시적으로 처리되나요?` **체크박스**가 따로 있다(같은 문서의 본문과 FAQ가 **정반대**라 보수적 독법을 쓴다). ② 그런데 조각의 AI는 **어차피 예외가 아니다** — 요약문 90일 · 벤더 30일 · `ai_usage` 영구, 셋 중 하나만으로도 깨진다. 반대로 **E2EE 예외는 백업에 그대로 걸려** `사진`을 선언하지 않는다(선언하면 스토어 카드가 E2EE 약속과 어긋나 보인다). §5.1의 옛 지시 *"일기 본문·사진을 추가"* 를 이만큼 좁힌다 — **본문은 AI 때문에 넣고, 사진은 AI를 안 지나가서 뺀다.** 🔴 그리고 §14가 못 잡은 **네 번째 구멍 `구매 내역`** 이 나왔다(`purchase_events`). ⚠ 기존 6개(AdMob 계열)는 **손대지 않는다** — 과다 선언은 제재 대상이 아니고, 좁히는 변경만 릴리스와 묶는다 ([`docs/PLAY_DATA_SAFETY.md`](./docs/PLAY_DATA_SAFETY.md)) |
| 2026-08-24 | 🔴 **고지가 코드를 앞질렀다 — 탈퇴가 AI 테이블을 안 지운다** | 데이터 보안 전수조사에서 나왔다. 8/23 승격으로 `PRIVACY §4`(*"리포트 이용 기록 … 탈퇴 시까지"*)와 `DELETE_ACCOUNT §3`(*"탈퇴하면 … AI 요약문과 이용 기록을 파기"*)이 **본문**이 됐는데, `deleteAccount()`는 백업 파기 → `DELETE /auth/me` → 구글 revoke → 로컬 정리 넷뿐이고 **`ai_reports`·`ai_usage`를 건드리지 않는다**(`purgeVault`가 import조차 안 한다 · `delete(aiUsage)` 레포 전체 0건). 요약문은 `subject_id`에 묶인 채 90일, 이용 기록은 **영구**로 남는다. ⚠ **백업 쪽은 *"계정보다 백업을 먼저"* 를 차단형으로까지 설계해놨는데 AI 쪽에 그 대칭이 없다** — 예고문이던 동안에는 미래형이라 거짓이 아니었고, **본문으로 올리는 순간 현재형 진술이 됐다.** §5.1이 잡으려던 것과 같은 종류인데 방향이 반대다(그땐 코드가 앞섰다). 🚫 Play 배지 조건은 `or`이라 **폼 블로커는 아니다** — 프로덕션 전까지 닫는다. ⚠ `ai_usage` 가명화는 `uq_ai_usage_period`(평생 1회 캡의 유일한 근거)를 깨지 않아야 하는데 **확인 안 됨** |
| 2026-08-24 | ✅ **그 거짓을 코드로 닫았다 — `POST /api/v1/ai/purge`** | 위 줄의 해소다. ~~`ai_usage`를 가명화한다~~ → **지운다.** 가명화를 검토한 이유는 `uq_ai_usage_period`(평생 1회 캡의 유일한 근거)를 지키려던 것인데, **탈퇴하면 `subject_id`가 영영 재사용되지 않으므로**(§7.2 `softDeleteSubject`) 그 행은 아무것도 막지 못한다 — 지킬 캡이 없는데 죽은 식별자만 남는다. 🚫 **`backup/delete`에 얹지 않았다**: 그 라우트는 탈퇴가 아닐 때도 쓰여서(백업만 파기) 거기 넣으면 **백업만 지우려던 사람의 리포트까지 사라진다** — 계정 범위와 금고 범위는 다르다. ⚠ **앱 안 삭제와도 다르다** — 그쪽은 묘비(`kind, period_key`)를 남기지만(2026-08-18) 탈퇴는 남기지 않는다. `e2e:ai`에 파기 4개를 더해 15개, **일부러 망가뜨려 실패하는 것까지** 확인했다 ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §7.1) |
| 2026-08-12 | 리포트 언어는 **설정에 전역 하나**(`ai_report_language`) | 생성은 1탭이어야 한다(기둥 1) — 만들 때마다 묻지 않는다. `null`/빈 값이 "앱 언어와 같음"이다. 앱 언어와 합치지 않는 이유는 **"앱 언어와 같음"과 "우연히 한국어를 골랐음"이 다르기** 때문 |

### 암호화 키 — 왜 이 선택인가 (2026-08-07)

| 안 | 판단 |
|---|---|
| 기기에만 저장, 복구 수단 없음 | 🚫 **자기모순**. 기기를 잃었을 때 쓰라고 만든 백업인데 기기를 잃으면 키도 잃는다 |
| 사용자 비밀번호에서 키 유도 | 🚫 구글 로그인만 쓰기로 해놓고 비밀번호를 하나 더 요구 — 기둥 1과 충돌 |
| 서버가 키 보관 | 🚫 E2EE가 아니게 되어 위 결정 전체가 무의미해짐 |
| **기기 SecureStore + 복구 코드 1회 발급** | ✅ **채택**. 우리는 키를 갖지 않고, 복구 경로는 사용자 손에 있다 |

- 약점("사용자가 복구 코드를 안 적어두면 같다")은 **설정에서 복구 코드를 언제든 다시 볼 수 있게** 해서 완화한다
  (기기에 키가 살아 있는 동안은 재발급 없이 조회 가능).
- 복구 코드 분실 = 백업 복원 불가임을 **백업을 켜는 화면에서 명시**한다. 나중에 CS로 오면 해줄 방법이 없다.

### ⚠ 미결정 (착수 전에 매듭)

1. ~~구독 상품 구성~~ → **확정(2026-08-10): 단일 구독 `조각 Pro`** · 월 ₩3,900 / 연 ₩29,000 ·
   7일 체험 · 엔타이틀먼트 키 `pro` 하나(§7.2).
   ⏭ 남은 것은 **구독 만료 후 백업 처리**(제안: 90일 유예) — 조각 서버 스키마에 영향을 준다.
2. ~~광고 빈도 캡 수치~~ → **확정(2026-08-08): 전면 하루 1회**(§7).
3. **백업 단위** — 전체 스냅샷 vs 변경분 증분. 복원 충돌(기기 2대) 처리 규칙.
4. **암호화 구현 수단** — RN에서 AES-256-GCM을 무엇으로 돌릴지(expo-crypto는 해시·난수 중심).
   착수 시 공식 문서로 확인한다 — 기억으로 단정 금지(DOC_DISCIPLINE §6).
5. ~~**LLM 제공자 처리위탁·국외이전 고지**~~ → ✅ **해소(2026-08-19)**.
   ⚠ **2026-08-12 조사는 Anthropic 기준이었다**(보관 7일). 벤더가 **8/13에 OpenAI로 바뀌었는데
   이 줄만 안 따라왔다** — 법적 문안(`features/legal/legal-text.ts`)은 **OpenAI 30일**로
   제때 고쳐져 있어 **고지 자체는 내내 정확했고**, 틀린 것은 결정 로그뿐이었다.
   그래도 **근거가 옛 벤더를 가리키면 다음 사람이 그 근거로 판단한다** — 그래서 고친다.
   학습에 안 쓰므로 **위탁으로 구성되고 §5가 유지**된다. Play 데이터 보안은
   **Collected · Optional, Shared 아님** — 위탁사 제외와 사용자 개시/동의 제외에 각각 걸린다.
   🔴 단 **그 제외는 조건부다** — 학습 미사용 · 눈에 띄는 고지와 동의 · 자동 생성 금지.
   근거와 원문은 [`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §9.2~9.4.
   ⏭ 남은 것은 사업자명·국가·연락처 기재. **값은 확정됐다(2026-08-19)** — `features/ai/vendor.ts`가
   `OpenAI OpCo, LLC` · `US` · `dpo@openai.com`을 갖고, 동의 화면(`app/ai-consent.tsx`)이 그걸 쓴다. **법인명이 지역마다 다르다** —
   EEA·스위스는 `OpenAI Ireland Limited`이고, 조각은 EEA 배포 제외(§9.1)라 그 외 지역이 맞다.
   ⏭ UMP로 EEA를 열면 **이 값이 갈라져야 한다.**
6. **로컬 DB 암호화 여부** — 잠금(UI 게이트)만으로는 루팅 기기·기기 백업 추출을 막지 못한다(§7.1).
   SQLCipher류 도입 여부와 그 키를 잠금 수단과 묶을지(PIN에서 유도 vs 별도 키).

---

## 13. 선행 의존 (조각 밖 작업)

MVP를 이 범위로 내려면 **common_server 쪽 작업이 먼저** 끝나야 한다.

MVP를 이 범위로 내려면 **common_server 쪽 작업이 먼저** 끝나야 한다.
로컬 기능(일기·잠금)은 이 목록과 무관하게 굴러간다 — 로그인이 선택이기 때문이다(§4).

| 선행 | 내용 | 조각의 무엇이 막히나 | 누가 |
|---|---|---|---|
| common_server **Phase 7** | `subjects` + 토큰 발급 + `tickets.subjectId` | 구글 로그인 · 문의 · 구독 · 백업 | 사용자/나 |
| **dev build 전환** | Expo Go로는 구글 로그인이 안 된다(네이티브 코드) | 위와 동시에 필요 | 나 |
| **Google Cloud OAuth 클라이언트** | Android·iOS·Web 클라이언트 ID 발급, SHA-1 등록 | 구글 로그인 | **사용자**(콘솔 계정 필요) |
| common_server **Phase 9** | RevenueCat 엔타이틀먼트 + 웹훅 | 월 구독 · 광고 제거 | |
| common_server `apps` 등록 | `node tools/seed.ts jogak "조각"` | 공지 · 문의 | |
| 조각 서버 신규 | Next.js + Supabase 프로젝트 | 백업/복원 · AI 리포트 | |

---

## 14. 현재 상태 (2026-08-12)

**세부는 [`docs/README.md`](./docs/README.md) 현황표가 정본이다.** 여기는 요약만 둔다.

- ✅ 로컬 기능 전부 — 홈·작성/수정·상세·캘린더·검색·모든 조각·설정·다크모드·15개 언어
- ✅ 앱 잠금(PIN·패턴 + 힌트 되찾기 + 초기화 + 앱 스위처 가림). ~~생체~~ 제거(§7.1)
- ✅ 공지·문의·구글 로그인·탈퇴 — `bootstrap?app=jogak`이 **200**이다(2026-08-11 실측)
- ✅ **문의 내역·답변**(2026-08-13) — 로그인을 요구해 놓고 답변을 볼 화면이 없었다.
  서버 라우트와 SDK는 둘 다 있었고 앱만 안 불렀다(`docs/SUPPORT_SYSTEM.md` §5.5)
- ✅ 광고(AdMob) + **구독자 광고 제거**(엔타이틀먼트 연결)
- ✅ **백업/복원 — 텍스트 1차.** 앱 암호 계층·매니페스트·조각 서버·화면까지.
  [`docs/BACKUP_SYSTEM.md`](./docs/BACKUP_SYSTEM.md)
- ✅ **Play 구독 상품 등록**(2026-08-12) — `jogak_pro` 월 ₩3,900 / 연 ₩29,000 + 체험 7일, 전부 활성
  ([`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.2)
- ✅ **AI 리포트 — 앱 쪽 전부**(2026-08-12). 리포트 탭(검색 탭 자리)·상세·홈 카드·설정(리포트 언어)·
  DB v5·백업 매니페스트 포함. 🔴 **서버 라우트가 없어 [만들기]는 아직 실패한다**
  ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §11–§12)
- ✅ **사진 백업(2차)** · 되찾기(`rebind`)·파기(`delete`)·리퍼(`cron/reap`) 라우트
  — 2026-08-13 정정. 이 줄이 ❌로 남아 있었다(`docs/README.md` §2가 정본이고 그쪽은 맞았다)
- ✅ **RevenueCat 배선 완료**(2026-08-17) — 운영·stg 두 앱에 `jogak_pro` 월·연 import →
  `pro` attach → 오퍼링 `default`의 `$rc_monthly`·`$rc_annual` 연결.
  🔴 그 과정에서 **운영 앱조차 깨져 있던 것을 발견**했다: Play에 상품이 멀쩡히 있는데
  RC에는 0개였고 `pro`에 붙은 2개는 **Test Store 상품**이었다 — 결제해도 권한이 안 붙는
  상태였고 §7.2 함정 #2가 실제로 걸려 있었다. `npm run check:play-access`로 진단한다
- ✅ **백업 기기 검증**(2026-08-11 에뮬레이터 15/18) — 5MB PUT 바이트 일치 · `backupDatabaseAsync` 9ms.
  ⏭ 암호 처리량만 실기기(ARM) 재측정이 남았다. 순수 JS 유지로 이미 결론냈다
- ✅ **AI 서버** — 라우트·벤더 경계(OpenAI)·캡·잠금·`ai_usage`·`ai_reports` + **탈퇴 파기**. `e2e:ai` 15개 통과
- ~~🔴 **AI 사업자 연락처 미기재** — 출시 차단~~ → ✅ **해소**(2026-08-19). `features/ai/vendor.ts`가
  `OpenAI OpCo, LLC` · `US` · `dpo@openai.com`을 갖고 `docs/privacy.html` 15개 언어에 들어갔다.
  `check:ai`가 통과한다 — 이 줄이 며칠 더 🔴로 남아 **없는 블로커를 크리티컬 패스에 올려놨었다**
- ✅ **AI 실호출 확인**(2026-08-13) — `gpt-5.6-luna` · `AI_EFFORT=medium` 확정.
  **원가가 계획의 1/4**(리포트 1건 ₩0.9, 매출 대비 0.15%)이고 위기 판정도 정확했다
  ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §4.2.1)
- 🔴 **AI 라우트는 2026-08-18까지 배포된 적이 없었다** — `@shared/*`가 `../features/*`를
  가리켰는데 **Vercel CLI는 `server/`만 업로드**한다. 로컬 빌드·e2e·typecheck가 전부 통과해
  7일 동안 안 보였고, 배포본에서 `POST /api/v1/ai/report`가 **404**였다. 순수 계층을
  `server/shared/`로 **생성 복사**하고(`sync:shared` + `check:shared` 드리프트 검사)
  배포하니 **401**이 됐다. ⚠ 아래 "라우트 전 구간 성공"은 전부 **localhost** 기준이다
- ✅ **라우트 전 구간 성공**(2026-08-14, **로컬**) — `POST /api/v1/ai/report`가 게이트·캡·잠금·멱등을
  지나 OpenAI를 부르고 `ai_usage`·`ai_reports`까지 썼다. ⚠ **의도한 실행이 아니었다** —
  `e2e:ai`의 한 검사가 *"키가 없다"* 를 전제하고 있어 매 실행마다 실호출을 내고 있었고,
  200을 받고 실패하며 드러났다. 같은 커밋에서 **키가 있어도 모델을 안 부르도록** 고쳤다
  ([`docs/AI_REPORT_SYSTEM.md`](./docs/AI_REPORT_SYSTEM.md) §12)
- ✅ **앱에서 [만들기] 확인**(2026-08-14 에뮬레이터) — 동의 → 빠진 날 확인 → 생성 →
  로컬 저장 → 상세 렌더까지 전부. 캡도 두 번째 시도를 막았다.
  🔴 화면에서 **`1 entries`** 를 잡았다 — 6개 언어가 수 일치를 요구하고 있었다.
  `check:i18n`이 못 잡는 종류다(`docs/I18N_SYSTEM.md` §4.5)

### 🔴 비공개 테스트 제출 완료 (2026-08-20)

운영 앱(`com.son0925.jogak` · 앱 ID `4973120282466573971`)에서 **프로덕션 액세스 요건**
(12명 이상 · 14일 이상, `MONETIZATION_SYSTEM.md` §6.1.2)을 채우기 위한 비공개 테스트를 제출했다.

| | |
|---|---|
| 트랙 | **비공개 테스트 - Alpha** (`4699161911571044727`) — 이미 있었다 |
| 빌드 | **versionCode 8** · 로컬 릴리스 AAB · 운영 업로드 키 `B9:A7:29…` (지문 대조 통과) |
| 테스터 | `doply` 41명 + `조각 내부 테스트` 2명 |
| 국가 | 대한민국 |
| 스토어 등록정보 | 아이콘 · 그래픽 이미지 · 스크린샷 4장 (문구는 기존 것 유지) |
| 상태 | **Google 검토 중** (안내상 보통 7일 이내) |

⚠ **EAS 무료 빌드 쿼터가 소진**(9/1 리셋)이라 로컬로 구웠다. 운영 앱 업로드 키는 stg 키
(`3A:47…`)와 **다르다** — `secrets/jogak-prod-upload.jks`로 따로 받았다. 섞으면 업로드가 거부된다.
⚠ 서비스 계정 권한은 **켰다 끈다**를 지켰고 `프로덕션으로 출시`는 한 번도 켜지 않았다.

🔴 **데이터 보안 선언을 맞추지 않은 채 올렸다**(사용자 결정). 이 빌드에는 백업·AI가 들어 있는데
선언에는 `User IDs`가 빠져 있고 `데이터 삭제 지원 = 아니요`다. §5.1이 *"올리기 전에 고친다.
올린 뒤에 고치면 그 사이가 위반"* 이라고 적어둔 그 항목이다 — **프로덕션 전에 반드시 닫는다.**
~~⏭ 선행 작업: Play의 *"일시 처리 데이터"* 예외 조건을 **공식 문서로** 확인(§5.1, 기억으로 단정 금지).~~
→ ✅ **선행 작업이 전부 끝났다**(2026-08-24). 일시처리·E2EE·공유 제외 원문 실측 완료, 처리방침
승격 완료(2026-08-23), 선언값 확정 완료. **남은 것은 사용자가 콘솔에서 누르는 것뿐**이고
넓히는 변경이라 새 AAB를 기다리지 않는다 — 미루는 하루가 그대로 불일치 기간이다
([`docs/PLAY_DATA_SAFETY.md`](./docs/PLAY_DATA_SAFETY.md) §0·§5·§7).

⚠ **미출시다.** 남은 것은 대체로 외부 대기다.

### 🔴 Play 콘솔 계정이 바뀌었다 — 아래 실측치는 **옛 계정 것이다** (2026-08-14)

`bjpio113@gmail.com` → **`sonwheesung925@gmail.com`.** 앞으로의 업로드는 전부 새 계정이다.

→ **정정(2026-08-20)**: 개발자 인증 ✅ · **결제 수단 확인 ✅**(2026-08-18) · **앱 이전 ✅ 완료**(2026-08-19,
배구명가 등 8개 앱 전부). 정본은 [`C:\project\common\BUSINESS_INFO.md`](file:///C:/project/common/BUSINESS_INFO.md) §4이고
**여기에 다시 적지 않는다** — 두 곳에 적어서 이 줄이 이틀 낡았다.

**이전 승인을 기다리지 않는다** — 새 계정에 **조각 stg 앱을 따로 만들어** 내부 테스트를 돌리고,
운영 앱은 이전 완료 후에 쓴다(2026-08-14 사용자 결정). ⚠ 앱이 둘이면 **패키지명 · AdMob 앱 ID ·
구글 OAuth 클라이언트(SHA-1)** 가 두 벌이 된다 — 상세와 미결정은
[`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.1~§6.1.2.

⚠ **아래 표는 옛 계정에서 잰 값이다.** 이전은 재등록이 아니라 옮기는 것이라 대부분 따라올
가능성이 높지만 **확인 전까지 유효하다고 치지 않는다.** 승인 후 항목별로 다시 본다.

### 2026-08-12 실측으로 지워진 블로커 (⚠ 옛 계정 기준)

| 것 | 결과 |
|---|---|
| 🔴 **Play 계정 유형 — 되살아났다** | ~~✅ 조직 계정이라 해소~~ → **최종은 개인 계정이다**(2026-08-17, `common/BUSINESS_INFO.md` §4). 조직 전환은 8/10 완료됐으나 **Google 결제팀이 개인사업자의 조직 결제 프로필을 불인정**해 → 개인 유형 신규 계정(`6329667596149711059` · Vivace Games Studio) + 앱 이전으로 선회했다. **비공개 테스트 요건이 다시 걸린다** — 사용자가 직접 처리한다(2026-08-17 사용자 결정). ~~⚠ 인원·기간 수치는 확인 안 함~~ → **콘솔 실측: 12명 이상 · 14일 이상**([`MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) §6.1.2) |
| ~~Play 상품 생성이 막힘~~ | ✅ 빌링 권한이 든 v7을 올리자 열렸다. **결제 프로필과 무관한 별개 관문이었다** |
| ~~데이터 보안이 "수집 안 함"일까~~ | ✅ 아니다. **6가지 유형**이 선언돼 있다(CSV 실측). 목록은 아래 |
| ~~앱 콘텐츠 미완료 항목~~ | ✅ 정책 선언 10개 전부 **조치됨**, `주의 필요` 0건. 대시보드의 10/11은 **`스토어 등록정보`** 하나이고 이건 프로덕션 출시용이다 |

**여전히 막고 있는 것**

| 것 | 누가 |
|---|---|
| ~~🟡 Play **앱 이전** · **결제 수단 확인**~~ | ✅ **둘 다 완료**(BUSINESS_INFO §4) |
| ~~🟡 **조각 stg 앱 생성** + 내부 테스트~~ | ✅ 완료. ⏭ **비공개 테스트는 운영 앱에서 돈다** — stg에서 14일을 채워도 운영 앱 시계는 0일이다(패키지가 다르면 다른 앱이다) |
| ~~통신판매업 신고~~ | ✅ **완료** — 2026-07-14 신고, 공정위 조회 실측(§7.2). 블로커 아님 |
| ~~RevenueCat 상품 import·attach~~ | ✅ **완료**(2026-08-17). 남은 것은 stg 재빌드 후 **실결제 확인**뿐 |
| ~~🔴 **AI 사업자 연락처**(§28-8② 3호)~~ | ✅ **완료**(2026-08-19) |
| 🔴 데이터 보안 **선언이 배포본보다 좁다** — `User IDs`·`구매 내역`·`기타 UGC` 미선언 + `삭제 지원 = 아니요`(아래) | ~~백업 릴리스에 묶는다(2026-08-12)~~ → **값 확정 완료**(2026-08-24). 넓히는 변경은 빌드를 기다리지 않는다 — **지금 콘솔에서 누르면 끝난다.** 클릭 순서는 [`PLAY_DATA_SAFETY.md`](./docs/PLAY_DATA_SAFETY.md) §7 |
| ~~🟡 **스토어 등록정보** — 프로덕션 출시 전 필수~~ | ✅ **완료**(2026-08-20). ⚠ 그리고 **프로덕션 전용이 아니었다** — 비공개 테스트도 막는다(§12) |
| ~~백업·구독 릴리스는 2026-09-10 이후~~ | 🔴 **무효**(2026-08-13, §12) — 30일 자기구속은 해당 없었다 |

~~🔴 Play **결제 프로필** "문제가 있음"~~ → 계정이 바뀌어 그 진단은 옛 계정 것이다.
새 계정은 결제 수단 확인까지 **끝났다**(위).

### 데이터 보안 선언 실측 (2026-08-12 · CSV 내보내기)

| 선언된 유형 | 수집/공유 | 필수 | 목적 |
|---|---|---|---|
| 개인 정보 → **이메일 주소** | 수집만 | 선택 | 앱 기능 · 계정 관리 |
| 메시지 → **기타 인앱 메시지** | 수집만 | 선택 | 앱 기능 |
| 위치 → 대략적 위치 | 수집+공유 | 필수 | 광고 |
| 앱 성능 → 진단 | 수집+공유 | 필수 | 분석 |
| 앱 활동 → 앱 상호작용 | 수집+공유 | 필수 | 분석·부정행위 방지·광고 |
| 기기 또는 기타 ID | 수집+공유 | 필수 | 분석·부정행위 방지·광고 |

부가: `계정 생성 방법 = OAuth` ✅ · `전송 중 암호화` ✅

🔴 **`User IDs`(`PSL_USER_ACCOUNT`)가 빈칸이다 — 과소 선언이다.** 구글 로그인이 ID 토큰의 `sub`를
common_server로 보내고 서버가 저장한다. Play의 정의(*"계정 ID·계정 번호·계정 이름"*)에 해당하고,
§5.1이 이미 *"User IDs | Collected | **지금** + 백업"* 이라 적어놨으며 `DELETE_ACCOUNT §3`은
**구글 sub를 삭제 대상으로 명시**한다 — 저장한다는 것을 우리 문서가 이미 인정한다.
⚠ 반대로 위치·진단은 다소 넓게 잡혀 있는데 **과다 선언은 제재 대상이 아니다.** 건드리지 않는다.

⚠ `데이터 삭제가 지원되지 않습니다`로 되어 있다(계정 삭제 URL은 제공). **앱에는 탈퇴가 있으므로
사실과 다르다.** 축소 선언이라 위반은 아니지만, 백업이 붙으면 §5.1의 *"삭제 요청 가능"* 전제와 충돌한다.

→ **둘 다 백업 릴리스에서 함께 고친다**(2026-08-12 사용자 결정). 지금 마법사를 건드리지 않는 이유는
법적 성격의 제출이고 릴리스와 선언이 어긋나는 창을 만들지 않기 위해서다.
