import { View } from 'react-native';

/**
 * 가운데 + 버튼 **자리만** 차지하는 파일.
 *
 * expo-router 는 탭을 파일로 만드는데, 이 탭은 머무는 화면이 아니라 작성 화면을 여는 동작이다.
 * 그래서 **누름은 `_layout.tsx` 의 `tabPress` 가 가로채고 여기로는 오지 않는다.**
 * 이 컴포넌트가 그려지는 상태는 정상 경로에 없다.
 *
 * 🔴 ~~포커스를 받으면 `router.back()` 으로 되돌리고 `push('/write')` 를 부른다~~ (2026-09-10 제거).
 * 그 방식은 **두 번을 못 버텼다** — 실기기에서 오늘 조각이 있는 상태로 + 를 누르고(날짜 시트가 열린다)
 * 뒤로 나갔다가 다시 + 를 누르면 작성 화면이 안 뜨고 **빈 탭**만 남았다.
 * 되돌리기와 띄우기를 한 틱에 같이 했기 때문이고, 결과가 그 순간의 히스토리에 달려 있었다.
 * 고칠 곳은 **되돌리는 방법이 아니라 애초에 오지 않게 하는 것**이었다.
 *
 * ⚠ 오늘 조각이 있어도 **여기는 상세로 보내지 않는다**(2026-08-08). 보내버리면 지난 날짜로
 * 쓰러 들어갈 문이 사라진다 — 이 버튼은 '새 조각'의 입구다. 오늘이 막혀 있으면 작성 화면이
 * 날짜 시트를 먼저 열어 다른 날을 고르게 한다.
 */
export default function WriteTab() {
  return <View />;
}
