/**
 * AI 사업자 정보 — **한 곳에서만 정의한다.**
 *
 * 「개인정보 보호법」 §28-8②는 국외 이전 동의를 받기 전에 **3호까지** 알리도록 요구한다:
 * 이전받는 자의 성명(법인명)과 **연락처**, 이전 국가·시기·방법, 이전 항목, 이용 목적, 보유 기간.
 *
 * 이 값이 동의 화면과 처리방침 두 곳에 쓰인다 — 두 벌로 두면 반드시 어긋나고,
 * 어긋난 쪽이 법정 고지 사항이면 그건 고지를 안 한 것과 같다.
 */

export const AI_VENDOR = {
  /** 법인명. 고유명사라 번역하지 않는다 — 어느 언어에서도 `OpenAI`다 */
  name: 'OpenAI',
  /**
   * 이전받는 국가 — **코드로 갖는다**(ISO 3166-1 alpha-2).
   *
   * 🔴 여기에 `'미국'`이라고 적었다가 **영어·독일어 화면에 한글이 그대로 떴다.**
   *   §9.1 규칙 2(코드/id만 저장, 문구 저장 금지)를 상수에서 어긴 것이고,
   *   화면을 영어로 보고서야 알았다.
   *
   * ⚠ 표시 문구는 `aiConsent.vendorCountry`가 언어별로 갖는다.
   *   **이 코드를 바꾸면 15개 언어의 그 키도 함께 바꾼다.**
   */
  countryCode: 'US',
  /**
   * 🔴 **출시 전에 반드시 채운다. §28-8② 3호가 연락처를 요구한다.**
   *
   * ⚠ **비워둔 이유는 확인하지 못했기 때문이다.** 2026-08-12에 공식 처리방침 페이지가
   *   403을 돌려줬고, **기억으로 이메일 주소를 적지 않았다** — 법정 고지 사항에
   *   틀린 연락처를 적는 것은 안 적은 것보다 나쁘다(위기 상담 전화번호와 같은 원리).
   *
   * 채우면 동의 화면과 처리방침 다목에 자동으로 반영된다.
   */
  contact: '',
} as const;

/** 연락처가 채워졌는가 — 출시 게이트. `scripts/check-ai.mjs`가 이 값을 본다 */
export function vendorContactReady(): boolean {
  return AI_VENDOR.contact.length > 0;
}
