/**
 * AI 사업자 정보 — **한 곳에서만 정의한다.**
 *
 * 「개인정보 보호법」 §28-8②는 국외 이전 동의를 받기 전에 **3호까지** 알리도록 요구한다:
 * 이전받는 자의 성명(법인명)과 **연락처**, 이전 국가·시기·방법, 이전 항목, 이용 목적, 보유 기간.
 *
 * 이 값이 동의 화면과 처리방침 두 곳에 쓰인다 — 두 벌로 두면 반드시 어긋나고,
 * 어긋난 쪽이 법정 고지 사항이면 그건 고지를 안 한 것과 같다.
 *
 * 🔴 **처리방침은 이 상수를 읽지 않는다. 손으로 맞춰야 한다.**
 *   여기 주석이 오래 *"채우면 처리방침에 자동으로 반영된다"* 고 말하고 있었는데 **거짓이었다.**
 *   `features/legal/legal-text.ts`의 국외이전 항목은 **정적 문자열**이고, 실제로 2026-08-19까지
 *   *"사업자명·연락처는 기능 출시 시점에 기재하겠습니다"* 라는 placeholder가 남아 있었다.
 *   주석을 믿고 `check:ai`만 초록인 것을 보면 **다 된 줄 안다.**
 *
 *   왜 자동화하지 않나: 처리방침은 **14개 언어 번역**이 지문(`sourceFingerprint`)으로
 *   정본에 묶여 있다. 동적 값을 끼우면 지문이 무의미해지고, 그 지문이 번역 드리프트를
 *   막는 **유일한 장치**다(`features/legal/fingerprint.ts`).
 *
 * ⚠ **이 파일을 고치면 반드시 함께 한다:**
 *   ① `features/legal/legal-text.ts` 국외이전 항목의 해당 줄
 *   ② 14개 번역 + `npm run legal:stamp`
 *   ③ `npm run legal:html` → push (공개 URL이 낡으면 게시된 고지가 틀린 것이다)
 */

export const AI_VENDOR = {
  /**
   * **법인명**. 고유명사라 번역하지 않는다 — 어느 언어에서도 그대로다.
   *
   * 🔴 `'OpenAI'`(브랜드)가 아니라 **법인명**이다. §28-8② 1호가 요구하는 것은
   *   *"이전받는 자의 성명(법인·단체의 경우 그 명칭)"* 이다.
   *
   * ⚠ **지역에 따라 법인이 다르다.** 공식 처리방침(2026-08-19 실측):
   *   EEA·스위스는 `OpenAI Ireland Limited`, **그 외 지역(=한국)은 `OpenAI OpCo, LLC`**
   *   (1455 Third Street, San Francisco, California 94158, USA).
   *   조각의 사용자는 EEA 배포 제외(§9.1)라 **그 외 지역**이 맞다.
   *   ⏭ UMP를 붙여 EEA를 열면 **이 값이 지역에 따라 갈라져야 한다.**
   */
  name: 'OpenAI OpCo, LLC',
  /**
   * 이전받는 국가 — **코드로 갖는다**(ISO 3166-1 alpha-2).
   *
   * 🔴 여기에 `'미국'`이라고 적었다가 **영어·독일어 화면에 한글이 그대로 떴다.**
   *   §9.1 규칙 2(코드/id만 저장, 문구 저장 금지)를 상수에서 어긴 것이고,
   *   화면을 영어로 보고서야 알았다.
   *
   * ⚠ 표시 문구는 `aiConsent.vendorCountry`가 언어별로 갖는다.
   *   **이 코드를 바꾸면 15개 언어의 그 키도 함께 바꾼다.**
   */
  countryCode: 'US',
  /**
   * §28-8② 3호가 요구하는 **이전받는 자의 연락처**.
   *
   * 🔴 **우리 연락처가 아니다.** 사용자가 *"내 데이터를 지워달라"* 고
   *   **OpenAI에 직접** 요구할 수 있는 창구다. 휘성게임즈 연락처
   *   (`support@vivace-games.com`)는 전자상거래법 §13① 표시 6종으로 **따로** 들어간다 —
   *   두 개를 헷갈려 우리 주소를 여기 적으면 고지를 안 한 것과 같다.
   *
   * ⚠ **우리가 지어낼 수 없다.** OpenAI가 자기 처리방침에 공개한 값을 그대로 옮긴다.
   *   2026-08-19 공식 페이지 실측 원문:
   *     · *"`dpo@openai.com`으로 **데이터 보호 책임자**에게 문의할 수 있습니다"* ← 채택
   *     · *"`privacy.openai.com`이나 `dsar@openai.com`으로 **요청을 제출**할 수 있습니다"*
   *   지정된 **책임자**가 *"이전받는 자의 연락처"* 라는 법 문언에 가장 곧게 대응한다.
   *
   * ⚠ 2026-08-12에는 그 페이지가 **403**이라 못 읽었고, 그래서 비워뒀다 —
   *   법정 고지에 틀린 연락처를 적는 것은 안 적는 것보다 나쁘다(위기 상담 전화번호와 같은 원리).
   *   ⏭ 403은 `WebFetch`에서만 난다. **브라우저로는 읽힌다** — 다음에 같은 벽을 만나면 그쪽으로.
   */
  contact: 'dpo@openai.com',
} as const;

/** 연락처가 채워졌는가 — 출시 게이트. `scripts/check-ai.mjs`가 이 값을 본다 */
export function vendorContactReady(): boolean {
  return AI_VENDOR.contact.length > 0;
}
