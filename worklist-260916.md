# worklist 2026-09-16 (조각)

양식 정본: `C:\project\common\WORKLIST_HANDOVER.md`

## 0. 바로 시작할 것

**25 (1.1.1) 이 게시됐는지 확인하고, 실기기로 "리포트는 서버에만" 이 실제 서버에서 도는지 본다.**

- 설계 정본: `docs/AI_REPORT_SYSTEM.md` §5.7 (리포트는 서버에만 · 탈퇴 후 30일 파기)
- 먼저 확인할 것:
  - Play 콘솔 → 게시 개요(검토 중인지 게시됐는지). 게시됐으면 `common/PLAY_CONSOLE_STATUS.md` 조각 행과 `docs/README.md` 25 절을 고친다
  - 서버가 살아 있는지: `curl -s https://jogak-stg.vercel.app/api/health` (`db: up` 이어야 한다)
- 핵심 요약
  - 어제(9/15) 앱이 리포트를 폰에 저장하지 않고 **늘 서버에서 불러오게** 바꿨다. 앱에서 지우면 서버에서도 지워진다
  - 탈퇴하면 리포트만 30일 뒤 지워지고 이용 기록은 바로 지워진다. 백업에는 일기·사진·태그만 들어간다
  - 서버와 공개 처리방침은 이미 바뀌었다. 25 가 게시돼야 앱이 따라온다
  - 에뮬레이터에서는 가짜 서버로만 확인했다. **실제 서버·운영 DB 로는 한 번도 안 밟았다**
- 실기기에서 볼 순서(구독 중인 구글 계정 필요)
  1. 리포트 탭에서 지난주 리포트 만들기 → 목록에 뜨는가
  2. 그 리포트 삭제 → 목록에서 사라지고 같은 주가 "이미 있어요" 로 막히는가
  3. 다른 주로 하나 더 만들고 앱을 지웠다 다시 깔아 로그인 → 그 리포트가 그대로 보이는가
  4. 비행기 모드로 리포트 탭 → "인터넷에 연결하고 로그인하면 볼 수 있어요" 가 뜨는가
- 주의
  - 🔴 **에뮬레이터로는 실제 서버 확인이 안 된다.** 개발용 로그인은 운영 서버에서 거절된다
  - ⚠ 사용자 계정의 구독이 9/15 에 끝났다. 만들기는 구독이 있어야 되고, 보기·삭제는 로그인만 있으면 된다
  - ⚠ 리포트 생성은 실제 AI 호출이라 원가가 든다(1건 1원 안팎)

## 1. 백로그

### 유저 피해가 실재
- 없음 (25 게시 전까지 옛 앱 24 는 리포트를 폰에 저장한다. 공개 처리방침 제9항 "앱에서 지우면 서버에서도 지워진다" 는 25 부터 참이다. 실사용자 0 이라 급하지 않다)

### 보안·수익
| 항목 | 정본 | 왜 |
|---|---|---|
| 서버 e2e 16개를 한 번도 안 돌렸다 | `docs/README.md` §3 조각 서버 | 로컬 DB(도커 Supabase)가 필요하다. 탈퇴 유예·앱 안 삭제·쓴 기간 목록을 서버 코드로 통과시킨 적이 없다 |
| ⏰ 탈퇴 30일 뒤 리포트가 실제로 지워지는가 | `AI_REPORT_SYSTEM.md` §5.7 · 리퍼 7단계 | 코드만 있다. 테스트 계정으로 탈퇴한 날부터 30일 뒤(예: 9/16 탈퇴 → 10/16 이후) 운영 DB 에서 행이 사라졌는지 본다 |
| 🙋 탈퇴 후 30일 동안 "되찾아 달라" 문의가 오면 어떻게 돌려주나 | `AI_REPORT_SYSTEM.md` §5.7 | 재가입하면 계정 번호가 새로 나와 자동으로 못 돌려준다. 운영 절차를 정하지 않았다(사용자 결정) |

### 검증 도구
| 항목 | 정본 | 왜 |
|---|---|---|
| 개발용 시드가 아직 폰의 리포트 표에 가짜 리포트를 넣는다 | `features/dev/seed-diaries.ts` | 앱이 그 표를 더 안 읽어서 시드해도 화면에 안 뜬다. 화면 확인할 때 헛수고가 된다 |

### 표면 흠
- 없음

### 문서가 코드보다 낡은 것
| 항목 | 정본 | 왜 |
|---|---|---|
| 리포트를 폰에 저장한다고 적힌 옛 절들 | `AI_REPORT_SYSTEM.md` §5.3 · §5.6 · §11.7 · §11.9 | 머리에 "낡았다" 표시만 달았다. 본문을 믿으면 되찾기 동기화·로컬 묘비를 되살리는 사고가 난다 |
| `CLAUDE.md` §1 한 줄 요약 "AI 리포트는 서버가 저장하지 않는다" | `CLAUDE.md` §1 · §5.1 | 8/13 부터 사실이 아니었고 9/15 에 리포트가 서버에만 있게 됐다 |
| `docs/DATABASE.md` 의 폰 리포트 표 | `docs/DATABASE.md` | 이제 읽지 않는 표라는 표시가 없다 |
| 공용 출시 현황 문서가 커밋 안 됐다 | `C:\project\common\PLAY_CONSOLE_STATUS.md` | 조각 행은 고쳤지만 같은 파일에 다른 세션 변경이 섞여 있어 커밋하지 않았다 |

## 2. 오늘 끝낸 것 (2026-09-15)

- `/check` 로 관리자 수정사항 시트(`조각` 탭)를 읽게 붙였다 — `b5e4da7`
- 22 에서 빠진 R8 권장 두 줄을 넣어 23 으로 다시 굽고 릴리스 APK 확인 — `0bb9246`
- 시트 #2: 서버 리포트 보관을 90일에서 탈퇴 시까지로(뒤에 #3 이 다시 바꿈), 15개 언어 처리방침 — `5c38d06` `d225872` (에뮬레이터 릴리스 APK 로 처리방침·동의 문구 확인)
- 시트 #1: 리포트를 만드는 동안 다른 화면으로 나갈 수 있게, 끝나면 어느 화면에서든 알림창 — `3f690a2` `6b9e3e8` (에뮬레이터 · 가짜 서버로 홈에서 알림창까지 확인)
- 23 이 옛 문구를 담은 채 업로드돼 24 로 다시 구움 — `777ee5a`
- **24 (1.1.0) 게시** (사용자 확인 · 게시 개요 대기 0)
- 시트 #4: 구독이 없으면 리포트 기간 선택을 막음 — `1a30d9f` (에뮬레이터 · 버튼 비활성·시트 안 뜸)
- 시트 #3: 리포트를 서버에만 두고 탈퇴 30일 뒤 파기, 백업은 일기·사진·태그만, 15개 언어 처리방침 — `ebca6ea` `b26ed5e` (에뮬레이터 · 가짜 서버로 만들기·삭제·"이미 있어요"·연결 없음 안내·구독 끊은 뒤 보기)
- 서버 배포 + 운영 DB 에 탈퇴 유예 칸 추가, 공개 처리방침 반영 — `7d322e4` (배포 Ready · health db up · 새 라우트 무인증 401)
- 25 (1.1.1) AAB 굽고 검토 제출 — `c47bd24` `a524191` (서명·번들 검사·새 문구 포함 확인)
- 시트 #1~#4 모두 수정여부·확인 여부 체크 + 비고

## 3. 대기 중

| 무엇 | 누구를 |
|---|---|
| 25 (1.1.1) 검토·게시 | Google Play |
| 시트 `조각` #1~#4 최종 확인 | 관리자(사장님) |
| 실기기로 #3 실제 서버 확인 | 사용자 기기 · 구독 계정 |
